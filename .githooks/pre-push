#!/bin/sh
# Pre-push hook: heavy quality gates
set -e

echo "=== pre-push: tests ==="
cargo test --workspace --all-targets

echo "=== pre-push: rustdoc (warnings are errors) ==="
RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --workspace

echo "=== pre-push: all checks passed ==="
