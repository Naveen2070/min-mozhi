# Functional bugs — BUG-61 to BUG-70

One of four numbered ranges split out of the old single-file
`bugs.md`. The index — every bug, its severity, its status and where it
lives — is [`../bugs.md`](../bugs.md); see [`../README.md`](../README.md)
for method.

---

## BUG-61 (HIGH, FIXED 2026-08-13) — `ExprKind::Index`'s base is never hoisted: a bit-select over a composite expression emits unparseable Verilog

**What.** BUG-20 forces a non-identifier **slice** base to a named wire. Nothing
does the same for a **bit-select** base, so `extend(a, 8)[7]` emits a bit-select
applied directly to a parenthesized expression, which is not legal
Verilog-2005.

Two reproductions, both `mimz check`-clean and `mimz compile` exit 0:

| source                | emitted                 | `iverilog -g2005`                                        |
| --------------------- | ----------------------- | -------------------------------------------------------- |
| `y = extend(a, 8)[7]` | `assign y = (a)[7];`    | `syntax error` · `Syntax error in continuous assignment` |
| `y = {a, b}[3]`       | `assign y = {a, b}[3];` | `syntax error` · `Syntax error in continuous assignment` |

`mimz eval` answers correctly (0 for the first, on `a = 0b1111`), so the checker
and the kernel agree with each other and only the emitter is wrong — the same
shape as BUG-49.

**Control case, which pins the diagnosis.** The **slice** form of the identical
expression, `extend(a, 8)[7:4]`, hoists correctly
(`wire [7:0] __mimz_sub_1; assign y = __mimz_sub_1[7:4];`) and agrees with
Icarus at 0. The machinery exists and is correct; `Index` was never wired into
it.

**Cause.** `ExprKind::Slice`'s render arm calls `hoist_slice_base_if_needed`;
`ExprKind::Index`'s bit-select branch (`emit_verilog/expr.rs:703`) does not.
The coverage axis's `Index` arm reads _"a bit-select is 1 bit in both models; a
`mem` read renders as the declared element width"_ and cites
`bug_41_mem_read_operand_of_add_in_concat_matches_icarus` — again a claim about
the **result**, and a citation covering only the mem-read branch. The
bit-select branch's **base** is unexamined in both the code and the Task 4
audit.

**How found.** Round-5 review ([`review-2026-08-13.md`](review-2026-08-13.md)),
same sweep as BUG-60 — probing every classifier arm that returns `None` with an
operand that renders narrower than its mimz width.

**Severity.** HIGH, not CRITICAL — same reasoning as BUG-49: a loud Icarus
elaboration failure at the user's first `iverilog` step, not a silent wrong
value. Not ruled out: whether another synthesis toolchain accepts this text and
assumes some width.

**Fix (2026-08-13, round-5 plan Task 3).** Called the existing
`hoist_slice_base_if_needed` from `ExprKind::Index`'s bit-select render arm,
exactly as `ExprKind::Slice`'s arm already does a few lines away, same
`signed: false` argument. 2 new differentials in
`tests/self_determined_regression.rs`
(`bug_61_bit_select_of_an_extend_hoists_the_base_and_matches_icarus`,
`bug_61_bit_select_of_a_concat_hoists_the_base_and_matches_icarus`). Watched
fail first: reverted just the new hoist call, both failed (real Icarus syntax
error, matching the filed repro), restored, both pass. Coverage doc's `Index`
arm (`expr_kind_self_determined_coverage`) rewritten to state the base is a
separate self-determined position from the result the arm's own `None`
answers, and to cite the new tests. Workspace **1256/1256** (35 suites),
fmt/clippy clean.

**Category** (round-4 taxonomy): _a fix that doesn't recurse into a nested
position_ — BUG-20's slice-base hoist, never extended to the sibling node.

---

## BUG-62 (CRITICAL, OPEN) — the self-determined hoist machinery is inert outside a module body: `fn` bodies, testbench bodies, and parameter-valued widths

**Status:** OPEN at `1bd8b81`. Filed 2026-08-15 by round 6
([`review-2026-08-15.md`](review-2026-08-15.md)).

**Status update (2026-08-15, same day, round-6 plan Tasks 1–3):** ①②③ (fn-body
reduction/nand/concat) and ⑦⑧ (module-parameter-width reduction/concat) are
FIXED and verified against real Icarus — `packit`/`allset`/`nandit` now hoist
correctly inside a `fn` (e.g. `y = 2575`, matching mimz exactly, not 175), and
`&extend(a, W)`/`{ b, extend(a, W) }` with `W` a module parameter now render an
explicit widening concat with no wire needed (`{{(W)-(4){1'b0}}, a}`), correct
at the default width and at an override. ⑨ (reduction in a testbench `expect`)
is FIXED the same way; ⑩ (bit-select in a testbench `expect`) needed a second,
undocumented fix — `emit_testbench` never flushed `self.hoisted_decls` into the
emitted text at all, so a hoisted wire was silently dropped regardless of Task
2 — now flushed via the same `insert_str`-at-a-saved-position `module()` uses.
④⑤⑥ (`fn`-body Index/Slice/Trunc base) are no longer silent OR unparseable:
they now produce a clean `Diag` ("this expression needs a helper wire ... but a
value inside a `fn` body cannot declare one yet") instead, because
hoisting **inside** a `fn` body turned out to need its own guard the moment
Tasks 1–2 made it possible to reach at all — filed as a follow-on under Task 4
in the round-6 plan, not yet fixed (`mimz compile` now exits non-zero with a
real diagnostic rather than 0 with either wrong values or invalid Verilog).
**Remaining open:** the ④⑤⑥ shapes themselves (still diagnosed, not compiled);
a symbolic-width `extend` whose OWN operand's `Kind` also fails to resolve (no
repro currently exercises it); the `W == N` replication-count-0 edge case
(Icarus accepts it in practice, strict Verilog-2005 does not). Full detail:
[`v0.2-class-closure-round6.local.md`](../../plan/v0.2-class-closure-round6.local.md)'s
own Task 1–3 status notes.

**What.** Every hoist call site in `emit_verilog/expr.rs` is gated on
`kinds::infer_kind` resolving a `Kind`, and reacts to `None` by rendering the
text unchanged. `infer_kind`'s only data source is `cur_decls`, which
`module()` builds **once per module** from that module's own signals. Three
emitter contexts therefore run the whole machinery blind:

1. **`fn` bodies** — `render_fn_decl` (`emit_verilog/module/funcs.rs:22`)
   renders the body under the enclosing module's `cur_decls`; the `fn`'s own
   parameters and `let`s are never inserted.
2. **Testbench bodies** — `emit_testbench` (`emit_verilog/testbench.rs:151`)
   constructs its `Emitter` with `cur_decls: Default::default()` — empty.
3. **Parameter-valued width arguments**, inside an ordinary module body —
   `infer_call`'s `Extend`/`Trunc` arm is `const_fold(&args[1])?`, which is
   `None` for a module `parameter`.

Ten reproductions, every one `mimz check` **OK** and `mimz compile` **exit 0**
(`a`/`x` = `0b1111`, `b` = `0b1010`):

| #   | source                                                | emitted                         | mimz      | Icarus                     |
| --- | ----------------------------------------------------- | ------------------------------- | --------- | -------------------------- |
| ①   | `fn allset(x) -> bit { &extend(x, 8) }`               | `allset = (&(x));`              | 0         | **1** — silent             |
| ②   | `fn nandit(x) -> bit { nand(extend(x, 8)) }`          | `nandit = (~&((x)));`           | 1         | **0** — silent             |
| ③   | `fn packit(x, b) -> bits[12] { { b, extend(x, 8) } }` | `packit = {b, (x)};`            | 2575      | **175** — silent           |
| ④   | `fn topbit(x) -> bit { extend(x, 8)[7] }`             | `topbit = (x)[7];`              | 0         | **syntax error**           |
| ⑤   | `fn lowbits(x) -> bits[2] { extend(x, 8)[1:0] }`      | `lowbits = (x)[1:0];`           | 3         | **syntax error**           |
| ⑥   | `fn low2(x) -> bits[2] { trunc(extend(x, 8), 2) }`    | `low2 = (x)[(2)-1:0];`          | 3         | **syntax error**           |
| ⑦   | `module Fuzz(W: int = 8) { y = &extend(a, W) }`       | `assign y = (&(a));`            | 0         | **1** — silent             |
| ⑧   | `module Fuzz(W: int = 8) { y = { b, extend(a, W) } }` | `assign y = {b, (a)};`          | 2575      | **175** — silent           |
| ⑨   | `expect &extend(y, 8) == 0` in a clocked `test`       | `if ((((&(y)) == 0)) !== 1'b1)` | test PASS | **testbench FAIL**         |
| ⑩   | `expect extend(y, 8)[7] == 0` in a `test`             | `((y)[7] == 0)`                 | test PASS | **testbench syntax error** |

③ and ⑧ are BUG-28's original divergence, value for value (2575 vs 175), still
live. ① and ② are BUG-60 in a context its own fix does not reach. ④⑤⑥ are
BUG-20/BUG-49/BUG-61's class.

**Cause.** The shared fallback, at twelve call sites:

```rust
match crate::emit_verilog::kinds::infer_kind(inner, &decls, &self.env) {
    Some(k) => self.hoist_if_needed(inner, x, k, &decls),
    None => x,                    // ← silent, everywhere
}
```

`hoist_width_effect_operand`'s own comment names these exact shapes ("a
`fn`-body local, a module `parameter`, a testbench signal, or a symbolic
parametric width") and calls the fallback "the same, already-correct fallback
these shapes had before this task". It is not correct — it is silent. This is
the same `infer_kind`-returns-`None` residue as BUG-41/46/48/49, whose fixes
each removed one _source_ of `None` without ever making the _fallback_ safe.

**How found.** Round-6 review, sweeping the hoist call sites rather than the
classifier arms — the surface rule (a′) does not cover ([GAP-17](gaps.md)).

**Why no instrument saw it.** The differential fuzzer emits exactly one `fn`
per program, `fn identW(x: bits[W]) -> bits[W] { x }`
(`tests/differential_fuzz.rs:1086`), whose body is a bare parameter, and never
emits a module `parameter` at all. Both regions are unreachable by the
generator's grammar at any depth and under any value policy. The emitted
testbench layer is checked for elaboration only (`tests/icarus.rs:409`), never
for its verdict. No shipped example puts a width-effect expression in a
self-determined position inside a `fn` (`foreach_sum.mimz` comes closest, and
its `acc +% extend(v, 11)` is context-determined, hence benign).

**Severity.** CRITICAL — ③⑦⑧ are silent wrong hardware from checker-clean,
`mimz test`-passing source, and `fn` plus a parametric width is ordinary
second-week code, not a corner.

**Fix shape.** Two independent halves.
_(a)_ Give the non-module contexts a real `decls`: `render_fn_decl` should
build one from the `fn`'s params (`self.width(&param.ty)`) and `let`s
(`LocalLet::inferred_width`), install it for the body, and restore the module's
afterwards; `emit_testbench` should install the DUT module's own `decls`.
_(b)_ Stop failing open: at a position whose _grammar_ requires a named wire (a
slice/bit-select/`trunc` base) an unresolvable `Kind` has no safe rendering and
should be a diagnostic; at the width-mismatch sites a `debug_assert!` on `None`
would have surfaced ①–③ the first time any test compiled a `fn` body.

**Category** (round-4 taxonomy): _a fix that doesn't recurse into a nested
position_, generalised from a nested AST position to a nested **emitter
context** — compounded by _arm present, reasoning wrong_ (the fallback's own
"already-correct" comment).

---

## BUG-63 (HIGH, FIXED 2026-08-16) — a hoist inside `function automatic` emits a module-scope wire that captures the module's signal, after the function that reads it

**Status:** FIXED 2026-08-16 (round-6 plan Task 4's real fix, superseding the
same-day mitigation below). Filed 2026-08-15 by round 6
([`review-2026-08-15.md`](review-2026-08-15.md)).

**Status update (2026-08-16, round-6 plan Task 4, real fix):** `render_fn_decl`
now owns a fresh `fn_hoist_counter`/`fn_hoisted_regs`/`fn_hoisted_stmts` buffer,
never shared with the enclosing module's `hoist_counter`/`hoisted_decls`.
`hoist_if_needed`/`hoist_slice_base_if_needed`, when `self.in_fn_body`, hoist
into a function-local `reg` (`__mimz_fn_sub_N`) instead of diagnosing: the
`reg` is declared before the function's `begin` (spliced in after the whole
body renders, at a saved position, mirroring `module()`'s own `insert_str`
trick one level down), and its value is assigned via a blocking `=` statement
a new `render_fn_operand` helper threads immediately before the statement the
hoisted operand belongs to — so the assignment lands in the right branch and
executes before its use, exactly what a `function automatic`'s sequential
body requires. Verified against real `iverilog`: this bug's own repro
(`pack(c, b)`, `a` param shadowing a module signal) now elaborates clean and
reads the ARGUMENT (`c`), not the module's `a` (256-case-equivalent spot
check: `y = {b, extend(c,8)}` for arbitrary `a != c`, `iverilog`-confirmed);
BUG-62①②③'s `allset`/`nandit`/`packit` shapes (which Task 2 first unlocked and
Task 4's mitigation had been diagnosing rather than compiling) now compile and
match exhaustively across all 256 `(x, b)` pairs against real `iverilog`. The
`in_fn_body` flag stays — it now selects the hoist target (function-local
`reg` vs. module-scope `wire`) instead of gating a diagnostic.

**Status update (2026-08-15, same day, round-6 plan Task 2's own follow-on):**
mitigated, not fixed. Once Task 2 gave `fn` bodies a real `decls`, a hoist can
genuinely fire inside one for the first time (not just this bug's own name-
collision shape) — attempting one still tries to push a `wire`/`assign` pair
into the enclosing module's `hoisted_decls`, which a `function automatic`
cannot legally reference, confirmed against real `iverilog` with the identical
error this bug names. Rather than let that ship as silently-invalid Verilog, a
new `in_fn_body` flag on `Emitter` makes both `hoist_if_needed` and
`hoist_slice_base_if_needed` push a `Diag` instead when set. This closes the
window BUG-63's own fix-shape worried about ("silent the moment the ordering
half is fixed on its own") — the diagnostic fires before wrong-value or
ordering behavior can be observed at all — but the underlying repro
(`pack(c, b)` reading the argument correctly) still does not compile
successfully; it now fails at compile time with a clear message instead of at
`iverilog` elaboration with a confusing one. Real fix (function-local `reg`-
based hoisting) is unstarted.

**What.** BUG-62 above is what happens when the module's `decls` cannot resolve
a `fn` parameter's name. When it _can_ — because a module signal happens to share that name — the
hoist fires with the **module signal's** `Kind` and emits its wire at **module
scope**:

```mimz
fn pack(a: bits[4], b: bits[4]) -> bits[12] { { b, extend(a, 8) } }
module Fuzz { in a: bits[4]  in c: bits[4]  in b: bits[4]  out y: bits[12]
  y = pack(c, b) }
```

```verilog
function automatic [(12)-1:0] pack;
    input [(4)-1:0] a;
    begin  pack = {b, __mimz_sub_1};  end     // reads a module-scope wire
endfunction
wire [7:0] __mimz_sub_1;
assign __mimz_sub_1 = (a);                    // the MODULE's `a`, not the argument
```

```text
iverilog  error: Unable to bind wire/reg/memory `__mimz_sub_1' in `tb.dut.pack'
          : A symbol with that name was declared here. Check for declaration after use.
```

Two defects in one emission: the wire is declared **after** the function that
references it (BUG-45's ordering class, in a new place), and it computes over
the module's `a` while the call passes
`c` — so even correctly ordered it would be the wrong value.

**Severity.** HIGH — Icarus refuses it loudly today. It becomes a silent
wrong-value bug the moment the ordering half is fixed on its own.

**Fix shape.** Falls out of BUG-62's fix _(a)_: with a real `fn`-scope `decls`
the name resolves to the parameter and the hoist is either correct or not
attempted. Until then `render_fn_decl` must not share `hoisted_decls` with the
enclosing module.

**Category** (round-4 taxonomy): _position not analysed at all_ — nobody has
asked what a hoist means inside a `function automatic`.

---

## BUG-64 (HIGH, FIXED 2026-08-16) — every combinational emitted testbench checks before its own stimulus lands; 3 of 6 shipped examples report FAIL on a correct design

**Status:** FIXED 2026-08-16 (round-6 plan Task 5). Filed 2026-08-15 by round 6
([`review-2026-08-15.md`](review-2026-08-15.md)). Independent of BUG-62.

**Status update (2026-08-16, round-6 plan Task 5):** two independent races
closed, both in `emit_verilog/testbench.rs`, both confirmed against real
`iverilog`/`vvp`, neither the one this entry's own "fix shape" named alone.
`TestStmt::Drive` and `TestStmt::Tick` now emit an unconditional `#1;` right
after their own statement (not gated on "does this test ever tick a clock" —
tried first, found insufficient, see below):

1. **`Drive` without a following `Tick`** — this entry's own repro
   (`shift`/`tested_adder`, no clock at all) and half of `enum_encoding`'s
   (`rst <= 0;` immediately followed by an `expect`, inside an otherwise
   clocked test with ticks elsewhere).
2. **`Tick` immediately followed by a read** — found while verifying (1):
   `repeat (n) @(posedge clk)` resumes in the SAME active region as the
   DUT's own `always @(posedge clk)` block firing for that identical edge,
   and an NBA never lands until the active region for that time step is
   entirely done, regardless of relative ordering — so a statement reading
   a clocked output right after `Tick` resumes deterministically sees the
   PRE-edge value, not a maybe-race. `enum_encoding`'s
   `tick(clk); expect state_bits == …` pairs hit exactly this, undetected
   by the original 3-example manual sweep because the plan's "Why" text only
   analyzed the no-clock case. Standard "sample at posedge+delta" testbench
   idiom, not optional — first attempt (gate both `#1`s on
   `test_is_combinational`, a whole-test "does it ever tick" check) reverted
   after this class surfaced, since a test with ticks elsewhere still needs
   the delay at each individual Tick/Drive site.

**Second, independent bug found running the acceptance sweep for real (BUG-65,
this file):** the sweep test this entry's own "fix shape" called for
(`every_emitted_testbench_reports_pass_under_vvp`, `tests/icarus.rs`) had
never existed before — running it once found 50 more FAILs, unrelated to
either race above, across `debouncer`/`fifo`/`pwm`/`uart_tx` × 5 language
variants. Root cause and fix are BUG-65's own entry, immediately below; noted
here because both bugs were required for this entry's own acceptance
criterion ("every shipped example's emitted testbench prints PASS") to hold.

**Verification.** `every_emitted_testbench_reports_pass_under_vvp` (new,
`tests/icarus.rs`) runs every `test`-block-bearing shipped example's emitted
testbench MODULE (one `iverilog`/`vvp` invocation per module, `-s
<module>`, so one module's `$finish` can't cut off a sibling's `$display`)
and asserts PASS with no FAIL — 0 failures across the whole corpus with both
this fix and BUG-65's landed. Full workspace green, fmt/clippy clean.

**What.** `mimz compile --emit-testbench` on a **combinational** design emits a
testbench whose every `expect` reads the value the design had **before** the
test's own stimulus:

```console
$ mimz test examples/english/shift.mimz
1 passed, 0 failed
$ iverilog -g2005 -o shift.vvp shift.v shift_tb.v && vvp shift.vvp
FAIL: expect (var_shift == 12) failed
```

Swept every shipped example with `test` blocks — emitted, elaborated, ran, and
compared verdicts with `mimz test`:

| example         | shape         | `mimz test` | emitted testbench |
| --------------- | ------------- | ----------- | ----------------- |
| `blinker`       | clocked       | PASS        | PASS              |
| `traffic_light` | clocked       | PASS        | PASS              |
| `vilakku`       | clocked       | PASS        | PASS              |
| `enum_encoding` | combinational | PASS        | **FAIL**          |
| `shift`         | combinational | PASS        | **FAIL**          |
| `tested_adder`  | combinational | PASS        | **FAIL**          |

**Cause.** BUG-51's fix made every `TestStmt::Drive` non-blocking (`emit_verilog/testbench.rs:64`),
which is right for a clocked test because `tick(clk)` renders
`repeat (N) @(posedge clk);` and time advances before the `expect`. A
combinational test has no `tick`, nothing advances time, and the `expect`
executes in the same time step as the drive — so the non-blocking update has
not landed. `a = 0; a <= 5; if ((y == 5) !== 1'b1) …` reads `y == 0`.

**It is not always loud.** A vacuous PASS is equally reachable:

```mimz
test "…" for Fuzz { a = 0b1111  expect &extend(y, 8) == 0 }
```

passes in the emitted testbench while `y` is still `0` from its initialiser —
the check never observed the stimulus. That is BUG-54's false-green class,
reintroduced by a different mechanism one round after it was closed.

**How found.** Round-6 review, checking whether the testbench emitter shares
the module emitter's `decls` (it does not — BUG-62 ⑨⑩); the race turned up in
the same experiment.

**Why the suite missed it.** `every_emitted_testbench_passes_iverilog`
(`tests/icarus.rs:409`) asserts only that the emitted testbench **elaborates**
(`iverilog -t null`). The one verdict-level test,
`emitted_testbench_reset_deassert_does_not_race_the_dut`, uses a **clocked**
design — the one shape that works.

**Severity.** HIGH — loud, but it makes the project's own headline claim ("two
independent implementations agree") false for combinational designs, and the
vacuous-PASS variant is silent.

**Fix shape.** Emit a `#1;` after the drive block and before the first `Expect`
when the test declares no clock — or equivalently keep blocking `=` for a test
with no `tick`, since BUG-51's race requires a clock edge to exist. Then
promote the sweep above into the suite: run each emitted testbench and assert
`PASS`, not merely that it elaborates.

**Category** (round-4 taxonomy): _a fix that doesn't recurse into a sibling
position_ — BUG-51's `=`→`<=` applied to every drive, correct only where a
`tick` follows.

---

## BUG-65 (HIGH, FIXED 2026-08-16) — a plain `reg`'s declared reset value is never emitted as an `initial`, so a design read before its first reset pulse is a real 4-state X in Verilog but a defined value in `mimz test`

**Status:** FIXED 2026-08-16. Found 2026-08-16 running BUG-64's own acceptance
sweep for the first time (`every_emitted_testbench_reports_pass_under_vvp`,
`tests/icarus.rs`) — never filed before because no test had ever asserted
PASS across the whole example corpus; only elaboration was checked
(`every_emitted_testbench_passes_iverilog`).

**What.** `ModuleItem::Reg`'s own doc comment states the same safety rule
`ModuleItem::Mem`'s does, near-verbatim: _"the reset value is mandatory
(safety rule: no uninitialized state)."_ `mimz-sim/src/sim/kernel.rs`'s own
test, `regs_init_to_their_reset_value`, confirms the KERNEL honors this
unconditionally: _"Before any tick, a reg holds its (non-zero) reset value"_
— no reset pulse required, matching `spec/01-goals-and-philosophy.md`'s
stated guarantee ("Every reg declares a reset value; a module with regs must
declare a reset"). The EMITTER only ever encoded that value into the
synchronous `if (rst)` branch inside each `always @(posedge clk)` block —
never as a Verilog `initial`, unlike `mem` (`module/mod.rs`'s own
`initial for (…) data[i] = …;` loop, with its own documented ASIC caveat).
A design read before its first reset pulse — directly, as `std/fifo.mimz`'s
"starts empty" test does with zero stimulus at all, or indirectly through a
few cycles of X-tainted comparisons before a register a design USES to gate
other regs (`std/debouncer.mimz`'s `sync1 == out_q`) happens to escape X —
is a real 4-state `X` in the emitted Verilog and a defined value in `mimz
test`, disagreeing on a correct design.

```mimz
module Fifo { clock clk  reset rst  reg count: bits[3] = 0  out empty: bit
  empty = count == 0 }
test "starts empty" for Fifo { expect empty == 1 }
```

```console
$ mimz test fifo.mimz
1 passed, 0 failed
$ iverilog -g2005 -o fifo.vvp fifo.v fifo_tb.v && vvp fifo.vvp
FAIL: expect (empty == 1) failed
```

**Scope.** Not the 3 examples any filing named — a corpus-wide sweep (new
`every_emitted_testbench_reports_pass_under_vvp`, `tests/icarus.rs`) found 50
failures: `std/debouncer`, `std/fifo`, `std/pwm`, `std/uart_tx`, each × 5
language variants (their tamil-pure equivalents: `anuppi`, `varisai`,
`minukki`, `nilaippaduthi`) — every shipped stdlib example whose `test` block
reads a reg-derived value before ever asserting `rst`.

**Two candidate fixes, resolved by checking the kernel first (not assumed):**
patch the 20 example test files to assert `rst` first (would make the
EXAMPLES conform to an emitter limitation), or extend `mem`'s own `initial`
mechanism to plain `reg` (makes the EMITTER conform to the kernel's already-
tested, spec-documented contract). The kernel's own regression test settled
it — `regs_init_to_their_reset_value` is deliberate, not incidental.

**Fix.** `module/mod.rs`: a new loop, immediately after the existing `mem`
one, seeds every `ModuleItem::Reg` with its own `reset` expression via
`initial <name> = <value>;`, with the same "simulation/FPGA-only, an ASIC
flow has no defined power-on default" caveat `mem`'s own note carries — the
synchronous `if (rst)` path is unaffected, still the synthesizable reset.

**Comment wording constraint, found the hard way.** The new note is plain
ASCII natural-language text emitted into every clocked module's `.v` output,
compared byte-for-byte (`tests/golden/*.v`) and — separately —
identifier-anonymized against the pure-Tamil corpus
(`pure_tamil_examples_are_equivalent_to_their_counterparts`,
`tests/examples.rs`), which canonicalizes every non-keyword word-token
(comments included) to `id<N>` in first-occurrence order. A comment
containing an ordinary English noun that happens to also be a real signal
name in that specific example (tried: "state" — collided with
`blinker.mimz`'s own `reg state`; "value" — collided with `counter.mimz`'s
own `reg value`) gets aliased to that signal's OWN id in the file where it
collides and a fresh id where it doesn't, failing the equivalence check
between the English and Tamil renderings of the SAME circuit. Final wording
avoids common single-word nouns; the `mem` note above it survived by luck
rather than by a stated rule, so this constraint isn't written down anywhere
else — noting it here for the next hoist/`initial`/note that adds emitted
prose.

**Verification.** Confirmed against real `iverilog`: `fifo`'s "starts empty"
test (was FAIL at time 0, zero stimulus) and all 50 corpus failures now PASS.
`every_emitted_testbench_reports_pass_under_vvp` (new) sweeps the whole
corpus and would fail if any regressed. Full workspace green, fmt/clippy
clean, `MIMZ_UPDATE_GOLDENS=1` regenerated the affected goldens (every
clocked module's `.v`/`_tb.v` — the `initial` lines are new, expected text,
not a behavior change to anything the goldens were pinning).

**Category** (round-4 taxonomy): _arm present, reasoning wrong_ — `mem`'s own
"no uninitialized state" rule was implemented for one register-like construct
and never asked whether the sibling construct with the identical doc-comment
promise needed the same treatment.
