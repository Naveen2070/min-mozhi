# 2026-08-25

## v0.2.0 release date moved to 2026-08-29

User decision: the planned v0.2.0 public-release date is now **2026-08-29**
(was `2026-08-24` in CHANGELOG/site, `2026-08-20` in ROADMAP). Docs-only
change, no code, spec, or test impact.

Updated every live statement of the release date:

- `CHANGELOG.md` - `[0.2.0]` heading date -> `2026-08-29`. The "amended
  2026-08-24" note inside the Tag-pending blockquote stays as-is (it is a
  historical fact about when the section was last amended).
- `ROADMAP.md` - v0.2.0 row Completed column -> `2026-08-29`.
- `site/src/lib/releases.ts` - v0.2.0 entry `date` -> `"2026-08-29"`.
- `site/src/lib/changelog.ts` - heading-format comment example synced to
  the new changelog heading.

Historical records (`docs/log/*`, `docs/audit/*`, `docs/plan/*`) left
untouched per repo convention - they record what happened on those dates,
not the release plan.

**Verification:** prettier clean on all four touched files;
markdownlint-cli2 clean (256 files, 0 issues). No site tests pin these
values (re-checked by grep).
