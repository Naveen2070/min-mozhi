# 2026-08-20

## Round-8 plan Task 3 — BUG-71: `emit_testbench` must see the DUT's own file consts

### Summary

Fixed BUG-71 on branch `round8-class-closure`: `emit_testbench` never folded a DUT's file- or module-level consts into its own env, so a `const if` referencing one was unevaluable there even though `mimz check`/`mimz compile` (without `--emit-testbench`) resolve the identical design fine. `flatten_items` silently read the failure as `unwrap_or(0)` — false — dropping the port with no diagnostic.

### Fix

Two parts, plus a third call site found while wiring part (1) in.

1. `emit_testbench` (`crates/mimz-core/src/emit_verilog/testbench.rs`) gained a `files: &[File]` parameter (mirroring `emit()`'s own signature — `Project` never retained the raw file list; it discards `TopItem::Const` entirely building its module/enum/bundle/func tables). Seeds its env from the DUT's file consts, then module-level consts (`em.eval_consts_items(&dut.items, dut_file_consts)`, the same fold `module()` does for a directly-compiled module), before resolving test args/parameter defaults on top — overlay order matters, so an explicit test argument still wins on a name collision. Both call sites (`src/commands/compile.rs`, the crate's own test helper) updated.
2. `flatten_items` (`module/instances.rs`) now pushes `consteval::eval`'s own `Diag` (`Result<_, Diag>`, directly reusable) instead of `.unwrap_or(0)` — needed `&mut self` (was `&self`).
3. **Found while wiring (2) in**: `eval_consts_items` — module-level const folding, which `emit_testbench` now calls for the first time — had the identical `unwrap_or(0)`-is-silently-false gap, one function over. Fixed the same way, widened from `pub(super)` to `pub(in crate::emit_verilog)` so `testbench.rs` can call it. Deliberately did NOT extend this to `declare_instance_outputs`/`emit_instances`'s own copy of the same evaluation — they run after `flatten_items` on the same raw items and would only re-diagnose an already-reported failure (the same "don't double-report" reasoning `declare_instance_outputs`'s own doc comment already gives for unknown-module resolution).

### Verification

- All three review repros (port declaration, hoisting `expect`, width divergence) now compile, elaborate under real Icarus, and PASS, agreeing with `mimz test`'s own verdict.
- 4 tests added: `bug_71_file_level_const_if_port_is_declared_and_connected_in_the_emitted_testbench`, `bug_71_file_level_const_if_hoisting_expect_matches_icarus`, `bug_71_const_if_branch_width_divergence_agrees_with_mimz_test` (`tests/self_determined_regression.rs`, alongside the pre-existing `bug_68_*` literal-condition controls, left unchanged) and `bug_71_unevaluable_const_if_condition_is_a_diagnostic_not_a_silent_else` (`crates/mimz-core/src/emit_verilog/tests/consts_scoping.rs` — checker-bypassing unit test, since a real checker-accepted-yet-unresolvable condition couldn't be constructed post-fix: `const if` can't reference the enclosing module's own parameter, E0811, and mimz has no division operator to force a runtime eval failure another way).
- One pre-existing test repointed: `task2_unresolvable_concat_member_is_a_diagnostic_not_a_panic` used BUG-68's own repro to exercise `hoist_unresolved`'s fallback — its own doc comment predicted this exact churn ("any test targeting it necessarily borrows a temporarily-open bug"). This fix makes that repro resolve and compile clean, so it now targets BUG-72 instead (`&trunc(sr << 1, WIDTH)`, still genuinely unresolvable today).
- Full workspace: `MIMZ_REQUIRE_WASM=1 REQUIRE_IVERILOG=1 cargo test --workspace` → **1307/1307**, 35 suites (wasm pkg rebuilt). `cargo fmt --all -- --check` / `cargo clippy --workspace --all-targets -- -D warnings` clean.
- `graphify update .` run.

### Docs updated

- `docs/audit/bugs/bug-71-80.md` — BUG-71 `OPEN` → `FIXED 2026-08-20`, fix description and verification evidence.
- `docs/audit/bugs/bug-61-70.md` — BUG-68 `PARTIALLY FIXED` → `FIXED 2026-08-20` (its own stated closure condition — BUG-71 landing + both repros passing in file-level-const form — is now met).
- `docs/audit/bugs.md` — index status for both + removed from "Still open".
- `docs/plan/v0.2-class-closure-round8.local.md` — Task 3 marked `DONE 2026-08-20` with evidence.

### Next

Task 4 (corpus elaboration sweep as an in-repo test) or Task 5 (`assert_hoists_declared_before_use` diagnose-not-panic in release) — both only required if skipping the RC. Not started — stopping here per instruction to check in after each task.

## Round-8 plan Task 4 — pin the corpus-wide elaboration sweep to `-g2005`

### Summary

Before writing a new test, found that `every_emitted_verilog_passes_iverilog` (`tests/icarus.rs`) — already runs `iverilog -t null` over the whole 226-file corpus via `support::corpus_files()`, with a `checked >= 226` floor, added back in round 7's `8a24f86`. Task 4's own text names it as one of "three existing siblings" the new test would share `corpus_files()` with; the actual gap was narrower than the task's "Why" implied — not a missing corpus-wide sweep, just an unpinned language generation. Extended that test in place (`-g2005` added to its `iverilog` invocation) rather than adding a duplicate fourth loop that would double the runtime for identical coverage.

### Verification

- Hand-built BUG-70's own broken declaration order (a hoisted wire `__mimz_sub_2` referencing `u1_q` before `u1_q`'s own `wire` declaration line, matching the review's Appendix A.10/A.11 shape) as a standalone `.v` file and fed it directly to real `iverilog`: both `-g2005` and Icarus's own default generation reject it identically — `Unable to bind wire/reg/memory 'u1_q' ... declaration after use`. Confirms `-g2005` is a future-proofing pin against Icarus's default generation changing across versions, not today's detection mechanism (which was already `-t null`, unpinned, and already worked).
- Full workspace: `cargo test --workspace` (`MIMZ_REQUIRE_WASM=1 REQUIRE_IVERILOG=1`) → **1307/1307** (no new tests — an existing one gained a flag). `cargo fmt --all -- --check` / `cargo clippy --workspace --all-targets -- -D warnings` clean.
- Runtime recorded in the test's own doc comment: ~14s on this machine (vs. round 8's manual ~3min sweep — no per-file process-pool overhead running in-repo).

### Docs updated

- `tests/icarus.rs` — `every_emitted_verilog_passes_iverilog` doc comment expanded with the Task 4 rationale, the `-g2005` pin, and the verification method.
- `docs/plan/v0.2-class-closure-round8.local.md` — Task 4 marked `DONE 2026-08-20` with evidence and the reasoning for extending rather than duplicating.

### Next

Task 5 (`assert_hoists_declared_before_use` diagnose-not-panic in a release binary) — only required if skipping the RC. Not started — stopping here per instruction to check in after each task.

## Round-8 plan Task 5 — `assert_hoists_declared_before_use` must diagnose, not abort a release binary

### Summary

`assert_hoists_declared_before_use` (`emit_verilog/mod.rs`) was a bare `debug_assert!`, and this workspace ships `[profile.release] debug-assertions = true` — so a violation would abort a real, shipped `mimz compile` with a Rust panic and backtrace instead of exiting non-zero with a message. Same bug pattern round-7 Task 2 removed from `hoist_unresolved` (`module/ports.rs`), ten lines away, the same day.

### Fix

Widened the function into `Emitter`-aware form: `assert_hoists_declared_before_use(em: &mut Emitter, module_text: &str, module_span: Span)`, called from `emit()` with the module's own `m.span` (per-line spans into generated Verilog don't map back to source). The violation branch now pushes a real `Diag` via `em.err(...)` **unconditionally, before** the `cfg!(test)`-gated `debug_assert!` — reordered from `hoist_unresolved`'s own sequence deliberately: it makes the push itself observable by an in-crate test even though `cfg!(test)` forces the panic branch too inside this crate's own unit-test binary.

### Verification

- `task5_declaration_order_violation_is_a_diagnostic_not_a_panic_outside_tests` (`tests/hoist_declaration_order.rs`): wraps the call in `std::panic::catch_unwind` (capturing `em` via a reborrow, `&mut *em`, not a move — no `unsafe` needed), asserting the panic still fires in this binary (matches the two pre-existing `#[should_panic]` fixture tests, unchanged) **and** that the `Diag` was genuinely constructed and pushed to `em.diags` before that panic unwound. `cfg!(test)` is a per-compilation constant, unconditionally true for `cargo test -p mimz-core` — no test compiled inside this crate can directly observe "a real `mimz` binary takes the non-panicking branch" (same limitation `hoist_unresolved`'s identical gate has always had, with no in-repo precedent test either), so this is the strongest evidence obtainable in-process.
- The two existing `#[should_panic(expected = "GAP-18")]` fixture tests needed only a signature update — added `with_test_emitter` (`tests/hoist_declaration_order.rs`) and `test_emitter` (`tests/mod.rs`), mirroring `module/tests.rs`'s own `test_emitter` one level up (no shared helper existed across the two test trees).
- Sanity-checked the real `target/debug/mimz.exe` against a normal example (`examples/english/adder.mimz`) post-fix — compiles clean, exit 0.
- Full workspace: `cargo test --workspace` (`MIMZ_REQUIRE_WASM=1 REQUIRE_IVERILOG=1`) → **1308/1308** (675 mimz-core unit tests, +1 over Task 4's count). No wasm pkg rebuild needed — no public `emit()` signature changed. `cargo fmt --all -- --check` / `cargo clippy --workspace --all-targets -- -D warnings` clean (one `collapsible_if` fix, `let`-chain form).

### Docs updated

- `docs/plan/v0.2-class-closure-round8.local.md` — Task 5 marked `DONE 2026-08-20` with evidence; noted Task 6 (docs corrections) has not been re-verified item by item this round.

### Next

With Tasks 1–5 all closed, the only unconditionally ship-blocking item left unverified is Task 6 (documentation corrections). Tasks 7–9 are non-blocking cleanup/hardening. Stopping here per instruction to check in after each task.

## Round-8 plan Task 6 — documentation corrections

### Summary

Verified all six checklist items rather than assumed done, since four had already been synced incrementally as part of Tasks 1–3's own per-task doc cadence. Two genuine gaps found and fixed:

1. **Item 5** (`docs/audit/README.md` + `audit-log/2026-08.md`): README.md already listed `review-2026-08-18.md`'s row (correct — the established convention lists reviews only, never `.local.md` plan files, since the latter are gitignored). `audit-log/2026-08.md` had the review's own entry but nothing for this plan's remediation work — added a new entry mirroring round 7's own `## 2026-08-18 — Round-7 plan Tasks 1–11` format, covering round-8 plan Tasks 1–5.
2. **Item 6** (the `Diag` AST dump nit): `hoist_unresolved` (`module/ports.rs`) embedded a raw `Expr {…}` `Debug` dump in its user-facing `Diag`, not just its `debug_assert!` text. Split the two: `debug_assert!` keeps the full dump, the `Diag` now carries the same site/grammar-note/rendered-text content without it.

Items 1–4 (BUG-70 filed + BUG-66 safety sentence corrected, BUG-71 filed, BUG-68 → FIXED, GAP-18 → CLOSED) were already correct — confirmed by reading the current file contents rather than trusting memory of having done them.

### Additional corrections found while re-verifying `gaps.md`

- GAP-18's own "Same `cfg!(test)` fix applies" note was stale as of Task 5 landing — added a closure note pointing at Task 5's fix.
- GAP-20's own claim ("no in-repo test elaborates the corpus it ships") was checked directly and found **false even at filing time** — `every_emitted_verilog_passes_iverilog` already existed (round 7's `8a24f86`), the same discovery Task 4 made. Corrected in place; GAP-20 stays OPEN on its real remaining half (Task 9's fuzz vocabulary for the three pre-declaration sites), not the corpus sweep.

### Verification

Full workspace: `cargo test --workspace` (`MIMZ_REQUIRE_WASM=1 REQUIRE_IVERILOG=1`) → **1308/1308** (unchanged — this task touched no test-affecting code paths beyond the `Diag` message text, which no test asserts on beyond a `GAP-16` substring check that still holds). `cargo fmt --all -- --check` / `cargo clippy --workspace --all-targets -- -D warnings` clean. markdownlint/prettier clean on every touched doc.

### Docs updated

- `docs/audit/audit-log/2026-08.md` — new entry for round-8 plan Tasks 1–5.
- `docs/audit/gaps.md` — GAP-18 closure note added (Task 5), GAP-20 correction added (Task 4's real finding).
- `crates/mimz-core/src/emit_verilog/module/ports.rs` — `hoist_unresolved`'s `Diag` no longer carries the raw AST dump.
- `docs/plan/v0.2-class-closure-round8.local.md` — Task 6 marked `DONE 2026-08-20` with evidence.

### Next

Every unconditionally ship-blocking item (Tasks 1, 2, 3, 6) and both RC-avoidance-conditional items (Tasks 4, 5) are now closed. Only Tasks 7–9 remain — non-blocking cleanup (`initial #0` narrowing) and optional hardening (fuzz vocabulary). Stopping here per instruction to check in after each task.

## Round-8 plan Task 7 — narrow `initial #0` to the case that needs it

### Summary

Round-7 Task 3 added `#0` to every `mem`/`reg` power-on seed unconditionally — the sole content of all 33 golden diffs at the time. The reasoning (a hoisted operand is a continuous `assign` racing the `initial` block in time-0's active region) is sound only when the seed's own rendered value actually hoists a wire; for the entire shipped corpus, none does, so the guard was firing 33 times for a race that can only happen in a case none of them are.

### Fix

Both the `mem`-init loop and the `reg`-reset loop (`crates/mimz-core/src/emit_verilog/module/mod.rs`) now record `self.pre_decl_hoisted_decls.len()` immediately before rendering their own value (`self.expr(depth)`/`self.expr(init)` for `mem`, `self.expr(reset)` for `reg`) and compare the length after: growth means that render pushed a new hoisted `wire`/`assign` pair, so `#0` is still emitted; no growth means a bare `initial`. Reuses the existing hoist-buffer bookkeeping directly rather than re-scanning the rendered text, so the guard cannot be silently dropped by a text-matching false negative — only by nothing actually having hoisted.

### Verification

- All `initial #0` lines across `tests/golden/` (base + tamil-pure + showcase, 33 files — matches the plan's own estimate) reverted to a bare `initial`, regenerated via `MIMZ_UPDATE_GOLDENS=1 cargo test --test examples goldens` and `--test showcase`; confirmed `grep -rl "initial #0" tests/golden/` returns nothing afterward.
- Two new differentials prove the guard still fires where it matters: `task7_mem_init_delay_guard_still_fires_when_the_init_value_hoists` and `task7_reg_reset_delay_guard_still_fires_when_the_reset_value_hoists` (both reuse BUG-66 A2/A3's own hoisting shape, `{ b, extend(a, 8) }`), plus a control, `task7_mem_init_delay_guard_is_dropped_when_the_init_value_does_not_hoist`, for the literal-init case (`tests/self_determined_regression.rs`). The pre-existing `bug_66_a2`/`bug_66_a3` Icarus-oracle differentials pass unchanged.
- Full workspace: `cargo test --workspace` (`MIMZ_REQUIRE_WASM=1 REQUIRE_IVERILOG=1`) → **1311/1311**, 35 suites. `mimz-wasm` rebuilt (`wasm-pack build crates/mimz-wasm --target web --release`) — the emitted text changed even though `emit()`'s own public signature didn't, so the wasm parity test needed a fresh `pkg/`. `cargo fmt --all -- --check` / `cargo clippy --workspace --all-targets -- -D warnings` clean.
- `graphify update .` run.

### Docs updated

- `docs/plan/v0.2-class-closure-round8.local.md` — Task 7 marked `DONE 2026-08-20` with evidence.

### Next

Only Tasks 8–9 remain in this plan: Task 8 is release mechanics, gated on the ship decision; Task 9 is optional fuzz-vocabulary hardening. Neither was requested this round. Stopping here per instruction to check in after each task.

## Round-8 plan Task 9 — teach the fuzz generator the instance-port-connection pre-declaration site (partial)

### Summary

Gate 5's differential fuzzer (`tests/differential_fuzz.rs`) was structurally blind to the axis BUG-66/BUG-70 lived in: instance connections were always a plain identifier, `mem`/`reg` inits were always literal `0`. Implemented the instance-connection third of this (the one both bugs reached through); the `reg`-reset and `mem`-init thirds turned out to need a separate oracle path, not a small addition, so they're left open rather than forced in.

### Fix

`gen_special_leaves`'s array-instance branch now connects the second generated instance's input through `extend(<first instance's own output>, growth)`, nested as a concat member, whenever there's room for a real growth plus a nonzero pad (`w2 >= w + 2`) — BUG-70's own reproduction shape verbatim (`{ b, extend(u1.q, 8) }`).

Two structural missteps corrected empirically, not by inspection, before landing on this construction:

1. A bare `extend(s.q, w2)` alone as the WHOLE connection compiles fine even with Task 1 reverted — `extend`'s target width is already explicit, so no hoist fires when it's the sole top-level expression. Only nesting it as a concat MEMBER reaches the hoist machinery.
2. Disabling `declare_instance_outputs`'s pre-pass alone doesn't revert Task 1 — it reproduces a different failure (an undeclared implicit net), since Task 1 replaced the declare mechanism rather than relocating it. A faithful revert needed the old inline-declare `Dir::Out` arm restored in `instance()` too.

### Verification

- With both correctly reverted, the generated seed fails `mimz compile` itself with GAP-18's widened invariant ("declared name `s_q` referenced before its own declaration"); with the fix restored, it compiles clean. This is the plan's own acceptance criterion, verified by hand.
- New permanent test: `task9_instance_port_connection_reaches_a_hoisting_expression_within_400_seeds` (finds the shape at seed index 15 within the first 400 fresh clocked seeds).
- `differential_fuzz_clocked_generates_checker_valid_programs` (1000 seeds) and `differential_fuzz_clocked_matches_icarus` at `MIMZ_DIFF_FUZZ_CLOCKED_N=500` both clean — the new shape is checker-valid and agrees with real Icarus at a depth well past where it first appears.
- Full workspace: `cargo test --workspace` (`MIMZ_REQUIRE_WASM=1 REQUIRE_IVERILOG=1`) → **1312/1312**, 35 suites. `cargo fmt --all -- --check` / `cargo clippy --workspace --all-targets -- -D warnings` clean.
- `graphify update .` run.

### Docs updated

- `docs/audit/gaps.md` — GAP-20 status update: instance-connection vocabulary done, `reg`/`mem` vocabulary's real blocker (the kernel's own compile-time-constant requirement for reset/init values) documented, gap stays OPEN on that remaining half.
- `docs/plan/v0.2-class-closure-round8.local.md` — Task 9 marked `PARTIALLY DONE 2026-08-20` with the full construction, both corrected missteps, and an honest scope note on what's left.

### Next

Only Task 8 (release mechanics, gated on the ship decision) and the `reg`/`mem` vocabulary half of Task 9 (needs a separate Icarus-only oracle, a follow-up round) remain open in this plan. Neither requested. Stopping here per instruction to check in after each task.

## Round-8 plan Task 10 — BUG-72: give symbolic-width `trunc` the same self-determined-position fallback `extend` already has

### Summary

`try_widen_symbolic_extend` (`expr.rs`) recovered a self-determined position from `infer_kind` returning `None` for a symbolic-width `extend(x, W)`, but had no equivalent for `trunc(x, W)` — a checker-accepted, `extend`-symmetric construct that instead fell through all 8 self-determined-position call sites to `hoist_unresolved`'s diagnostic. One `&` away from a shipped example (`shift_register.mimz`).

### Fix

Simpler than even the plan's own anticipated shape: no new hoist-base/part-select/signed-wrap logic was needed. Added a `Builtin::Trunc` arm to `try_widen_symbolic_extend` that just returns the caller's own already-rendered `text` unchanged — every one of the 8 call sites already computes it via `render_shift_ctx_operand`, which dispatches through the ORDINARY `Builtin::Trunc` render arm (`x[(n)-1:0]`), and that rendering is already correct at a self-determined position regardless of width symbolicness: a Verilog part-select's own width is always exactly `n` bits by construction, unlike `extend`'s ambient-context-dependent passthrough. Widened the function's signature to accept that pre-rendered text (threaded through all 8 call sites) rather than re-deriving it from `args[0]`/`args[1]`, which would have left the first hoist of a composite base dead and unreferenced.

### Verification

- Two named differentials added and watch-fail-first confirmed (`bug_72_trunc_symbolic_width_reduction_operand_matches_icarus` — the plan's own filed repro verbatim; `bug_72_trunc_symbolic_width_concat_member_matches_icarus` — same defect at a different self-determined position, proving the fix isn't narrow to one call site). Temporarily disabling the new arm reproduces BUG-72's own GAP-16 diagnostic exactly on both; restoring it passes both.
- `shift_register.mimz`'s golden unchanged by this fix (its own 1-line diff this round is Task 7's, unrelated).
- Full workspace: `cargo test --workspace` (`MIMZ_REQUIRE_WASM=1 REQUIRE_IVERILOG=1`) → **1314/1314**, 35 suites (no wasm rebuild — `try_widen_symbolic_extend` is private). `cargo fmt --all -- --check` / `cargo clippy --workspace --all-targets -- -D warnings` clean.
- `graphify update .` run.

### A necessary consequence

Fixing BUG-72 retired `task2_unresolvable_concat_member_is_a_diagnostic_not_a_panic`'s own repro (its third repointing). Rather than assume no live repro remained, checked the suite's own `HOIST_CALL_SITES` documentation first — the "encoding operand" entry already described and hand-confirmed a distinct, still-open shape: an `if`/`match`-wrapped `EnumConstruct` passed directly as `encoding()`'s own argument (an intermediate named `wire` short-circuits it; only the direct-argument form reaches the fallback). Filed as **BUG-74** (MEDIUM, OPEN) — not a new discovery, just giving an already-found gap a bug number and a permanent test. Repointed the rotating test there.

### Docs updated

- `docs/audit/bugs/bug-71-80.md` — BUG-72 `OPEN` → `FIXED 2026-08-20`; BUG-74 filed (new entry).
- `docs/audit/bugs.md` — index and "Still open" table updated for both.
- `tests/self_determined_regression.rs` — `HOIST_CALL_SITES`'s module doc gained a short Task 10 note (the 8 affected entries' "None branch" prose now also covers `Trunc`, not repeated per-entry); the "encoding operand" entry updated with BUG-74's citation.
- `docs/plan/v0.2-class-closure-round8.local.md` — Task 10 marked `DONE 2026-08-20` with evidence.

### Next

This plan's only remaining open items are Task 8 (release mechanics, gated on the ship decision) and the `reg`/`mem` vocabulary half of Task 9 (needs a separate Icarus-only oracle). BUG-74 is a new, separate, not-ship-blocking item for a future round. Neither requested this round. Stopping here per instruction to check in after each task.

## Round-8 plan Task 8 — release mechanics: v0.2.0 prepared, no RC

### Summary

The plan's last open task. Ship decision taken on evidence rather than on a label: Tasks 1, 2 and 4 all landed, with Task 2 in its **widened** form (GAP-18 CLOSED for real, not the narrow fallback), which is exactly the review's stated condition for skipping the RC. Tag chosen: **`v0.2.0` directly, no `v0.2.0-rc1`**.

### Version bump

`0.1.0` → `0.2.0` in the root `[package]` and `[workspace.package]`. Found a fourth version string the review's own list missed: `crates/mimz-wasm/Cargo.toml` carried its own literal `version = "0.1.0"` instead of inheriting, unlike `mimz-core`/`mimz-sim` which both use `version.workspace = true` — a workspace bump would have left the wasm crate silently behind. Switched it to `version.workspace = true` rather than hand-editing the literal, so the four crate versions cannot drift apart at the next bump either.

`crates/mimz-core/src/version.rs` needed no edit: `COMPILER_VERSION` is `env!("CARGO_PKG_VERSION")` and its banner test reads that constant, not a literal. `EDITION_HISTORY` deliberately untouched — the language axis did not move (keyword set v1, no new edition, so `mimz translate` needs no migration rule for this release). Verified on the real binary: `mimz --version` → `mimz 0.2.0 (compiler)` / `edition wingless-butterfly-2026-1 (language)`.

**Zero golden churn**, which is not luck: every golden comparison strips the `// Generated by mimz <version>` banner line (`tests/examples.rs`, `tests/grammar.rs`, `tests/compile_string.rs`, `tests/wasm_parity.rs`, and `mimz-bench`'s metrics) — that is what those strippers exist for. `git status` confirms zero files under `tests/golden/`.

### Known-issues list

Re-derived from current status lines rather than copied from the review's own draft, which predates Tasks 3 and 10: BUG-71 is now FIXED and GAP-18 is now CLOSED, so the two items the review named as "whatever is still open" are both closed and would have been wrong to ship. Counted the entries directly — 73 filed, **69 fixed, 4 open**. Along the way found `bugs.md`'s own range table still reading 3 for BUG-71–80 (BUG-74 had a row in the "Every bug" table but was missing from the count); corrected to 4.

Shipped list: **GAP-1** (HIGH, the v0.3 direction, as the review required), **GAP-20** (HIGH, the `reg`/`mem` fuzz-vocabulary half Task 9 left open), **BUG-32/38/39/74** (all MEDIUM). Also checked the review's own warning about BUG-69/GAP-19 — both are already FIXED/CLOSED at HEAD and correctly absent.

### The withheld claim

Stays withheld, and is now withheld _explicitly_ rather than by omission: `RELEASE_NOTES.md` has a "What it does **not** claim" paragraph naming the sentence and pointing at GAP-1 as the reason. The defensible replacement claim is reproduced **verbatim** from `round-8-suggestion.local.md` §5.

### Release gate — all five re-run at this state

1. BUG-70's three tests pass under real `iverilog`/`vvp`.
2. The widened invariant's 7 tests pass; silent across the corpus.
3. BUG-68's two filed reproductions (file-level `const` forms) pass under real `vvp`, plus the width-divergence case.
4. Corpus clean: all 226 files elaborate at `-g2005`; 50-file/90-module testbench floor holds; zero golden diffs from this task.
5. **Gate 5 CLEAN at 5000/5000** — `tools/gate.sh`, exit 0, 6 passed, 1205.18 s.

### Verification

- Workspace **1314 passed / 0 failed** (35 suites, `MIMZ_REQUIRE_WASM=1 REQUIRE_IVERILOG=1 cargo test --workspace`) — identical to Task 10's count, confirming the bump changed no observable behavior.
- `wasm-pack` was absent from this machine. Installed it (v0.15.0) and rebuilt `crates/mimz-wasm/pkg` rather than running the suite with `wasm_parity` silently vacuous — that vacuous-pass mode is the exact failure GAP-19 closed, and skipping it here would have quietly reopened it. Generated `package.json` confirmed at `"version": "0.2.0"`.
- `cargo fmt --all -- --check` / `cargo clippy --workspace --all-targets -- -D warnings` clean (the one "warning" in the output is the build script's git-hooks notice, not a lint). Prettier + markdownlint clean on every touched file.

### Docs updated

- `RELEASE_NOTES.md` — rewritten for v0.2.0 (the release pipeline reads line 1 as the GitHub Release title, the rest as the body).
- `CHANGELOG.md` — `[Unreleased]` → `[0.2.0] — 2026-08-20`, plus a `### Fixed` summary of the eight-round audit series and the `### Known issues` list.
- `docs/audit/bugs.md` — stale BUG-71–80 range count 3 → 4.
- `docs/code/09-walkthrough-counter.md`, `docs/how-the-compiler-works.md`, `spec/06-editions.md` — emitted-banner samples bumped to 0.2.0; `docs/code/13-tooling.md`, `docs/guide/11-toolchain.md`, `spec/06-editions.md` — `mimz --version` sample output bumped.
- `site/src/pages/downloads.astro` — v0.2.0 release entry prepended per that file's own documented convention.
- `docs/plan/v0.2-class-closure-round8.local.md` — Task 8 marked `DONE 2026-08-20` with the full evidence; the release-gate section records the re-run result.

### Deliberately not done

**The tag is not created or pushed.** Everything the release pipeline reads is staged, but tagging is an irreversible outward-facing action and waits for an explicit go-ahead. Note ordering when it happens: `downloads.astro`'s new asset links 404 until the tag exists, so the tag should land before or with the site deploy, not after.

### Next

This plan is complete except Task 9's `reg`/`mem` fuzz-vocabulary half, which is tracked as GAP-20 rather than as a plan item and needs a separate Icarus-only oracle. Open for a future round: GAP-1 (v0.3 direction), GAP-20, BUG-32/38/39/74. Stopping here per instruction to check in after each task.

---

## Round 9 — v0.2.0 release verification (`docs/audit/review-2026-08-20.md`)

Independent verification of the eight checks round 8's plan specified for this
round. Scope held: no full sweep, no re-derivation of rounds 1–7, no hunt for a
ninth emitter context. **8 of 8 pass. Ship call: `v0.2.0` directly, no RC.**

### What was reproduced, not read

- **BUG-70's three constructions** rebuilt from Appendix A.10/A.11, the
  `repeat`-array variant constructed independently. All three elaborate at
  `-g2005`; two agree with `mimz test` and `vvp`; the `mem`-init case gives
  Icarus 778 through a hand-written testbench (it has no `mimz test` side —
  `mimz-sim` rejects a non-constant `mem` init with E0201, which is exactly why
  the in-repo test uses the `icarus_only_clocked` oracle).
- **The widened invariant fires on a faithful Task-1 revert** (pre-pass call
  disabled **and** `instance()`'s pre-Task-1 inline `Dir::Out` declaration
  restored — disabling the pre-pass alone reproduces a different failure, as the
  plan says). Exit 1 naming identifier and both lines: `u1_q` twice, `u__1_q` for
  the array case. It is a `Diag`, not a panic, so **round-8 Task 5 was verified in
  the same run** on a binary where `cfg!(test)` is false.
- **Silent across all 226 files with the fix in — and also with it reverted**,
  confirming the plan's own note that no shipped design reaches BUG-70's shape.
  The corpus sweep is a regression net, not a detector.
- **BUG-71's three filed repros** (file-level `const` form; the `bug_68_*`
  literal-condition controls confirmed still present as controls) all PASS under
  real `vvp` and agree with `mimz test`, width divergence included.
- **BUG-72 fixed, BUG-74 open**, both reproduced from their filed entries.
- **Task 9's acceptance criterion re-derived** rather than trusting the seed
  index: Task 1 reverted, clocked differential at N=400 fails in 5.0 s on
  "declared name `s_q` referenced before its own declaration".
- **GAP-20's deferral checked against the kernel**: both `reg` reset and `mem`
  init are rejected by `mimz-sim` with E0201, so the Icarus-only-oracle argument
  holds.
- Corpus 226/226 compile and elaborate; 50 files / **90 testbench modules / 90
  PASS**; 33 goldens re-derived line by line as **116 pure `initial #0` →
  `initial` pairs**, zero value change; suite **1314/1314**; **gate 5 CLEAN at
  5000/5000**, exit 0, 1655.85 s.

### Decision: gate 5's wall time is not scored as a regression

- **Context:** 1655.85 s against round 8's 1100.66 s baseline and Task 8's
  1205.18 s re-run.
- **Decision:** record the number, do not treat it as a fuzz-throughput
  regression.
- **Why:** the review's own 226-file compile sweep, 226 `iverilog` elaborations
  and ~140 `vvp` runs were contending for the same 16 GB machine for most of that
  window. A clean comparison needs an idle box. Reporting a regression from a
  knowingly contended measurement would be worse than reporting no measurement.
- **Impact:** noted in the review (Part 5) and the audit-log entry.

### Three documentation defects found and fixed — no behaviour defect

1. **`strip_string_literals`'s doc comment was wrong**
   (`crates/mimz-core/src/emit_verilog/mod.rs`). It claimed no `$display` is ever
   emitted into a plain module body, making the string-literal guard defensive.
   `assert` emits one at both module-item and `on`-block level, and with the
   optional message omitted the string's text is the **rendered condition** —
   real identifiers, verbatim. Constructed the false positive the guard prevents:
   `cover(a[0] == 1)` plus `assert(a[1] == 0, "beware __cover_0_count")`, where
   the comb-`cover` block declares that name at the very end of the module. **No
   corpus example uses `assert` at all**, which is why the shape had never been
   exercised. Comment rewritten as load-bearing, with the reason the naive quote
   toggling is still safe (mimz's lexer has no string escape; E1002 rejects a
   quoted message before emission).
2. **Two stale "BUG-73 not fixed here" notes** (`docs/audit/gaps.md` GAP-18 item
   4, and round-8 plan Task 2's status note) contradicting commit `6d30146`,
   which fixed it. Reproduced fixed at HEAD (loud "declared more than once", exit
   1. and corrected both.
3. **Known-issues framing over-claimed.** `RELEASE_NOTES.md` / `CHANGELOG.md`
   read "Open at tag time, all with a written entry" while listing 2 of 8 open
   gaps — GAP-8 (no division operator) being the one a user meets fastest.
   Framing now states exactly what the list is (every open bug, the two HIGH
   gaps, the two most user-visible MEDIUM ones, pointer to the full ledger);
   GAP-8 and GAP-2 added; BUG-74's description narrowed to its real reachability
   (a **direct** `encoding()` argument — binding to a named `wire` first is
   unaffected).

### Recorded for future rounds, not blocking

- **`.portname(` is defensive only.** No false positive or false negative is
  constructible: every `wire`/`reg` declaration precedes every instance line by
  construction. Correct safety net; recorded so the next round does not re-derive
  it.
- **Residual narrowing in the widened invariant.** It skips
  `function`/`endfunction` blocks entirely, so `__mimz_fn_sub_N` regs — which
  round 7's narrow scan **did** watch — are no longer covered; safe today only
  because `render_fn_decl` splices them at a fixed `regs_pos` before `begin`.
  Only `wire`/`reg` lines populate the declaration map, so a body `integer` or
  `localparam` is outside it. Worth a sentence in GAP-18 if that code is touched
  again.
- **Header heuristic checked corpus-wide**: exactly one `);` line per module
  across all 243 emitted modules, so the `header_end == None` branch that would
  restore BUG-73's shape as a false positive is unreached.
- **BUG-72's hoisted base takes a literal width from the parameter default**
  (`wire [1:0] __mimz_sub_1` at `WIDTH = 1`) while the part-select bound stays
  symbolic. Unreachable from mimz (E0402 rejects a `WIDTH: 8` override at the
  instantiation), but a Verilog-level override of the emitted module would not
  behave as it reads. Pre-existing dumb-emitter property, not introduced by that
  fix.
- **Harness note:** running a testbench file's modules in one `vvp` image reads
  60 PASS, not 90 — the first module's `$finish` cuts off the rest. The in-repo
  test already builds each with `-s <module>` and documents why.

### Docs updated

- `docs/audit/review-2026-08-20.md` — new, the round-9 review.
- `docs/audit/README.md` — round-9 row added to the review table.
- `docs/audit/audit-log/2026-08.md` — round-9 entry appended.
- `docs/audit/gaps.md`, `docs/plan/v0.2-class-closure-round8.local.md`,
  `RELEASE_NOTES.md`, `CHANGELOG.md`,
  `crates/mimz-core/src/emit_verilog/mod.rs` — the three corrections above.

### Next

The tag is still not created or pushed — unchanged from Task 8, and still waiting
on an explicit go-ahead. Open for a future round: GAP-1 (v0.3 direction), GAP-20,
BUG-32/38/39/74.
