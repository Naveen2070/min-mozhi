# 2026-08-11

## Round-4 plan (`docs/plan/v0.2-class-closure-round4.local.md`) - Tasks 1–5

Drafted the round-4 remediation plan from
[`review-2026-08-10.md`](../audit/review-2026-08-10.md), following round-3's
plan template. Tasks 1 (GAP-14) and 2 (BUG-52) were already done in
`46b9577` (a prior commit today), verified against the working tree before
being marked done in the plan. Tasks 3–5 implemented and verified below, all
on branch `bug-41-42-43-self-determined-class-closure`.

### Task 3 - BUG-55: recursed BUG-47's signed-`>>` hoist into `if`/`match` branches

Root cause one level deeper than the filed repro: `expr.rs`'s `IfExpr`/`Match`
rendering always hoisted branch shifts with `allow_shift: false` - correct
only when the `if`/`match` itself sits in a context-determined position
(BUG-24), wrong whenever it sits in a self-determined one instead (concat
member, `extend()`'s argument, a `min`/`max`/`abs`/cast/`fn`-call operand,
…), since `eval_ctx` propagates the SAME `expected_width` into every
branch/arm. Fixed by factoring `IfExpr`/`Match` rendering into
`if_expr_subst`/`match_subst` (new `allow_shift` parameter) plus a new
`render_shift_ctx_operand` helper that every one of the ~20 existing
self-determined call sites now renders its child through, instead of
`expr_subst` directly - one mechanism, not a special case bolted onto
`Extend` alone.

New differential `bug_55_signed_shift_right_inside_match_wildcard_arm_matches_icarus`
watched fail first (`git stash` of just the emitter change: kernel says 0,
Icarus says 3 - the exact filed values), then pass restored.
`bug_24_regression_shift_in_if_branch_stays_unhoisted` (the guard this fix
must not break) stays green - its `if` sits at a bare top-level assignment
RHS, a position the new call sites never touch. `self_determined_regression`
55/55; full workspace 1234/1234 (the 2 `wasm_parity` failures are a
pre-existing stale-WASM-build issue, confirmed by reproducing them
identically on unmodified HEAD). `fmt`/`clippy -D warnings` clean.

### Task 4 - audited the `ExprKind` coverage axis's arm citations (scoped)

Fixed the three miscalibrated citations the review named in
`tests/self_determined_regression.rs`'s `expr_kind_self_determined_coverage`:

- **`Unary`** was `NotApplicable`, citing `self_determined.rs`'s own
  catch-all as confirmation - a catch-all confirms nothing, and BUG-52
  proved the claim itself false (`~extend(a,8)` genuinely differs). Now
  cites `bug_52_unary_not_of_an_extend_in_a_concat_matches_icarus`.
- **`IfExpr`/`Match`** cited `bug_41_if_expr_operand_of_add_in_concat_matches_icarus`/
  `shape_match_operand_of_add_in_concat_matches_icarus` - both place the
  shape as an operand of `+`, a position where `+`'s own hoist masks this
  exact defect. Now cite the `bug_52_*` tests that place it as the concat
  member/replication-body itself.
- Rewrote the axis's own module doc, still claiming `infer_kind` "is NOT
  wildcard-free over `ExprKind`" - false since round 3's Task 3, and never
  updated to ask the same question of `verilog_self_determined_kind`, which
  Task 2 (round 4) just closed too.

Spot-verified the remaining 9 citations exist and, structurally, carry no
"masked by an enclosing operator" risk (either a structural `None` with
nothing to compare, or already tested at the exact position the arm
covers) - so the "operand of `+`" naming pattern wrong for `IfExpr`/`Match`
isn't a problem for those. **Scoped, not exhaustive**: this closes the
review's three named findings plus the doc-staleness they sit under, not a
fresh Icarus re-verification of all ~62 arms across both matches/axes,
which the review itself sizes at "a few days" of bounded work. That larger
pass remains open.

### Task 5 - BUG-54: `expect`'s guard now fails on an `x`-valued comparison

`emit_test_stmts`'s `Expect` arm (`emit_verilog/testbench.rs`) rendered
`if (!(cond)) FAIL`; under Verilog's 4-state logic `!(x)` is `x`, and `if`
treats `x` as false, so an all-`x` design (never reset, never driven)
silently printed PASS. Changed to `if ((cond) !== 1'b1) FAIL` - case
inequality against `1'b1` is never itself `x`, so it fails correctly on
`x`/`z` as well as a plain `0`.

New unit test `expect_guard_uses_case_inequality_not_plain_negation` pins
the emitted text directly (mimz's own "every reg/mem has a mandatory init
value" design makes a genuine `x` at an `expect` point unreachable from
valid source alone in practice, so this is a text-shape test, not a full
Icarus round-trip) - watched fail against the pre-fix rendering, restored.
Re-verified `tests/icarus.rs`'s `emitted_testbench_reset_deassert_does_not_race_the_dut`
(previously vacuous per BUG-54's own filing) is now actually sensitive to
BUG-51's regression class: reintroducing BUG-51 (`<=` → `=`) now correctly
prints FAIL and fails the test, where before this fix it printed PASS
either way. 14 testbench goldens regenerated (`tests/golden/*_tb.v`) - every
diff is exactly and only the guard-shape change, confirmed by inspection.
Full workspace green (same 2 pre-existing `wasm_parity` failures, nothing
else); `fmt`/`clippy -D warnings` clean.

`bugs.md`: BUG-54 now `(HIGH, FIXED 2026-08-11)`.

### Task 6 - BUG-53: array-instance `decls` keyed off the loop counter, not the folded index

`insert_repeat_instance_output_kinds` (`emit_verilog/module/ports.rs`) keyed
every `repeat`-body instance's `decls` entry as `{inst}__{i}` using the raw
loop counter `i`, diverging from `inst_name`'s own `{inst}__{eval_const(inst.index)}`
render whenever the index isn't the bare loop variable (`s[i + 1]`), and only
scanned `r.items` one level deep, missing instances nested inside another
`repeat` or a `const if`. Same "arm present, data source under-populated"
shape as BUG-48. Fixed by splitting the function into three
(`insert_repeat_instance_output_kinds` /
`insert_repeat_instance_output_kinds_in` /
`insert_array_instance_output_kinds`), keying by the same folded `index`
expression `inst_name` uses and recursing into nested `Repeat`/`ConstIf`/`ForEach`
the way `emit_instances` already does - one shared shape of traversal instead
of two that can drift.

The review's own repros still fail in `mimz sim` (`S0125`/`S0126`) even
though `mimz check` accepts them, so `differential_clocked` couldn't be used
for the new tests; added `emitter_only_clocked_check` instead - bypasses the
kernel and checks the emitted Verilog directly against real Icarus. Three new
tests (`bug_53_offset_array_instance_index_matches_icarus`,
`bug_53_nested_repeat_array_instance_matches_icarus`,
`bug_53_const_if_in_repeat_array_instance_matches_icarus`) watched fail
pre-fix at the exact filed values (Icarus 174, declared 350) via `git stash`
of just the emitter change, pass restored. A fourth test
(`bug_53_control_case_non_zero_base_identity_index_still_hoists`) pins the
review's own control case (non-zero base, identity index) as a regression
guard, using `differential_clocked` since that shape simulates fine. Full
workspace green (same 2 pre-existing `wasm_parity` failures, nothing else -
no golden mismatches, since this touches `decls` population, not emitted
text); `fmt`/`clippy -D warnings` clean.

`bugs.md`: BUG-53 now `(HIGH, FIXED 2026-08-11)`. The check/sim/emit
disagreement table BUG-53 exposed (checker accepts, simulator rejects,
emitter silently miscompiles) is **not** resolved by this fix - tracked as
Task 8, still open.

### Task 7 - appended the two nightly-depth-only seeds to the fuzz corpus

Both BUG-52 and BUG-55 were only found at fuzz depths past the per-PR default
of 400 (indices 449 and 925 out of 2000), so without a corpus entry either
could silently regress and never be caught again short of a nightly run.
Appended `tests/fixtures/fuzz-seeds/comb.txt`'s `12649355  # i=925  BUG-55`
and `tests/fixtures/fuzz-seeds/clocked.txt`'s `202428078  # i=449  BUG-52`,
each in ascending-`i` order alongside the existing entries. Ran
`differential_fuzz_matches_icarus`/`differential_fuzz_clocked_matches_icarus`
(`REQUIRE_IVERILOG=1`) - both green, confirming the seeds replay clean now
that Tasks 2 and 3 have landed the fixes. `fmt`/`clippy -D warnings` clean.

### Doc sync - BUG-55's stale `OPEN` heading

`bugs.md`'s BUG-55 heading still read `(CRITICAL, OPEN)` after the fix above
landed; `README.md` already said "BUG-55 fixed" but `bugs.md` itself was
never updated. Corrected the heading to `FIXED 2026-08-11` and added the
same "Fixed" verification paragraph style BUG-53/54 already carry (root
cause, mechanism, test names, watched-fail-then-pass, suite numbers) -
`bugs.md` had the fix's own **What**/**Cause**/**Fix** sections but no
closing verification paragraph.

### Task 4 - started the real per-arm audit; found BUG-56

Began the actual reasoning-audit Task 4 asks for (round 4's scoped citation
fix only covered the 3 named findings, not a fresh per-arm pass). Verified,
as checkable facts rather than assertions:

- `Builtin::Clog2`'s "never reaches a rendered self-determined position"
  claim: the checker rejects it in any VALUE position with E0407
  (`checker/widths/ops/builtins.rs`) except a width/const/param context,
  which is a separate emit path (`bits[clog2(N)]`, never `expr()`/
  `expr_subst`, the function every self-determined call site actually
  routes through).
- `Builtin::SyncDoubleFlop`/`SyncPulse`'s same claim: grammar-restricted to
  the direct target of an `On` drive / `Wire` init, lowered by
  `ast::expand_sync_prims` before the module body reaches `expr()` at all -
  backed by a load-bearing `unreachable!()` in `expr.rs` if that pre-pass
  ever stops covering every call site.
- `ExprKind::FnCall`'s citation (`bug_41_fn_call_operand_of_add_in_concat_matches_icarus`)
  isn't subject to BUG-52's "masked by the enclosing operator" risk: a fn's
  rendered width is source-independent (the emitted Verilog function
  declares its own return width), so unlike `IfExpr`/`Match`/`Min`/`Max` it
  never needs to recurse into the call to see a mismatch - nothing for an
  enclosing `+` to mask.

**Found a new gap the same way BUG-52 was found - by checking a `None`
claim against real Icarus instead of trusting the prose.**
`ExprKind::Int`'s classifier arm claims "nothing to compare"; true only when
the literal is the DIRECT concat/replicate member (checker-rejected pre-emit,
E0405). False one level deeper: `{ b, a & 15 }` compiles clean through
`mimz check`/`mimz compile`, but `verilog_literal` renders the literal
unsized (`'sd15`), and real Icarus refuses to elaborate it -
`Concatenation operand "(a)&('sd15)" has indefinite width`. Same failure in
a replication body (`{2{ a & 15 }}`). Filed as
[BUG-56](../audit/bugs.md) (HIGH - a loud Icarus error, not a silent
miscompile, same class as BUG-49) - not fixed, per Task 4's own rule that an
audit finding gets its own bug and its own fix rather than a patch folded
into the audit. `Bool` checked too and is fine (`expr.rs` always renders a
sized `1'b1`/`1'b0`). Updated the coverage doc's `Int`/`Bool` citations and
`self_determined.rs`'s own module comment to state the now-verified,
narrower truth instead of the blanket claim that let this sit unverified.

Roughly 6 of ~62 arms independently re-verified this session (3 `Builtin`
arms shared across both matches, `FnCall`, `Int`, `Bool`). The rest of the
`ExprKind`/`Builtin` arms on both the gate (`infer_kind`/`infer_call`) and
classifier axes remain, and three of the four axes still have no formal
per-arm coverage doc (only the classifier's `ExprKind` axis
(`expr_kind_self_determined_coverage`) does) - continuing next session.

### Task 4, batch 2 - `Field`/`Index`/`Slice`/`Binary` verified, found BUG-57, fixed two more wrong-position citations

Continued the per-arm pass. `Field`/`Index`/`Slice` all render as a bare
identifier/part-select/memory-read - no wrapped sub-expression inside, so
(unlike `IfExpr`/`Match`/`Unary`/`Int`) no enclosing operator can mask a
mismatch regardless of position; confirmed by reading their `expr.rs`
rendering directly rather than trusting the "operand of `+`"-shaped
citations. `Binary`'s citation confirmed to place the shape as the direct
concat member (the position its own claim is about).

**Found the same "citation exercises a different mechanism" class BUG-52
found for `IfExpr`/`Match`, twice more.** `Concat`/`Replicate`'s citations
(`bug_36_trunc_of_a_concat_hoists_the_base_first`,
`shape_replicate_nested_in_trunc_hoists_the_base`) both actually exercise
`Builtin::Trunc`'s own unconditional `None` arm and its separate
`hoist_width_effect_operand` base-hoist mechanism - `Trunc` never recurses
via `self_determined_operand_width`, so neither test ever asked
`verilog_self_determined_kind`'s own `Concat`/`Replicate` arms anything.
Built a real position for it (a concat/replicate as `extend`'s argument,
which DOES recurse), confirmed both against real Icarus before writing the
test (`{c, extend({a,b},12)}` → 41205; `{c, extend({2{a}},12)}` → 41005,
both matching mimz's own semantics), added
`shape_concat_operand_of_extend_in_a_concat_matches_icarus`/
`shape_replicate_operand_of_extend_in_a_concat_matches_icarus`, corrected
both citations to point at them instead.

**Found a second new bug the same way.** `ArrayLit`'s citation ("the
checker rejects it upstream") is false: `mimz check` accepts `[a,a,a][0]`
clean, `mimz compile` panics rendering it -
`unreachable!("Task 8 or Task 9 wires this up")` in `expr.rs`'s general
expression renderer (not this axis's own `ArrayLit => None` arm, which is
never reached - the panic fires one level up, rendering the `Index`'s
`ArrayLit` base) - and it fires at ANY position, not a self-determined one
specifically. Filed as [BUG-57](../audit/bugs.md) (MEDIUM, same bar as
BUG-40: a crash on checker-accepted source, not a silent miscompile - not
fixed here, per Task 4's own rule). `BundleLit` checked too, and is
actually stronger than "checker-rejected": the `Type { .. }` literal syntax
is PARSER-restricted to a `Wire` init/`Drive` RHS - 3 independent parse
failures confirmed (bare, parenthesized, as a fn-call argument, all E1101)

- so it can never reach a general sub-expression position at all; `expr.rs`'s
  own `BundleLit => "0".into()` fallback is dead code for any parseable
  source. Corrected the coverage doc's `BundleLit`/`ArrayLit` citations and
  both `kinds.rs`'s and `self_determined.rs`'s shared 3-arm comment (which
  attributed all three to one "checker forbids it" claim) to state each
  variant's own, individually-verified reason.

`self_determined_regression` 61/61 (57 + 4 new this session: 2 differentials
above plus the 2 already added last entry). fmt/clippy clean.

Roughly 12 of ~62 arms now independently re-verified across this session.

### Task 4, batch 3 - `SignedCast`/`UnsignedCast` recursion never proven against a real mismatch, new differential closes it

Checked the `Builtin` axis's RECURSING arms (`SignedCast`/`UnsignedCast`/
`Encoding` - forward to their own argument's classification rather than
answering unconditionally, structurally the same shape `Min`/`Max` has,
which is exactly where BUG-42 shipped from).
`matrix_signed_unsigned_cast_roundtrip_in_concat_matches_icarus`'s own
comment already admitted the gap: it only proves the recursion is a no-op
over an already-matching operand (`unsigned(signed(a))` around a plain
`Ident`), never that the recursion actually catches a real one. Built a
real mismatch three levels deep (`unsigned(signed(extend(a, 8)))`),
confirmed by hand and against real Icarus BEFORE writing the test (1291,
matching mimz) that the emitter hoists correctly through all three levels,
then added
`matrix_signed_unsigned_cast_recursion_catches_a_mismatched_operand_matches_icarus`.
No bug this time - the arms were already right, just never tested against
the case that would have shown otherwise. `Encoding`'s identical recursion
mechanism remains untested against a real mismatch specifically (noted in
the `Call` arm's coverage-doc citation rather than re-derived a third
time - diminishing returns). `Extend`/`Abs`/`Trunc`/`Nand`/`Nor`/`Xnor`/
`Min`/`Max` spot-checked too: all direct concat-member citations, no
masking risk, sound as filed. `self_determined_regression` 62/62 (61 + 1
new); fmt/clippy clean.

Roughly 14 of ~62 arms now independently re-verified across three batches.

### Task 4, batch 4 - closed `Encoding`'s recursion question, added the classifier `Builtin` axis coverage doc

Closed `Encoding`'s remaining recursion question WITHOUT a differential -
checked structurally, not assumed: `Encoding`'s argument is always
enum-typed, and no enum-typed sub-expression can render narrower than its
own fixed width the way `extend(a,8)` does for a `bits` value
(`Ident`/`Field` render at a declared wire's width; `EnumConstruct` pads
every field with a SIZED literal to the enum's fixed width, `emit_verilog/
expr.rs` ~line 1098 - the same padding discipline BUG-56's own fix
generalizes; an `IfExpr`/`Match` branch is that arm's own concern, BUG-52).
No shape exists to build the `extend`-style mismatch `SignedCast` needed a
test for, so this closes as a documented fact rather than a forced test.

Then closed one of the three missing coverage docs from round-4's own
scoped Task 4 work: added `builtin_self_determined_coverage`
(`tests/self_determined_regression.rs`), exhaustive over `Builtin` (14
variants, no wildcard - mirroring `expr_kind_self_determined_coverage`'s
own convention), citing every arm audited across all four batches this
session. Verified the exhaustiveness is real: deleted the `Clog2` arm,
confirmed `error[E0004]: non-exhaustive patterns: &Builtin::Clog2 not
covered`, restored. `self_determined_regression` 62/62 (unchanged -
documentation plus one closed question, no new differential this batch);
fmt/clippy clean.

The classifier axis (`verilog_self_determined_kind`, both its `ExprKind`
and `Builtin` sub-matches) now has a complete, compile-enforced coverage
doc. The gate axis (`infer_kind`/`infer_call`) still has neither.

### Task 4, batch 5 - closed the last three coverage docs, all GATE-side

Added `expr_kind_infer_kind_coverage` (`infer_kind`'s own `ExprKind` match,
17 arms), `binop_infer_kind_coverage` (`infer_binary`'s own `BinOp`
sub-match, 20 variants), and `builtin_infer_call_coverage` (`infer_call`'s
own `Builtin` match, 14 arms) - `tests/self_determined_regression.rs`. Each
states the GATE's own different risk profile up front: a wrong GATE arm
sizes every hoisted wire directly and is what the classifier is compared
against, so it tends to surface as a plain wrong value in ANY test
exercising the shape, not specifically a self-determined-position one -
which is why most arms cite a `kinds.rs`-internal unit test (the sharpest
evidence, already existing for 11 of the arms) rather than a fresh
differential. Verified all three are genuinely exhaustive: deleted
`ExprKind::Match`, `BinOp::Coalesce`, `Builtin::Clog2` in turn, confirmed
`error[E0004]` each time, restored. No new bugs this batch - closing the
audit-trail structure, not new hunting.

**All four matches now have a complete, compile-enforced, per-arm coverage
doc.** That closes Task 4's STRUCTURAL "done when" bar. What remains open
is DEPTH: several arms still cite "exercised end-to-end" rather than a
dedicated proof, and roughly two-thirds of the ~62 arms (mostly GATE-side,
now all catalogued with an honest citation) haven't each been
independently stress-tested against real Icarus the way BUG-52/BUG-56/
BUG-57's own arms were.

### Next

BUG-56 and BUG-57 both need their own fix (make `verilog_literal` emit a
sized literal; decide whether `[lit][idx]` is checker-rejected or
emitter-implemented). Task 4's remaining depth: stress-test the GATE-side
arms now catalogued but not independently re-verified, particularly
`IfExpr`/`Match`'s "first resolvable branch wins" shortcut and the grouped
`BinOp` arms sharing one code path. Task 8 (resolve the check/sim/emit
disagreement BUG-53 exposed) and beyond remain open in the round-4 plan.
GAP-14's own `gaps.md` status stays `OPEN` until the release gate is
actually re-scored at nightly depth (5000/5000) per its own standing rule -
that re-score is still pending, tracked as part of the plan's closing
"Release gate - re-run before shipping" section, not any single task.
