# 2026-07-03

## Merge: phase-2-default-and-const-if → develop

- `phase-2-default-and-const-if` merged into `develop` (16 commits: default assignments + item-level const if, spec v0.2.17).
- Commits now on `origin/develop` at `6b03e9f`.

## Full-codebase review (audit)

- `docs(audit): add full-codebase review findings for 2026-07-03` (956afbd).
- Reviewed branches `phase-2-interfaces-bundles` and `phase-2-packages-namespacing` at HEAD.
- **14 findings** across Security (4), Performance (7), Code quality (3). All confirmed still present on both branches.
- Key items: S1/S2 — sim/eval paths skip checker, causing panics on malformed input (same class as ad08bdd/c26704a/874769d fix, root cause S3: run checker before elaborate). P1–P3 — simulator String allocation hot spots.
- Full report: `docs/audit/review-2026-07-03.md`

## Rebase: phase-2-interfaces-bundles → develop

- Rebased `phase-2-interfaces-bundles` onto `origin/develop` after the merge above landed.
- 16 duplicate commits (default + const if) auto-dropped. 19 bundles commits replayed cleanly on top — zero conflicts.
- Branch now sits on `origin/develop` HEAD (`6b03e9f`) with 19 unique commits.

## Feature: Statement-based function bodies & early return (Part 1)

- **Context:** Functions in min-mozhi previously only accepted a set of local declarations followed by a single expression body. This made guard clauses and complex control flow difficult to express.
- **Implemented Changes:**
  - **AST:** Redefined function bodies (`stmts + tail` expression replacing the old `locals + body` expression style) (`2151295`).
  - **Lexer:** Activated the `return` / `thirumbu` / `திரும்பு` keywords (`9f872a6`).
  - **Parser:** Added parsing logic for statements (`let`, `if`, and `return`) within function bodies (`771f56e`).
  - **Checker/Name Resolution:** Extended name-resolution (`e76303d`) and call-graph collection (`586c738`) to walk function statements/tail.
  - **Type Checking:** Width-check return expressions against the declared return type (`cdd65d6`) and added error `E0812` to reject unreachable statements following a return (`ec180e4`).
  - **Verilog Emitter:** Built continuation-passing style (CPS) Verilog lowering for early returns and `if` conditions in function bodies to generate standard, flat Verilog (`ce7cd92`).
- **Other Changes:**
  - Updated controlled loop description and clarified spec details in `docs/Ideas/language_plan.md` (`0b1d035`).
  - Added a downloads page to the site for downloading release assets per platform (`a7cec42`).
  - Updated GitHub CI checkout step branch reference (`eae0b9c`).
