# 2026-07-14

## WASM pkg rebuild — parity fix

The `crates/mimz-wasm/pkg/` WASM binary was stale — built before `foreach` support existed. The `wasm_parity::all_examples_work_in_wasm` test failed because `foreach_fill.mimz` compiled correctly on native CLI but produced parser errors (`E1101`/`E1102`) in the WASM playground.

**Fix:** rebuilt with `wasm-pack build crates/mimz-wasm --target web --release`, picking up the `foreach` keyword + parser + checker changes from the July 12–13 work. Parity test now passes — WASM and CLI produce byte-identical Verilog.
