# 2026-08-22

## Repo-wide doc audit — docs vs source, spec vs `lang/*.toml`, `.local.md` de-referencing

7 parallel subagent passes (6 initial + 1 follow-up), each owning a disjoint
doc-tree slice, cross-checked every factual claim against live source/tests
and fixed drift. No `docs/log/`, `docs/archive/`, `docs/superpowers/`, or any
`*.local.md` content was touched (rules-exempt / private).

**What was done:**

- `spec/*.md` vs `lang/*.toml` + lexer/parser/checker/morph source: EBNF
  gained `default`/`extern module`/`??` (real, shipped, never added to the
  grammar); ~15 stale `src/*.rs` paths fixed to the post-split `crates/`
  layout; keyword-localization count corrected 33→34 of 75; added spec/02
  prose for checker codes E0802/E0803/E0813/E0419; reworded the orphaned
  `struct` reservation (its original purpose shipped as `bundle`, not
  `struct` — un-reserving is a design call, left flagged not decided).
- `docs/code/*.md` (14 files + full `test-map/` subtree, 58 files): fixed a
  fixture-count bug (87 mislabeled as module-only, really 70 module + 17 tb),
  a retired sim-code row (`S0125`), 2 fabricated placeholder test names in
  `width-rules-conformance.md`, and several stale example/Icarus test counts
  (23→37, 10→16, etc.).
- `docs/guide/*.md` + `stdlib/`: already accurate (prior sync commit held up)
  — only 3 orphaned "Since v0.2.14/15" version breadcrumbs fixed (CHANGELOG
  is squashed to a single `[0.2.0]` entry, sub-tags don't resolve).
- `docs/source-guide/*.md`: added real coverage for 9 previously-unmentioned
  source files (`translate.rs`, `pretty/*`, `explain.rs`, `version.rs`,
  `lint.rs`, both crate roots, `mimz-bench/metrics/scaling.rs`) plus 5 stale
  counts.
- Root docs + `docs/plan/*.md` (non-local) + `docs/Ideas/*.md`: Phase 4 status
  ⚪→🟡 in `docs/README.md`, `phase-4-web-presence.md` 🟡→🟢 (shipped
  2026-06-24, verified against that day's log); `docs/architecture.md` gained
  3 missing test files; live-CI facts corrected (`nightly-bench`/`fuzz-nightly`
  cadence, branch-protection status — confirmed via `gh api` that `master` is
  still unprotected).
- `docs/audit/` (excl. `audit-log/`): GAP-17 flipped OPEN→CLOSED with citations
  (`tests/self_determined_regression.rs:2595`, `emit_verilog/self_determined.rs:48-51`);
  33 `.local.md` references dereferenced into standalone prose across
  `gaps.md`/`hardening.md`/`security.md`/`bugs/*.md`/`README.md`.
- Two stale in-source rustdoc/bench comments fixed to match the doc corrections:
  `crates/mimz-core/src/morph.rs:28` ("33 of 36" → "34 of 75"),
  `benches/compile.rs:38` ("six passes" → "nine passes").

**Totals:** 82 files touched (80 doc + 2 source-comment-only), 50
`.local.md` references dereferenced repo-wide, 1 confirmed status flip
(GAP-17), 1315-test workspace total re-verified live and cross-checked
against every per-suite doc.

### Decision: leave `struct` reserved, don't un-reserve

- **Context:** doc audit found `struct`'s spec/03 description ("bundles/interfaces,
  post-Phase 2") stale — that feature shipped as `bundle` on 2026-07-16, and
  `interface` is separately reserved for a distinct still-unshipped concept
  (named port bundles). `struct` itself has no feature attached to it anymore.
- **Decision:** reworded the description to state the truth (orphaned, no
  current owner) in both `lang/keywords.toml` and `spec/03-keywords-trilingual.md`,
  but did not release the reservation.
- **Why:** un-reserving a word is a "major design decision" (R11/R13) —
  releasing it now and regretting it later (a v0.1.0-frozen English column
  can't easily re-claim a word) costs more than leaving it reserved-but-idle
  costs today. Flagged for a deliberate maintainer call instead of guessing.
- **Impact:** `lang/keywords.toml`, `spec/03-keywords-trilingual.md`. No code
  change — the word was already rejected as an identifier before this session
  and still is.

**What's next:** two flags left open, both structural/account-level, not
doc-fixable — `Agents.md`/`AGENTS.md` byte-identical duplicate at repo root
(recommend deleting the stray-cased one), and the unprotected `master` branch
on GitHub. Full audit trail: `docs/plan/scratch/doc-audit-report.local.md`
(private working note, not part of this log).
