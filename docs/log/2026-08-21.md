# 2026-08-21

## Multi-Pass Adversarial Documentation, Spec, and Language Data Synchronization

### Summary

Conducted an exhaustive three-pass adversarial audit and synchronization across the entire Min-Mozhi repository documentation, language specifications, language data files, and root markdown files (excluding all private `*.local.md` files). Brought every document into 100% precision with the live v0.2.0 compiler, simulator, tooling, and test suite.

### Key Changes & Findings

#### 1. Language Specification & Data Files (`spec/`, `lang/`)

- **`lang/keywords.toml`**: Added missing documentation comments for previously undocumented keywords: `sim` (hardware emulation block), `bind` (peripheral binding), and `speed` (real-time simulation throttling rate). Confirmed all 44 active keywords and 12 reserved words.
- **`spec/03-keywords-trilingual.md`**: Updated `sync` token description from "still-unimplemented CDC synchronizer" to **shipped in v0.2.0** (`sync.double_flop` and `sync.pulse` lowering in `crates/mimz-core/src/ast/sync_prim_lower.rs`). Cross-referenced all 44 keywords in English, Tanglish, and Tamil.
- **`spec/05-simulator.md`**: Replaced private/local design document links with public references to [`docs/code/14-hardware-emulation.md`](../docs/code/14-hardware-emulation.md) and [`docs/Ideas/hardware_emulation.md`](../docs/Ideas/hardware_emulation.md).
- **`spec/02-syntax-and-grammar.md` & `spec/06-editions.md`**: Verified grammar productions for bundles, `T?` valid-bundle sugar, `??` coalesce operator, `assert`/`cover` statements, `extern module` Verilog FFI, `foreach` loops, `sync loop`, and the two-axis versioning model (`wingless-butterfly-2026-1` / v0.2.0).

#### 2. User Guide & Cheatsheet (`docs/guide/`)

- **`01-getting-started.md`**: Corrected station 3 pass count from "seven passes" to "nine passes" in the mental model pipeline section.
- **`03-types-and-values.md`**: Fixed `match` expression syntax and array literal context; documented `T?` valid-bundle sugar and `T[N]` fixed-size arrays.
- **`04-signals.md`**: Clarified wire inline initialization vs output port drives.
- **`05-operators.md`**: Documented `??` valid-bundle coalesce operator (unwrap and OR-mux forms).
- **`06-builtins.md`**: Wrapped `sr << 1` in `trunc(sr << 1, 8)` to adhere to the lossless shift-growth rule introduced in v0.2.0.
- **`08-sequential-logic.md`**: Fixed `sync loop` grammar and documented the 4 generated interface signals (`<name>_start`, `<name>_running`, `<name>_done`, `<name>_result`).
- **`11-toolchain.md`**: Documented `mimz doctor` (alias `mimz env`), `mimz repl`, `mimz eject std`, and the `--workspace` requirement for contributor test commands.
- **`12-cheatsheet.md`**: Expanded table to all 44 keywords across English, Tanglish, and Tamil; added entries for `T?`, `T[N]`, `??`, `assert`, `cover`, `Bundle`, and `encoding(e)`.

#### 3. Compiler Internals & Maintainer Manual (`docs/code/`, `docs/source-guide/`, `docs/architecture.md`)

- **`docs/code/01-pipeline.md`**: Added dedicated **Workspace Architecture** section describing the 3-crate layout (`mimz-core`, `mimz-sim`, `mimz-wasm`) + root `mimz` shell crate and the pure/impure boundary.
- **`docs/code/04-ast.md`**: Fixed stale statement on `LocalLet` type inference — documented `inferred_width: Cell<Option<u32>>` and sized `reg [W-1:0]` emission in Verilog functions.
- **`docs/code/06-diagnostics.md`**: Documented 75 checker error codes (`ALL_CHECKER_CODES`), 33 localized templates, and simulator runtime range `S01xx`–`S05xx` (79 codes).
- **`docs/code/13-tooling.md`**: Added dedicated sections for `stdlib.rs`, `lint.rs`, `mimz repl`, and `mimz eject std`.
- **`docs/source-guide/`**: Updated file counts across all chapters (`parser/tests/`: 14 files, `checker/tests/`: 12 files, `emit_verilog/tests/`: 12 files, `sim/`: 9 root files + 3 subdirectories, `sim_errors.rs`: 81 tests).
- **`docs/architecture.md` & `docs/how-the-compiler-works.md`**: Updated compiler directory trees to accurately reflect modular subdirectories (`emit_verilog/module/`, `checker/names/`, `checker/widths/`, `sim/elaborate/`, `sim/value/`, `sim/harness/`, and AST lowering modules).

#### 4. Plans, Ideas & Root Markdown Files

- **`docs/plan/phase-4-ecosystem.md`**: Updated status header to `🟡 in progress / ongoing` (WASM, playground, docs site, `mimz repl`, stdlib v1, LSP shipped).
- **`docs/plan/phase-2-ir-synthesis.md`**: Marked completed CDC synchronizers.
- **`docs/Ideas/language_plan.md`**: Updated `Enum.Variant(...)` status to shipped (`✅`) in the HDL Parity Gap Analysis table and the open items list.
- **`docs/Ideas/hardware_emulation.md`, `simulator_ideas.md`, `architectural_ideas.md`**: Added status banners and marked shipped v0.2.0 features.
- **`README.md`, `ROADMAP.md`, `Agents.md`, `CLAUDE.md`, `CHANGELOG.md`, `RELEASE_NOTES.md`, `CONTRIBUTING.md`**: Verified metrics (1315 passing tests across 35 test suites, 200 example files, 44 keywords, 3-crate workspace).

### Verification

- `cargo test --test docs_sync` → **5/5 PASS**
- `cargo clippy --workspace --all-targets -- -D warnings` → **0 warnings (PASS)**
- `cargo test --workspace` → **1315/1315 PASS** across 35 test suites
- `npx markdownlint-cli2` → **0 issues across 252 markdown files (PASS)**
- `npx prettier --check` → **0 issues (PASS)**
