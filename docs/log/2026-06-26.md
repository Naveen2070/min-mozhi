# Dev Log — 2026-06-26

## Documentation audit (`stdlib-modules` branch)

Full pass over all docs to sync stale numbers with current code state.

### Numbers updated

| Location                                        | Old                          | New                                                         |
| ----------------------------------------------- | ---------------------------- | ----------------------------------------------------------- |
| README.md badge + status line                   | 433 tests                    | 465 tests                                                   |
| README.md examples                              | 23 × 4 + stdlib + tamil-pure | 23 × 4 complete + 6 stdlib each + 13 tamil-pure (129 total) |
| docs/how-the-compiler-works.md                  | 433 tests                    | 465 tests                                                   |
| docs/code/10-test-map.md header                 | 469 tests (2026-06-25)       | 465 tests (2026-06-26)                                      |
| docs/code/10-test-map.md lib unit               | 315                          | 311                                                         |
| docs/code/10-test-map.md golden count           | 41                           | 42                                                          |
| docs/code/10-test-map.md icarus row             | "all 72 examples"            | "all 129 examples"                                          |
| docs/source-guide/10-ecosystem.md fixture count | 72 .mimz files               | 73 .mimz files                                              |
| docs/source-guide/10-ecosystem.md golden count  | 41                           | 42                                                          |

### File counts updated

| Location                                    | Module    | Old        | New      |
| ------------------------------------------- | --------- | ---------- | -------- |
| CLAUDE.md, architecture.md, source-guide/\* | lexer/    | 3 files    | 4 files  |
| CLAUDE.md, architecture.md, source-guide/\* | parser/   | 7 files    | 9 files  |
| CLAUDE.md, architecture.md, source-guide/\* | checker/  | 9 files    | 12 files |
| CLAUDE.md, architecture.md, source-guide/\* | sim/      | 8 files    | 9 files  |
| CLAUDE.md, architecture.md, source-guide/\* | commands/ | 9/10 files | 11 files |

### New modules added to maps

- `stdlib.rs` — added to all codebase maps, source-guide chapter tables, tests/ listing
- `analysis.rs` — added to all codebase maps and source-guide chapter tables
- `sim/mod.rs` — added to architecture.md sim/ listing (was missing)

### Files changed

- `README.md`
- `CLAUDE.md`
- `docs/architecture.md`
- `docs/how-the-compiler-works.md`
- `docs/code/README.md` (sync stamp → 2026-06-26)
- `docs/code/10-test-map.md`
- `docs/source-guide/README.md`
- `docs/source-guide/01-overview.md`
- `docs/source-guide/03-lexer.md`
- `docs/source-guide/04-parser.md`
- `docs/source-guide/06-checker.md`
- `docs/source-guide/08-simulator.md`
- `docs/source-guide/09-tooling-and-entry.md`
- `docs/source-guide/10-ecosystem.md`

## Documentation content pass (second pass)

Found and fixed content gaps — newly added files that were listed in maps but had no actual documentation section:

### Missing content added

| File              | Added to                                                                                                                   |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `src/stdlib.rs`   | New section in `source-guide/02-foundations.md`: catalog structure, `resolve`/`is_std_namespace`/`eject_to`, override path |
| `src/analysis.rs` | New section in `source-guide/09-tooling-and-entry.md`: `SymbolIndex`, `build_index`, `resolve_at`, `completions`           |

### Table gaps fixed

| Location                               | Gap                                                                                                                           |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `docs/code/10-test-map.md` fmt section | Header said 9 tests, table had 7 rows — added `output_to_the_input_path_round_trips` and `unknown_to_flavor_is_a_clean_error` |

### Chapter table updated

- `source-guide/01-overview.md`: ch02 now lists `stdlib`; ch09 now lists `analysis.rs` and `lsp.rs`

## Ideas-to-plan reconciliation pass

Audited every idea in `docs/Ideas/` against the committed phase plans to find
anything triaged-feasible (or notably flagged) but never tracked as a work item.

**Finding:** propagation is near-complete — all of `architectural_ideas.md`
(section 6 triage), `language_plan.md` (sections 7/8/9/10), and `simulator_ideas.md`
(sim asserts, step-back, REPL/`tui`, 4-state, `await`) already land in
`phase-2`/`phase-1.5`/`phase-4`; `benchmark_plan.md` and `ci_plan.md` are phased
and mostly done. Two gaps were the exception:

1. **`clog2` const-builtin** — flagged "fix later / worth considering" in
   `phase-4-ecosystem.md` (stdlib) and `phase-1.5-simulator.md`, but absent from
   the `phase-2` "Language features" backlog, which that plan declares the single
   source of truth. Added there next to the existing `count_ones` builtin.
2. **Simulator kernel performance ideas** (event-queue batching, signal-change
   coalescing, compiled closure-per-process path) — listed in
   `simulator_ideas.md` section 6 but never in `phase-1.5`'s open-items list.
   Added as trigger-based (baseline already met; build only if a real design is
   too slow).

Both are additive and edition-safe (R13): `clog2` is a compile-time const-builtin
like `min`/`max`/`abs` and needs no new keyword; the perf items are internal kernel
work. No spec, grammar, keyword, or phase-scope change → no Decision block, no
ROADMAP/README status edit (these are granular items under already-summarized
phases). Docs-only; no code, so no `cargo`/`graphify` gate.

- **Next:** when `clog2` is implemented, revert the `Fifo` `AW` workaround noted in
  `phase-4-ecosystem.md` and give it a spec/02 builtin entry + test.

### Files changed

- `docs/plan/phase-2-ir-synthesis.md`
- `docs/plan/phase-1.5-simulator.md`
- `docs/log/2026-06-26.md`

## Spec-vs-code health audit + drift fixes

Ran three parallel parity audits across the spec's hard contracts (R9/R10/R11)
and spec/02 grammar vs the compiler.

**Verdict: the spec needs no content update or expansion — it is accurate and
synced with (or ahead of) the code, zero spec↔code drift.** Every spec'd feature
is implemented and tested: the embedded `std.*` import (E1202), monotonic
comparison chaining (E1109), slice/concat/replication, the eight user-facing
builtins (`min`/`max`/`abs`/`nand`/`nor`/`xnor` plus the reduction ops), `mem`,
`on fall`, async reset, and don't-care patterns. `clog2`/`count_ones` are
correctly NOT user-facing (internal-only), matching the planned-future status of
the `clog2` backlog item added above. R10 diagnostics are a locked triple — 36
checker codes across `ALL_CHECKER_CODES`, `docs/code/11-checker.md`, and 81
fixtures, no gaps.

Three drifts found — all in **docs / plan / test**, none in the spec or core
compiler — and fixed:

1. **R2 (plan ≠ reality):** `phase-2-ir-synthesis.md` still listed five RTL-parity
   features as unchecked `[ ]` to-do, but all shipped 2026-06-17 — replication
   (v0.2.8), don't-care patterns (v0.2.9), `on fall` (v0.2.10), `mem` (v0.2.11),
   async reset (v0.2.12), each with an example. Marked `[x]` with spec version +
   example. (`function`, packages, `suzhal`, `foreach` stay open — genuinely
   not done; `suzhal` is still reserved, user functions still E1110.)
2. **R1 (stale version ref):** `docs/README.md` listed the simulator spec as
   "v0.1 DRAFT"; it is v0.2 (Phase 1.5 complete). Corrected.
3. **R11 (under-guarded contract):** the lexer test
   `future_keywords_are_reserved_not_usable` hardcoded 14 of 19 reserved words,
   leaving `sync`/`inout`/`struct`/`suzhal`/`சுழல்` un-regression-guarded
   (runtime enforcement was always correct — only the test's copy drifted).
   Rewrote it to iterate `TABLE.reserved` (single source of truth =
   `lang/keywords.toml`), so it can never fall behind the table again.

### Files changed (audit)

- `src/lexer/keywords.rs` (test rewritten to iterate the reserved list)
- `docs/README.md` (simulator spec version)
- `docs/plan/phase-2-ir-synthesis.md` (five shipped items marked done)
- `docs/log/2026-06-26.md`

## External-review triage (6 reviewer suggestions)

Triaged six reviewer-proposed improvements against the current compiler.
**Four already ship or are already planned** — no action, recorded so they are
not re-opened:

- **Operator precedence** — already defined (spec/02 section 3, Rust-style table;
  `a + b * c` = `a + (b * c)`). The reviewer read only the EBNF line and missed
  its "precedence table, section 3" cross-reference.
- **Enum match exhaustiveness** — already enforced (E0601 + E0602, fixtures).
- **User-defined functions** — already planned (phase-2 "Combinational
  `function`"; `fn`/`function` reserved; E1110 today).
- **Type inference `wire x = a + b`** — already planned (phase-2 wire inference);
  attempted 2026-06-14, deferred on the missing symbolic-width algebra.
- **`let` for locals** — named intermediates already exist via `wire`; `let` is
  reserved for instances by design. The ergonomics gap is item 5's annotation.

**One genuinely new, freeze-safe item:** port-declaration grouping
(`in { a, b, c: bits[8] }`). Mechanically trivial sugar but a second surface for
a declaration that already has one → a **G1 ("one obvious way") decision**, the
same basis ternary `?:` and Rust range-slice were rejected on. Recorded in
`language_plan.md` new **section 11** (full triage table) and added to the phase-2
"section 8 additive ideas" list as **decision-pending** (R13 — alert before code
on a philosophy conflict; not committed work). The expression-level `let … in …`
sliver of the `let` suggestion rides the same G1 gate.

Docs-only; no code, no spec change (the spec already covers items 1/4). No
Decision block written — adopting item 6 is the maintainer's G1 ruling to make.

### Files changed (review triage)

- `docs/Ideas/language_plan.md` (new section 11)
- `docs/plan/phase-2-ir-synthesis.md` (port-grouping decision-pending entry)
- `docs/log/2026-06-26.md`

## Done

- **`mimz repl`** — interactive REPL for combinational modules. Parses a `.mimz` file once at startup, then reads input bindings line-by-line from stdin and evaluates via `sim::comb::eval_outputs`. Supports `:quit`/`:q`, `:help`, and `:param P=8` for overriding parameters during the session. No new dependencies — uses `std::io::BufRead` for the thin readline loop. Registered as `Cmd::Repl` in the clap CLI with alias `repl`. `src/commands/repl.rs`.

- **Docs sync**: Updated `docs/code/README.md` to list `repl` among the nine tooling modules and bumped the sync stamp.

## Next

- End of session.
