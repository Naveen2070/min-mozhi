# 2026-07-29

## Oversized file split — sub-project C (`checker/names.rs` + `pretty.rs`) closed

Task 13 of
`docs/superpowers/plans/2026-07-28-oversized-file-split-c-names-pretty.local.md`
— final full-workspace verification and docs sync, closing out sub-project
C (Tasks 1–12 had already landed in prior sessions on this branch,
`oversized-test-file-split`).

**What moved:**

- `crates/mimz-core/src/checker/names.rs` (1,836 lines) →
  `checker/names/`: `mod.rs` (309, orchestrator: `resolve_names` +
  `check_module` + `collect_decls` + `declare` + `unknown`) +
  `resolve.rs` (121) + `insts.rs` (265) + `exprs.rs` (439) + `funcs.rs`
  (244) + `items.rs` (438) + `tests.rs` (56, 3 tests).
- `crates/mimz-core/src/pretty.rs` (1,324 lines) → `pretty/`: `mod.rs`
  (112, entry points + `Pretty` struct + `pad`/`kw`/`line`/`blank`) +
  `items.rs` (507) + `seq.rs` (249) + `exprs.rs` (269) + `tests.rs` (193,
  8 tests).
- Every resulting file is well under the 1,000-line threshold.

**Verification:**

- `cargo fmt --check` (workspace-wide): clean.
- `cargo clippy --workspace --all-targets -- -D warnings`: clean (one
  unrelated build-script notice about git hooks, not a lint).
- `cargo test --workspace --all-targets` with `REQUIRE_IVERILOG=1`: 1034
  passed, 0 failed, identical per-suite breakdown to a `git worktree`
  checkout of `0017074` (the commit immediately before this plan's Task 1)
  — confirms C1+C2 changed zero test behavior end to end, not just
  task-to-task.
- `wc -l` on every new file: all comfortably under 1,000 lines (largest is
  `pretty/items.rs` at 507).

### Decision: sub-project D (`mimz-sim`) stays unscheduled

- **Context:** R1 (oversized source files, `docs/audit/review-2026-07-17.md`)
  originally flagged three clusters — emitter (sub-project B, done
  2026-07-28), `checker/names.rs`+`pretty.rs` (sub-project C, done today),
  and `mimz-sim`'s `elaborate.rs`/`value.rs`/`harness.rs` (sub-project D).
- **Decision:** leave D unscheduled for now; this session only closes C.
- **Why:** D's own original design spec calls for a control-flow
  decomposition pass first (unlike B/C, which were mechanical `impl`-block
  extractions) — a different shape of work that needs its own scoping
  session, not a continuation of this plan.
- **Impact:** `docs/plan/phase-2-correctness-consolidation.local.md`'s R1
  note updated to reflect C's completion; "next up" now points at D as the
  only remaining R1 item. `checker/widths/mod.rs` (1,995 lines, never
  assigned to any cluster) and `sim/elaborate.rs`'s current line count both
  still flagged there for whoever scopes the next split.

**Docs synced:** `docs/plan/phase-2-correctness-consolidation.local.md`'s
R1 note and "next up" line.
