# 2026-07-28

## Docs sync: `phase-2-correctness-consolidation.local.md` + audit folder

A user-requested audit walkthrough (plan doc vs. `docs/audit/`) surfaced
three stale documents, none reflecting a code change - corrected all
three in place, no source touched.

### Decision: reconcile the stale Stage 5 label and cross-referenced audit notes

- **Context:** `docs/plan/phase-2-correctness-consolidation.local.md`'s
  Stage 5 ("Feature debt") was still labeled "not started," but L1/BUG-13
  (128-bit ceiling) and L2 (`sync` CDC construct) had both already landed
  - L1 across 2026-07-22/07-24/07-25 commits, L2 via a separate,
    never-looped-back-in effort (`sync_prim_lower.rs`). Separately,
    `docs/audit/security.md`'s SEC-9 entry claimed "SEC-8 itself stays
    OPEN," written before SEC-8's own A2 fix landed later the same day
    (2026-07-18) - factually wrong as written. `docs/audit/README.md`'s
    audit log table also stopped at the 2026-07-18 row, missing every
    event from 2026-07-19 through 2026-07-26 (BUG-17/19–25, Stage 4
    A1a/A1b, BUG-13 layers 1–2, the oversized-test-file split).
- **Decision:** updated all three in place. Stage 5 now documents both
  items as DONE with their actual landing dates; a new "Current status
  (synced 2026-07-28)" section closes out the plan doc - genuinely open
  work is BUG-15 (sim bundle-field-expansion gap) + the tamil-pure T1
  differential-coverage gap, everything else is either fixed or
  deliberately deferred as a feature (BUG-10 return case, BUG-12). SEC-9's
  cross-reference corrected to state SEC-8 is fixed, narrowing the
  still-true residual point (checker-gating closes reachability, not the
  divergence class itself - that's A1/Stage 4, also done). README's audit
  log table gained 6 new rows bridging 2026-07-19 through 07-26.
- **Why:** doc-sync rule (`.claude/Rules.md` section 2) - no doc should stay
  stale once a disagreement is found; resolve same day.
- **Impact:** `docs/plan/phase-2-correctness-consolidation.local.md`,
  `docs/audit/security.md`, `docs/audit/README.md`. No code changed. R1
  (oversized files: `sim/elaborate.rs` now 2,926 lines, `emit_verilog/
module.rs` now 2,229, both grew since the 2026-07-17 review) and R2
  (stringly sim errors) remain the real outstanding code-health debt -
  explicitly called out, not yet scheduled.

**Next:** R1 (schedule the emitter / `checker/names.rs`+`pretty.rs` /
`mimz-sim` source-file splits), R2 (sim runtime errors → `Diag`), BUG-15,
tamil-pure T1 gap - in that priority order per the plan doc's new
"Current status" section.

## R1 sub-project B: `emit_verilog` oversized-file split, executed

Continuation of the `oversized-test-file-split` branch (inline execution,
same convention as sub-project A) - picked up
`docs/superpowers/plans/2026-07-28-oversized-file-split-b-emit-verilog.local.md`
from Task 18 (Tasks 1–17 had landed in an earlier session) through Task 20's
full-workspace verification.

### Decision: finish B1's `module/mod.rs` split (Tasks 18–19), then close out B0+B1 together

- **Context:** B0 (test extraction) and most of B1 (production-code split
  of `module.rs`'s single `impl Emitter` block) were already committed
  (293e17e through f11b23d - `bundle_fields.rs`, `ports.rs`, `seq.rs`,
  `drives.rs` already extracted). Two files remained: `instances.rs`
  (`flatten_items`/`eval_consts_items`/`emit_instances`/`instance`) and
  `funcs.rs` (`render_fn_decl`/`emit_fn_stmts`/`emit_fn_loop_unroll`/
  `substitute_expr`/`collect_assigned`/`fn_all_locals`).
- **Decision:** extracted both per the plan's procedure (grep current
  line numbers, read, write to new file with `use super::*;` +
  `impl Emitter<'_> { ... }`, delete from source, add `mod` declaration).
  Task 18 needed no visibility bumps - all three moved functions are only
  called from `module/mod.rs`'s `module()`, so `pub(super)` sufficed.
  Task 19 hit the one real cross-file wrinkle in the whole plan:
  `substitute_expr` is called from `bundle_fields.rs` (a sibling file, not
  the new parent) and `collect_assigned` from `module/mod.rs`'s `module()`
  - `pub(super)` alone wasn't enough because Rust doesn't re-export a
    child module's items through a sibling's `use super::*` wildcard
    automatically; fixed with explicit `use super::funcs::substitute_expr;`
    / `use funcs::collect_assigned;` imports instead of guessing at a
    broader visibility level.
- **Why:** `.claude/Rules.md`'s zero-behavior-change convention for this
  plan (no test renamed/added/removed, no emitted-Verilog byte changed) -
  verified rather than assumed, per Task 20's own checklist.
- **Verification (Task 20):** `cargo fmt --check` and `cargo clippy
--workspace --all-targets -- -D warnings` both clean (workspace-wide).
  `REQUIRE_IVERILOG=1 cargo test --workspace --all-targets` → 1034/1034,
  confirmed identical against a `git worktree` checkout of the pre-plan
  baseline commit (`e1789bf`) - same 1034/1034, not just "looks
  unchanged." `git diff --stat e1789bf -- tests/golden/` → zero lines,
  confirming no emitted-Verilog byte changed anywhere in the golden suite.
- **Final sizes:** `emit_verilog/mod.rs` 1776→737 lines;
  `emit_verilog/tests/` 10 topic files (~100–185 lines each);
  `module/mod.rs` 2229→363 lines (now just `module()` + `mark_fn_used()`);
  `module/{bundle_fields,ports,seq,drives,instances,funcs}.rs` at 263,
  346, 106, 157, 322, 489 lines respectively; `module/tests.rs` 226 lines.
  Every file in the cluster is now well under the 1000-line threshold.
- **Impact:** `crates/mimz-core/src/emit_verilog/` (mod.rs + new `tests/`
  dir) and `crates/mimz-core/src/emit_verilog/module/` (mod.rs + 6 new
  files + tests.rs). No behavior change anywhere - pure structural split.
  Commits: `d60c556` (instances.rs), `a31981b` (funcs.rs), plus the
  earlier-session commits for B0/B1's first 5 production files (see the
  plan doc's own per-task progress notes for the full commit list).
  R1's sub-project B is now **DONE**; C (`checker/names.rs`+`pretty.rs`)
  and D (`mimz-sim`) remain unscheduled - see the updated
  `phase-2-correctness-consolidation.local.md` "Current status" section.

**Next:** sub-project C (`checker/names.rs`+`pretty.rs`) or D (`mimz-sim`,
needs a control-flow decomposition pass first) - whichever the user wants
scoped next; R2 (sim runtime errors → `Diag`); BUG-15; tamil-pure T1 gap.
