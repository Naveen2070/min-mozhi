# 2026-08-05

## GAP-6 closed — `assert(cond)` / `assert(cond, "msg")`, hard runtime invariants

Branch `gap-6-assert-cover`, v0.2 roadmap priority #7 (`docs/audit/
review-2026-08-02.md`), executed as a 13-task TDD plan
(`docs/superpowers/plans/2026-08-04-gap-6-assert.local.md`), each task its
own failing-test-first checkpoint, `wip:` commits throughout.

**T1 — keyword.** `assert`/`valiyuruthu`/`வலியுறுத்து` added to
`lang/keywords.toml`, `Kw::Assert`, `REQUIRE_KEYS`/`kw_for_key`
(Tanglish/Tamil spellings PROVISIONAL, pending native review, same status
`default`/`bundle`/`return` carried before their own review).

**T2 — AST.** One shared `AssertStmt { cond: Expr, msg: Option<String>,
span: Span }` payload reused by `ModuleItem::Assert` and `SeqStmt::Assert`.
Forced 26 exhaustive-match sites to classify the new variant — filled
compiler-guided, same technique GAP-3's own 60-site fill used: real logic
in the pretty-printer (round-trip), LSP refs/unused-signal
lint/`mimz translate` (all walk `cond`), the clock-domain checker (cond is
an ordinary expr read), and — a genuine correctness catch, not a
pass-through — `foreach`/`sync loop` lowering's substitution/rename passes
(an assert's `cond` can legitimately reference the loop variable, so a
naive pass-through would have been silently wrong); everything else
(driver analysis, name resolution) got the correct no-op (assert declares
and drives nothing). Placeholders left explicit for Tasks 5/7/9/10.
Surfaced 2 pre-existing `grammar_sync.rs` failures — the new keyword
hadn't propagated to the VS Code TextMate grammar or `spec/03`'s keyword
table — fixed as part of this checkpoint's "get to green" gate.

**T3/T4 — parser.** One shared `Parser::assert_stmt()` (`"assert" "("
expr [ "," string ] ")"`) called from both the module-item dispatch and
the seq-block dispatch (both word orders — `assert` is keyword-first in
thamizh order too, like `loop`/`default`/`foreach`, not a clause head).
The plan's own drafted tests had two real bugs, both caught before
committing: an off-by-one item index (two ports precede the assert in the
fixture, not one) and a missing `reset rst` line (E0301) in the on-block
fixture.

**T5 — checker.** `assert`'s condition reuses `check_cond` (E0404) — the
exact rule `if`/`&&`/`test`'s `expect` already enforce. Both arms live in
`checker/widths/stmts.rs` (the plan's draft named `widths/mod.rs` for the
module-item walk; the real file organization differs slightly, same class
of drift as T3's index bug).

**T6/T7 — emission.** Both sites lower to `` `ifndef SYNTHESIS `` /
`` `endif `` guarding an `if (!(cond)) begin $display("ASSERTION FAILED:
…"); $fatal(1); end` — a sibling `always @(*)` loop for the module-item
form, inline in the existing `posedge`-block body for the `on`-block form.
Synthesis-safe by construction: a real synthesis tool strips the whole
guarded region, `assert` is active only in simulation.

**T8/T9/T10 — simulator.** `S0501` opens the `S05xx` diagnostic family
(`ALL_SIM_CODES`, now 79 entries) — confirmed by reading the actual
completeness-check implementation (not assumed) that `sim_errors.rs`'s
`covered` list is hand-maintained, not auto-scanned, so the new code
needed explicit registration. `Design.asserts` collects module-body
asserts through elaboration (the `Elaboration` builder struct gained the
accumulator field, threaded into the final `Design{}` struct literal, same
pattern as `comb`/`procs`); `on`-block asserts need no new elaboration —
they already ride along in `Process.body` like any other `SeqStmt`. The
kernel evaluates every assert from the two places state is known
consistent: `run_seq` (a clocked assert fails its `tick_edge` immediately)
and the new `Sim::check_comb_asserts` (called from `tick_edge`'s tail for
every clocked entry point, and from `comb_run`'s per-vector loop for the
clockless path). A real bug caught writing T10's own test: `comb_run`
returns `Result<Timeline, String>`, and every call site maps `Box<Diag>` →
`e.msg`, silently dropping `.code` — the drafted test's
`err.contains("S0501")` could never have matched; fixed the assertion to
check the message text instead, documented inline why.

**T11 — Icarus differential.** A hand-rolled `iverilog`+`vvp` invocation
(not the shared `run_vvp` helper, which asserts SUCCESS — the opposite of
what this test needs) drives two clock edges against a compiled assert
design, asserting `vvp` exits non-zero and prints "ASSERTION FAILED" —
proving `$fatal` genuinely halts real hardware simulation, not just our
own kernel's belief that it should. First run hit `mimz compile`'s full
checker gate (E0301, reg-without-reset) — the mimz-sim unit tests in T10
bypass the checker entirely, this one doesn't. No emitter bug found;
Tasks 6/7 verified correct against real hardware.

**T12 — examples.** 10 files (comb + clocked × english/tanglish/tamil/
tamil-pure/mixed). English hand-written; tanglish/tamil generated via
`mimz translate --to <flavor>` (correctness by construction), with
comment headers hand-adjusted to each directory's flavor-annotation
convention (`translate` preserves comments verbatim). `mixed` hand-mixed
following `comparator.mixed`'s pattern. `tamil-pure` initially missed a
real convention — caught by reading `kanakki.mimz` before guessing: the
clocked form needs `இலக்கணம் தமிழ்` (enables SOV order, `ஏற்றம்(...)
போது` not `போது ஏற்றம்(...)`) and the `கடிகை`/`மீள்` clock/reset names.
All 10 picked up automatically by `tests/examples.rs`'s directory scan —
no hardcoded list to touch.

**T13 — docs** (this entry): `spec/02-syntax-and-grammar.md` gained
§1.17 + the `assertStmt` production in both `moduleItem`/`seqStmt` (v0.2.28);
`docs/code/11-checker.md`'s E0404 row and `docs/code/13-tooling.md`'s
`S0xxx` catalog updated; a new "Guarding invariants: `assert`" section in
`docs/guide/08-sequential-logic.md`; `docs/audit/gaps.md` GAP-6 → CLOSED
with a `Fix.`/`Test.` pair; this audit-log row.

Full workspace 1156/1156 (`REQUIRE_IVERILOG=1 cargo test --workspace
--no-fail-fast`), `fmt`/`clippy -D warnings` clean throughout every
checkpoint. Two independent bugs found and fixed along the way, both
outside `assert`'s own logic (T2's grammar-sync gap, T10's `comb_run`
error-boundary gap) — no bugs found in `assert` itself; Task 11's
real-hardware proof came back clean on the first fix. Closes v0.2 roadmap
priority #7. The enum↔bits cast (GAP-7) is now the only open
language-surface gap from the 2026-08-02 review.
