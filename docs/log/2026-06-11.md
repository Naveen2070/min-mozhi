# Dev Log — 2026-06-11

> `include` alias + examples reorganized into four flavor folders, all
> compile-tested.

## What was done

- **`include` is now an English alias of `import`** (spec v0.2.1):
  - `keywords.toml` gained per-column alias lists (`en_aliases` etc.);
    `src/lexer/keywords.rs` inserts aliases into the same spelling map, so
    an alias is indistinguishable from its canonical word after the lexer.
    The startup disjointness panic covers aliases too.
  - Parser unchanged (same `Kw::Import` token); EBNF doc comment + spec/02
    grammar updated to `( "import" | "include" )`.
  - spec/03: "Aliases" section added; the one-canonical-spelling rule
    clarified to ban spelling _variants_, not deliberate synonyms.
    Tanglish/Tamil columns get no aliases before native-speaker review.
- **examples/ reorganized into four flavor folders** — `english/`,
  `tanglish/`, `tamil/`, `mixed/` — each holding the SAME 11 examples with
  identical identifiers (only keywords differ): adder, alu, blinker,
  chained, comparator, counter, edge_detector, lib/full_adder, mux4,
  shift_register, traffic_light. Six examples are new (shift register,
  mux4, comparator, blinker, edge detector, chained ripple-carry adder);
  `chained` exercises `include lib.full_adder` — the alias AND dotted-path
  imports end-to-end. `mixed/` files mix all three flavors in one file
  (the migration-path showcase). Tanglish/Tamil/mixed keyword spellings
  taken verbatim from `keywords.toml`.
- **Integration tests rewritten** (`tests/examples.rs`): recursive
  example discovery; `every_example_compiles` compiles every `.mimz` in
  the folder (44 files); `all_four_flavors_compile_to_identical_verilog`
  asserts each base example emits byte-identical Verilog from all four
  flavors (replaces the EN-vs-Tanglish counter test — the thesis test,
  now 4-way and over every example); `include_alias_compiles_with_dotted_path`
  locks in the alias. Verified the gate gates: a deliberately broken tamil
  example fails CI naming the file.
- Docs synced: README (paths, status, 33 tests), docs/code/01/02/09/10,
  spec/02 + spec/03 changelogs (v0.2.1), `src/diag.rs` doc example path.

**Tests: 33 green** (22 unit + 7 integration + 4 docs-sync); fmt, clippy
`-D warnings`, markdownlint, prettier all clean.

## Decisions

### Decision: `include` as a synonym alias, English column only

- **Context:** user wants both `import` and `include` to work. The
  keyword table previously enforced "one canonical spelling, no aliases".
- **Decision:** alias mechanism as DATA (per-column `*_aliases` lists in
  `keywords.toml`); `include` → `KW_IMPORT`, English column only.
  Canonical word stays `import`; future `mimz translate`/`fmt` normalize
  aliases to canonical.
- **Why:** both verbs are common enough that either should "just work"
  (beginner-first, G1). English-only because inventing Tanglish/Tamil
  synonyms before the native-speaker review would pre-empt it; flavors
  mix freely, so `include` is usable in any file anyway. The
  no-aliases rule was aimed at _spelling variants_ (dialect risk) —
  deliberate synonyms are a different thing; spec/03 now says so.
- **Breaking change:** `include` stops being a legal identifier (pre-1.0,
  acceptable; did-you-mean economy unaffected).
- **Impact:** spec/02 + spec/03 → v0.2.1; keywords.toml schema; lexer
  table loader; docs/code/02.

### Decision: every example exists in all four flavors, identically named

- **Context:** examples were flat and almost entirely English; the
  trilingual thesis was only spot-checked (one Tanglish counter).
- **Decision:** four flavor folders with the same base examples,
  identical filenames and identifiers; `mixed/` deliberately mixes
  flavors per file. Tests enforce the structure: everything compiles,
  and per-example Verilog is byte-identical across the four folders.
- **Why:** turns "one AST, three skins" from a claim into a CI-asserted
  property over the whole example corpus; gives Tamil/Tanglish learners
  first-class examples instead of one token sample.
- **Impact:** examples/ layout; tests/examples.rs `BASE_EXAMPLES` const
  is the ledger; docs/code/10 house rule: new examples land in all four
  folders + the const, same commit.
