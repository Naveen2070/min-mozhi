# Dev Log — 2026-06-23

## What was done

**Spec formatting sweep** — broke down big prose paragraphs into bullet points across three spec docs for readability:

### spec/01-goals-and-philosophy.md

- "What Min-Mozhi Is" (section 3): restructured from prose paragraph into bullet points + key-facts list

### spec/04-grammar-engine.md

- Header block (status/version section): restructured monolithic prose into structured status history, goal, and implementation checklist
- "Parsing Feasibility" (section 4): broke strategy paragraph into token-lookahead table + clarified steps
- "Grammar-Aware Error Messages" (section 5 intro): restructured into approach bullets with example

### spec/06-editions.md

- "EDITION_HISTORY" (section 4): broke prose paragraph into lineage bullets + backward-compatibility note

### CI — push benchmark history to develop branch

- Changed `nightly-bench` job in `.github/workflows/ci.yml` to push `bench-history.jsonl` to `develop` branch (unprotected) instead of the current branch — bypasses branch protection without needing a PAT.

### Search UI — sidebar → popover

- Rewrote `Search.astro` from a static Pagefind sidebar widget into a popover overlay dialog:
  - Trigger button (magnifying glass icon + "Search" label + `Ctrl+K` badge) in the nav bar
  - On click, opens a centered overlay with backdrop blur, containing the Pagefind search input + results
  - Close on: backdrop click, Escape key, or when navigating to a result
  - `Ctrl+K` / `Cmd+K` keyboard shortcut to toggle open/close
  - Lazy Pagefind initialization (only on first open)
  - Pagefind UI restyled to match the site's dark/light theme tokens
- Moved `<Search />` from `Docs.astro` sidebar to `Nav.astro` (visible on every page, not just docs)
- Removed search import and usage from `Docs.astro`

### Live Waveforms in Standard Library Docs

- Segregated the Standard Library modules into their own dedicated sidebar section under `site/src/components/Sidebar.astro`.
- Implemented `LiveWaveformMount.tsx`, a wrapper around `mimz_wasm` and `WaveformViewer`, to allow interactive, in-browser simulations inside Markdown documents using standard `<div>` elements with data attributes.
- Replaced the static screenshot placeholders in `fifo.md`, `pwm.md`, `seg7.md`, `debouncer.md`, and `uart_tx.md` with `<div class="live-waveform" ...>`.
- Updated `site/src/layouts/Docs.astro` to statically load the English stdlib examples and mount `LiveWaveformMount` client-side, dynamically rendering an interactive waveform for each module.
- Adjusted `site/src/components/Sidebar.astro` to treat `docs/guide/stdlib/README.md` as the "Overview" page for the Standard Library sidebar section instead of listing it as a normal item.

## What was decided

No design decisions — purely a readability/formatting pass and UI improvements. All normative content preserved unchanged.

## Standard-library modules — focused set completed (`stdlib-modules` branch)

Following the debouncer template, shipped the remaining four stdlib modules as
trilingual `examples/.../std/*` **content** (no toolchain/language change):

| Module   | English file  | Pure-Tamil twin         | Built from                              |
| -------- | ------------- | ----------------------- | --------------------------------------- |
| `Seg7`   | `std/seg7`    | `எண்காட்டி` (ennkaatti) | combinational `match` glyph table       |
| `Pwm`    | `std/pwm`     | `மினுக்கி` (minukki)    | free-running counter + compare          |
| `Fifo`   | `std/fifo`    | `வரிசை` (varisai)       | `mem` ring + head/tail/count            |
| `UartTx` | `std/uart_tx` | `அனுப்பி` (anuppi)      | enum Moore FSM + baud divider + shifter |

Each: 4 keyword flavors (English authored, tanglish/tamil via `mimz translate
--to`, mixed hand-blended — all byte-identical Verilog), a pure-Tamil twin with
`இலக்கணம் தமிழ்` SOV order, inline `test` blocks (pass under `mimz test`), module
and emitted-testbench goldens, two hand-written self-checking Icarus testbenches
(English + romanized-port pure-Tamil, both `PASS` under `vvp`), and a doc page
with a reproducible ASCII `--trace` waveform. Gallery at
`docs/guide/stdlib/README.md`, linked from the guide index.

Harness: `BASE_EXAMPLES` 24 → 28, `PURE_TAMIL` 8 → 12 (`tests/examples.rs`);
`TESTBENCHES` 17 → 21, `PURE_TESTBENCHES` 7 → 11 (`tests/icarus.rs`). No new test
functions — the new modules ride the existing parametrized loops. Full gate
green: `cargo test` 432 passed (with `REQUIRE_IVERILOG=1`), fmt/clippy clean,
prettier + markdownlint clean.

### What was decided

- **FIFO uses an explicit `DEPTH` parameter, not `1 << AW`.** While building the
  FIFO I found the sim kernel (`src/sim/value.rs`) evaluates a parameterized
  `1 << AW` as 0, so `count != (1 << AW)` was always false — `mimz test` passed
  the empty/full assertions only trivially (`full = count == 0`). The emitter
  keeps `<<` symbolic so the _Verilog_ is correct, which is why Icarus would
  agree but our kernel would not. Rather than touch the toolchain (out of scope
  for this branch), the module takes `WIDTH`, `AW`, and `DEPTH` with a documented
  `DEPTH = 2^AW` contract — idiomatic since the language has no `clog2`. **This
  is a real sim-kernel const-eval bug worth a follow-up fix** (parameterized
  shift in a runtime expression).
- **Pure-Tamil names are semantic (a Tamil reader grasps the function from the
  name)**, following the established pattern (`kanakki`, `saalaivilakku`): queue
  → `வரிசை`, transmitter → `அனுப்பி`, dimmer → `மினுக்கி`, digit-display →
  `எண்காட்டி`. Pure-Tamil test _names_ must have distinct character lengths — the
  testbench emitter sanitizes a Tamil name to all-underscores, so equal-length
  names collide on the Verilog module name (hit on `varisai`/`anuppi`; reworded).

## What's next

- Run `cargo fmt`, `cargo clippy`, Prettier, markdownlint, and check for silcrow per R6 before closing
- `graphify update .` for hygiene (AST-only; the modules are `.mimz`, not Rust)
- Deferred stdlib follow-ups: UART RX, SPI master, and the importable `std.*`
  search path (`project.rs` + `mimz.toml`); per-module PNG waveform screenshots
  (maintainer capture); and the sim-kernel parameterized-`<<` const-eval fix.

### Simulator BUG-6 (Left-Shift Truncation) Fixed + Cleanup

- **Fix:** In `src/sim/value.rs`, BinOp::Shl now computes the result width as min(128, l.width + shift) instead of truncating to l.width.
- **Tests:** Added `shl_does_not_truncate_to_left_operand_width` unit test in sim/value.rs, and a new `examples/english/shift.mimz` that is part of the `tests/icarus.rs` differential suite.
- **Audit:** Marked BUG-6 as FIXED in docs/audit/bugs.md.

### Shift example → template-conforming

The shift example (BUG-6 regression) was rewritten to follow the BASE_EXAMPLES
template:

- Header comment with purpose, inline `test` blocks (combinational, like tested_adder)
- `examples/mixed/shift.mimz` hand-authored (keyword blend)
- New pure-Tamil twin `examples/tamil-pure/nakartthi.mimz` (Tamil keywords AND identifiers `நகர்த்தி`/`அளவு`/`தரவு`/`மாறிலிநகர்வு`/`அளவுநகர்வு`/`மாறிநகர்வு`) — old `shift.mimz` (English identifiers) deleted
- Registered: `BASE_EXAMPLES` 28→29, `PURE_TAMIL` 12→13; `nakartthi` added to `tests/icarus.rs` differential

### FIFO reverted to `1 << AW`

The workaround for BUG-6 was removed:

- All 4 keyword flavors of `examples/.../std/fifo.mimz` drop the `DEPTH` parameter
- `mem data: bits[WIDTH][1 << AW]` and `full = count == (1 << AW)` restored
- `examples/tamil-pure/varisai.mimz` updated: `ஆழம்` param dropped, `1 << சுட்டு` used
- Test headers simplified: `Fifo(WIDTH: 8, AW: 2)` / `வரிசை(அகலம்: 8, சுட்டு: 2)`
- Hand Icarus TBs (`std_fifo_tb.v`, `varisai_tb.v`) unchanged — default params still work
- Doc page `docs/guide/stdlib/fifo.md` updated (DEPTH row removed, `1 << AW` description restored)

### Docs synced

- `docs/audit/bugs.md` BUG-6 updated: workaround paragraph → workaround removed
- `docs/plan/phase-1.5-simulator.md`: OPEN known-issue → RESOLVED
- `docs/plan/phase-4-ecosystem.md`: OPEN bug note → FIXED; FIFO workaround note → reverted
- `docs/code/10-test-map.md`: changelog row for BUG-6 fix + shift + FIFO revert; corpus prose 28→29
- `docs/plan/stdlib-modules.local.md`: FIFO note updated
- `docs/guide/stdlib/fifo.md`: DEPTH param row removed, `1 << AW` ring description restored
