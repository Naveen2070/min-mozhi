# 2026-07-18

## Final whole-branch review + audit doc reconciliation

- Ran the final whole-branch code review for `phase-2-valid-bundle-sugar`
  per `superpowers:subagent-driven-development` (range `25e82ef..ac02826`,
  the plan's own 5 commits isolated from 3 unrelated concurrent-session
  docs/audit commits interspersed on the same branch). 3 Important findings
  (no Critical): `coalesce_ty` under-validated the `??` LHS/RHS shape
  (missing-`valid`/extra-field bundles type-checked but emitted dangling
  Verilog signals), a `todo!()` panic on an unreachable-in-practice sim
  path, and `builtin_valid_bundles()` leaking per-call instead of once
  (harmless for the CLI, a real leak for the LSP/WASM playground). All
  fixed in one batch (`5bf0bce`), 878/878 tests, clippy clean.
- Re-verified all 11 substantive claims in `review-2026-07-17.md` against
  current HEAD — every one still held (expected: nothing since the review
  touched this territory). Found and fixed a real doc defect: two different
  `security.md` entries were both numbered `SEC-5` (copy-paste-without-
  renumbering slip); renumbered the `[lib] std` one to `SEC-7` and corrected
  its file location (`src/commands/helpers.rs`, not `src/config.rs`).
  `BUG-12`'s 2026-07-17 severity/framing correction (HIGH → MEDIUM,
  consistent design limitation not a divergence) had never actually landed
  in `bugs.md` — re-filed. Filed the review's "A2" finding (checker skipped
  on `eval`/`sim`/`test`) as new `SEC-8` (previously verified-true but
  unfiled anywhere), cross-referenced BUG-11 as `SEC-9`.

## Correctness-consolidation roadmap opened

- New branch `phase-2-correctness-consolidation`, plan doc
  `docs/plan/phase-2-correctness-consolidation.local.md` (local, gitignored
  — mirrors `docs/superpowers/plans/*.local.md`'s convention) staging the
  review's top-10 priority list into 5 dependency-ordered stages, per the
  review's own explicit recommendation (§13: freeze feature additions,
  land these, then resume). One-line pointer added to `ROADMAP.md`'s
  Phase 2 section so the (untracked) plan doc isn't a dangling reference
  for anyone without it locally.

## BUG-11 fixed — bigger than originally scoped

- Stage 1 started with BUG-11 (simulator vs. synthesis mismatch on `<<`).
  The review's own prescribed fix ("truncate the result to `l.width`
  unconditionally") was tried first and **empirically disproven** against
  `iverilog` before landing: `din << 2` (`din: bits[4]`) assigned to an
  8-bit target computes **28** in real Verilog (context-extends `din`
  before shifting), not **12** (truncate-then-extend, what the review's own
  fix would have produced) — caught immediately by the pre-existing Icarus
  differential test for `english/shift.mimz`'s `var_shift`, which the
  review's fix broke.
- Real rule (ground-truthed, not assumed): Verilog's `<<`/`>>` are
  **context-determined** on their left operand — the operand widens to the
  enclosing assignment/`extend` target BEFORE the shift, not after. Neither
  "grow by the shift amount" (BUG-6's original fix, which is what caused
  BUG-11) nor "always truncate to `l.width`" (this review's fix) match
  Verilog in general; only threading the real context width through does.
- Implemented `eval_ctx`/`binary_ctx` (`crates/mimz-sim/src/sim/value.rs`)
  — an `expected_width: Option<u32>` parameter used only by `Shl`/`Shr`
  (every other operator's width rule is deliberately unchanged, scoped to
  this bug). Wired into every call site with a real known target width:
  combinational driver resolution (`comb.rs`, `kernel.rs`), register/
  default/memory-cell writes, `Builtin::Extend`'s argument (the exact site
  that exposed the `din << 2` case), and `fn`-body `let`s (reusing the
  checker's existing `inferred_width` cell). `if`/`match` propagate the
  same context into every branch. Both the review's own `(a<<2)>>2` repro
  (63, not 255) and the `din<<2` case now match Icarus. Old test
  `shl_does_not_truncate_to_left_operand_width` (which asserted the BUG-6
  overcorrection this bug's own root cause) replaced with 3 tests covering
  self-determined, context-widened, and chained-shift-shares-one-context
  behavior. `docs/audit/bugs.md`'s BUG-11 marked FIXED with full detail;
  `security.md`'s SEC-9 marked FIXED (SEC-8 — the reachability class itself
  — stays OPEN, this closed one instance of it, not the class).
- 880/880 workspace tests, clippy clean, `cargo fmt` clean.
- Committed (reworded locally after landing).

## BUG-10 returns — diagnostic instead of invalid Verilog

- Stage 1 continued with BUG-10's still-open return half. First attempt
  rejected bundle-typed `fn` returns in the checker
  (`check_func_body_widths`) — wrong, and reverted before landing: feature
  2.9's structural interface matching already has full, deliberately-built,
  tested checker-level support for bundle-typed `fn` returns
  (`check_return_ty`'s `BundleShapeMatch` handling, E0910/E0804 etc.).
  Rejecting there broke several existing, legitimate tests
  (`structurally_compatible_fn_return_checks_clean` and others) — would
  have fixed nothing, just destroyed working validation.
- Real gap is narrower: `width_subst`
  (`crates/mimz-core/src/emit_verilog/module.rs`) is reached **exclusively**
  via `render_fn_decl`'s `let ret_w = self.width(&decl.ret)` — every other
  caller (module ports/wires, `fn` params) flattens a bundle to per-field
  signals before ever calling `width()`/`width_subst()`. Its `Type::Bundle`
  arm silently returned `String::new()` (parametric form); its
  `Type::Named` arm's bundle case fell through to a misleading "not a
  declared enum" message (bare form). Both now report a real diagnostic.
  `mimz check` still accepts a bundle-typed `fn` return cleanly (the
  checker's own view is correct and unchanged); `mimz compile` now rejects
  it — the same check-vs-compile split the bare form already had, now
  consistent for both forms.
- Confirmed via `docs/plan/phase-2-ir-synthesis.md` that the real fix
  (call-site inlining, since a Verilog `function` can only return one
  value) is already tracked in the language-features backlog — not
  duplicated.
- 2 new tests plus one repurposed (the old
  `structurally_matched_fn_return_emits_same_as_nominal_match`, renamed to
  `..._is_a_diagnostic_same_as_nominal_match`) — its old dummy-`W`-param
  workaround for sidestepping the hard-error path no longer works now that
  BOTH forms are rejected; repurposed to the still-meaningful invariant it
  was really pinning: nominal and structurally-matched bundle returns get
  the identical diagnostic.
- 882/882 workspace tests, clippy/fmt clean.

## SEC-7 — `[lib] std` override sandbox (Stage 1's third and final item)

- `lib_std_dir` (`src/commands/helpers.rs`) now canonicalizes the workspace
  root (the directory holding `mimz.toml`) and the resolved `std` candidate,
  rejecting with a clean CLI error when the candidate escapes the root —
  closes the path-traversal gap where a malicious `mimz.toml` could point
  `std = "../../../etc"` at arbitrary host directories. Picked plain
  enforcement over adding an `--allow-external-std` escape-hatch flag
  (simpler, safer default; the roadmap's own "or" framing allowed either).
  Signature changed `Option<PathBuf>` → `Result<Option<PathBuf>, ExitCode>`;
  all 4 callers (`check`/`compile`/`sim`/`test`) updated to the same
  error-handling convention `resolve_config`/`resolve_lang` already use.
- 2 new CLI-driven tests (`tests/config.rs`, matching that file's existing
  real-binary-driven convention): one confirming the escape is rejected,
  one confirming an in-workspace override still works unaffected.
- 884/884 workspace tests, clippy/fmt clean.

**Stage 1 complete** — all 3 items done (BUG-11, BUG-10 returns, SEC-7).
Pausing here before Stage 2 as planned.

## Stage 2 — checker-gated execution (A2 + BUG-12)

- **A2**: gated `checker::check` before every unchecked-AST evaluation
  path. Found 4 call sites, not the 3 the review named — CLI
  `src/commands/{eval,sim,test}.rs` plus `crates/mimz-sim/src/runner.rs`
  (the WASM/playground engine shares the same bypass, same root cause).
  Each mirrors `check.rs`'s existing convention: warnings ride along,
  only errors abort.
- `eval.rs` needed a scoped gate, not a blanket one: eval never resolves
  `import`/`include` (single-file by design), so running the full-project
  checker on a file with unresolved cross-file instances rejected an
  unrelated module instead of letting `comb::eval_outputs`'s own
  "sub-module" message fire — broke 2 existing tests
  (`multi_module_file_needs_module_flag`, `instances_are_rejected_clearly`).
  Fixed by gating only when `file.imports.is_empty()`; the gap for
  import-bearing files is named in a `ponytail:` comment with its upgrade
  path (per-module check ignoring unrelated modules' import errors).
- Verified the actual fix, not just the tests: `mimz eval` on an
  `a + b` mixing `bits[8]`/`signed[8]` now reports the checker's E0403 and
  exits non-zero, instead of silently computing a wrong value — the exact
  class of bug A2 was filed to close.
- 884/884 tests, fmt+clippy clean.
- **BUG-12**: re-file itself was already done pre-session (MEDIUM,
  `bugs.md`). The remaining piece — the spec ruling (bless file-scoping vs.
  design module-scope `fn` capture) — is a language-design decision, asked
  the user rather than defaulting it. **Decision: defer.** Left open,
  reframed explicitly as a feature idea (not a bug to close) in
  `docs/Ideas/language_plan.md` §12; revisit after stages 3-4 land.

**Stage 2 complete.** Pausing before Stage 3 (differential testing
infrastructure) as instructed.

## Stage 3 (T1, in progress) — Icarus differential coverage + BUG-16

- T1's CI half was already done: `ci.yml`'s `check` job runs
  `cargo test --workspace --all-targets` with `REQUIRE_IVERILOG=1`, which
  already hard-gates layer 3 (`our_simulator_matches_icarus_bit_for_bit`),
  not just layer 1. No work needed there.
- T1's "curated list" half: found 10 `english` examples with zero semantic
  differential coverage (only layer-1 `iverilog -t null` validity) by
  diffing the full example corpus against `tests/icarus.rs`'s testbench
  tables + `differential()` calls. Added layer-3 coverage for 9 of them.
- Doing so found a real, previously-undiscovered bug: `enum_construct.mimz`
  (file-scoped `enum Packet { ... }`, spec/02 §1.5b) crashed `mimz sim`
  with "unknown enum type" despite `mimz check` passing clean. Root cause:
  `elaborate_module`'s `enums` map only ever scanned `ModuleItem::Enum`
  (enum nested inside the module body, like `traffic_light.mimz`'s
  `enum State`) — file-scoped `TopItem::Enum` declarations were never
  registered, unlike `func_reg`/`bundle_reg` (already project-wide).
  Filed + fixed as **BUG-16** (`docs/audit/bugs.md`): added
  `EnumRegistry`/`build_enum_registry` mirroring `build_func_registry`
  exactly, threaded through `elaborate_module`/`flatten_instance`.
- Also found the differential test harness itself (`tests/icarus.rs`'s
  `differential_m`) never ran `checker::check` before `elaborate_project`
  — needed for a tagged enum's `inferred_total_width` Cell to be
  populated. Added the same gate real `mimz sim`/`test` now have (A2).
- `foreach_fill.mimz` excluded from layer 3: hits a real, pre-existing
  simulator gap (no slice-driven signal support, `lamps[i*8+7:i*8] = ...`)
  — out of this pass's scope, left as layer-1-only.
- Retroactively closed **SEC-8** (`docs/audit/security.md`) — this was
  A2's own security filing, fixed in stage 2 but never marked closed;
  caught while cross-referencing for this entry.
- 884/884 tests (`REQUIRE_IVERILOG=1`), fmt+clippy clean.
- **Remaining T1 gap** (not closed this pass): `tamil-pure/` has 7
  examples with zero differential coverage — lower priority since
  `tanglish`/`tamil`/`mixed` are byte-identical Verilog to `english`
  (closing the `english` gap transitively covers those 3), but
  `tamil-pure` uses distinct romanized identifiers and needs its own pass.
- **T2 not started** — random-program differential fuzzing is a
  substantial new subsystem, needs its own scoping conversation before
  implementation.

Pausing here per instruction (stop after each task).
