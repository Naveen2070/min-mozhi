# 2026-09-15

## Fixed: `??` against a bare bundle-typed `fn` param (GAP-1 sub-gap, filed 2026-09-08)

- Root-caused: `flatten_bundle_refs_expr` (`elaborate/bundle.rs`) only
  rewrote `param.field` reads, never a bare `Ident(param)` inside a
  `Binary{Coalesce}` node — so `h ?? 0` for a bundle-typed param `h`
  survived elaboration unflattened, then panicked in `ir::lower::resolve()`
  ("no driver recorded for signal `h`") one step before Task 8's
  `unreachable!()` arm could ever fire.
- Fix: gave `flatten_bundle_refs_expr` its own `Binary{Coalesce}` case,
  mirroring `Rw::expr`'s module-level unwrap-form desugaring
  (`.valid`/`.data` field access + `IfExpr`), recursed back through itself
  so the resulting `Field` nodes flatten to `h_valid`/`h_data` via the
  function's existing check. Corrected `ir::lower`'s now-stale
  `BinOp::Coalesce => unreachable!()` comment to match.
- Scoped to the unwrap form only (`h ?? 0`) — the OR-mux form for a
  bundle-typed `fn` TAIL has no equivalent interception point in this
  codebase and stays a separate, still-open gap (documented in
  `docs/audit/gaps.md`).
- New test: `bare_bundle_typed_fn_param_coalesce_unwrap_is_eliminated`
  (`crates/mimz-core/src/ir/tests/lower_binops.rs`), real lex→parse→check→
  elaborate→lower pipeline.
- 1440/1440 workspace tests passing, clippy clean, fmt clean. `docs/code/
10-test-map.md` + `README.md` badge bumped to match.
- Plan: `docs/superpowers/plans/2026-09-15-bare-bundle-param-coalesce.local.md`
  (deleted, folded into this entry + `docs/audit/gaps.md`).
- Next: the two 2026-09-10 Decision-blocked items (signed `Bits` flag;
  const-var-substitution loop unrolling) are still the real work left
  before phase-2 IR closes out for the optimizer.
