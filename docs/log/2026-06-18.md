# 2026-06-18

## Plan: pull the Phase 4 web playground forward to lead the v0.1.0 public launch

Founder asked whether to build a landing page + docs — and, if possible, a
playground to run mimz and see the waveform — before going public. Verified
feasibility against the code and locked a plan.

### Decisions (founder)

- **Build one static site — landing + docs + in-browser playground — _before_ the
  public flip** (launch with the differentiator, not a README alone). Founder-gated
  (R12); the public flip + `v0.1.0` tag remain Workstream D.
- **Custom Astro app** (not Starlight): content-collections render the existing
  `docs/guide/*` + `docs/spec/*` markdown (sourced, never duplicated) and islands
  host the playground.
- **Monorepo, not a separate web repo** — docs stay a single source of truth, the
  WASM playground can't version-drift from the compiler (built against the same
  `src/lib.rs`, byte-equal differential test in one suite), versioning is atomic. The
  Node toolchain is isolated under `site/`; `crates/mimz-wasm` stays **out of
  `default-members`** so the R8 gate never builds it for the host.
- **Waveform: a custom canvas/SVG renderer behind a swappable `WaveformViewer({vcd})`
  boundary**, with **Surfer** (surfer.dev) as the documented drop-in upgrade if VCD
  later gains x/z or large traces. The VCD string is the stable contract.
- **Deploy: Vercel on a founder-owned subdomain** (not GitHub Pages) — served at root
  so no `base`-path WASM/asset pain, first-class Astro, per-branch preview deploys for
  founder review. Build wrinkle: Vercel's image lacks Rust+`wasm-pack`; leaning toward
  building wasm+site in the SHA-pinned GitHub Actions and `vercel deploy --prebuilt`
  (reproducible, pinned toolchain), with Vercel-native rust-bootstrap as the simpler
  fallback. Decided at Step 6. **Note:** any deploy (even a preview) makes the _site_
  public while the _repo_ stays private — the "go public" gate still governs the repo
  flip + `v0.1.0` tag (R12).
- **Landing may be a little flashy** — a domain-themed animated hero (gates/waveforms),
  but as a lazy-loaded island with a `prefers-reduced-motion` static fallback and a
  light WebGL/SVG approach over heavy Three.js (audience: college labs). Deferred to
  Step 6 polish so it can't delay the functional site.

### Why it's low-risk

Exploration confirmed the compiler+simulator lib is **WASM-ready today** — pure-sync,
no C deps, no `build.rs`, nothing that breaks `wasm32`. The whole pipeline is already
string-in / string-out and in-memory (`lex → parse → check → transliterate →
Project::from_files → emit`; `elaborate_project → run/comb_run → to_vcd`). The only
net-new library code is a ~30-line `compile_string(source, imports)` wrapper that
skips the `std::fs` file-loading path. The WASM browser playground is already the #1
priority in [`phase-4-ecosystem.md`](../plan/phase-4-ecosystem.md), and its
prerequisites (lib/bin split, `--json` diagnostics) landed in Phase 1.

### Recorded

- New durable plan doc: [`docs/plan/phase-4-web-presence.md`](../plan/phase-4-web-presence.md)
  (build sequence, reused-code map, verification). This pulls the playground forward
  from `phase-4-ecosystem.md` rather than adding new scope.

### Next (not started — paused at founder's request after writing the plan docs)

Step 1 onward: `compile_string` lib wrapper + tests, then `crates/mimz-wasm`, the
Astro site, the playground, the waveform viewer, and a SHA-pinned `pages.yml`. No
code or commits yet (R12).

## Docs sync: guide + code docs brought current with A1–A5 + versioning

Audited `docs/guide/` (12 chapters) and `docs/code/` (13 maintainer docs) against
the sources of truth (`spec/*`, `keywords.toml`, `examples/`, `src/`) — two parallel
read-only audits. The recent feature work (A1 replication, A2 don't-care `match`, A3
`on fall`, A4 `mem`, A5 `async reset`, Workstream B versioning) had landed in the
spec and examples but not yet in the prose docs. Fixed all gaps found (R1: non-source
docs follow spec/code). No code or test changes — counts unchanged; no `graphify
update` needed.

**Guide (`docs/guide/`):**

- **ch4 signals** — new `mem` (register-array) section; reset section now covers
  `async` (was "synchronous" only); `mem` row added to the quick-reference table.
- **ch5 operators** — replication `{N{x}}` added to the concat/index/slice section.
- **ch7 expressions** — new "don't-care patterns" subsection (`0b1??`, casez), with
  the width + exhaustiveness rules; the patterns list updated.
- **ch8 sequential** — removed the stale "only rising-edge exists"; added an
  `on fall(clk)` subsection and an async-vs-sync reset explanation.
- **ch11 toolchain** — new `mimz --version` section showing the real two-axis block.
- **ch12 cheatsheet** — moved `mem`/`async`/`fall` from _reserved_ to the active
  keyword table (with provisional Tanglish/Tamil spellings noted), added `{N{x}}` to
  the operators row, and aligned the E1104 row.

**Code (`docs/code/`):**

- **06-diagnostics** + **11-checker** — E1104 and E0503 documented as dual-use
  (reg-reset / mem-init, and reg / mem multi-writer respectively).
- **05-emit-verilog** — new subsections for async-reset sensitivity-list widening and
  `mem` reg-array lowering with the power-on `initial` loop.
- **13-tooling** — new `version` (`src/version.rs`) section: the two version axes,
  `EDITION_HISTORY`, `version_block()`, the `--version` intercept.

**R6 cleanup:** removed 5 section-sign (`§`) characters in `docs/code` (10-test-map,
13-tooling, README) and the ones I had introduced earlier today in `ci_plan.md` and
`phase-4-web-presence.md`; also fixed that plan doc's `docs/spec` path (the spec lives
at repo-root `spec/`). Then swept the pre-existing `§` too (founder asked): cleaned
`language_plan.md`, `phase-1.8-grammar-engine.md`, and `phase-2-ir-synthesis.md`. The
repo is now `§`-free outside the exempt `docs/log/` + `docs/archive/` history and the
git-excluded `rtl-parity-release-workflow.local.md` working note. Gate:
`prettier --check` clean, `markdownlint-cli2` 0 errors. No commit (R12).

## Pitch reorder + audience broadening (spec/01 v0.3.2)

Reworked the outward pitch across the public-facing docs ahead of the v0.1.0
launch.

### Decision: pitch order, "secure" framing, and audience

- **Context:** Founder wants the pitch to lead with **modern + secure HDL**, then
  **educational**, then **trilingual / first-native-Tamil** — and clarified the
  audience is **all students**, not Tamil Nadu only, with native Tamil there **to
  reach Tamil-speaking learners and to grow Tamil-rooted programming**.
- **Decision:** (1) Reorder the outward pitch to **modern + safe → educational →
  trilingual/first-Tamil** everywhere. (2) Frame **"secure" as a first-class
  compile-time _goal_** (G5, lands post-Phase-1), never as a shipped v0.1.0
  feature — only **"safe by default"** (G2) is claimed as shipped. (3) Broaden
  spec/01 section 2 to **students broadly**, keeping the TN polytechnic student as
  the archetype served most acutely, and naming native Tamil's **double purpose**.
- **Why:** Leading with safety + syntax matches spec/01's existing instinct ("the
  trilingual system is the soul, not the sales pitch") and widens reach; claiming
  shipped "security" would violate the hardware-honesty principle (tie-breaker 1)
  since G5 is unimplemented. Rejected: claiming "secure HDL" outright (overclaim);
  re-ranking the section 1 "why it exists" goals (left as the honest founder order);
  a fully generic persona (loses the deliberate "design for one" device).
- **Impact:** `spec/01` → **v0.3.2** (header, section 1 identity sentence, section 2
  audience, section 3 pitch + honesty note, changelog). `README.md` (header, "Why"
  reorder + security-as-goal note, "Who it's for", `§ 4` → "section 4").
  `min-mozhi-roadmap.md` header. `RELEASE_NOTES.md` (intro + Highlights reordered,
  safety first). `docs/plan/phase-4-web-presence.md` landing tagline. The section 1
  goal ranking, G1–G5, Constitution, and tie-breakers are unchanged. Technical docs
  (guide/code intros, architecture, how-the-compiler-works) untouched — not pitch
  surfaces. No commit (R12).

## Test-count bump + how-the-compiler-works refresh

- **Test count 369 → 404** in `README.md` (the CI badge URL + the "Status" line),
  matching `docs/code/10-test-map.md` ("404 tests as of 2026-06-17"). The other
  stale `369`s are in exempt logs and the git-excluded `.local.md` working note.
- **`docs/how-the-compiler-works.md` brought current + more detailed** (it had
  drifted): reframed the overview from "five stations" to a shared **front end**
  plus **two back ends** (emit Verilog _or_ simulate); added a beginner-level
  **Station 5 — Simulate** section (elaborate → run/kernel → VCD, the shared
  `value.rs` evaluator, the Icarus three-way differential); fixed the stale Verilog
  banner to include the **edition tag** (`(edition wingless-butterfly-2026-1)`);
  added the **Grammar Engine** (natural Tamil word order) note the page never had;
  noted the grown constructs (`on fall`, `async reset`, `mem`, `{N{x}}`) in the
  emitter section; added the Icarus differential to the test layers and bumped the
  count to 404; extended the "going deeper" + "where do I look" tables with the
  simulator and `13-tooling`. `src/diag.rs` is genuinely a single file — left as
  written. Gate green; no commit (R12).

## Website Phase 1 — landing + docs scaffold (`site/`)

Built the first website phase per the approved plan
(`.claude/plans/tingly-plotting-sketch.md`; full target in
[`phase-4-web-presence.md`](../plan/phase-4-web-presence.md)).

- **Stack:** official `npm create astro@latest` scaffold — **Astro 6.4.7**, **npm**,
  **React** (`@astrojs/react`) as the single island framework, **Tailwind v4**
  (`@tailwindcss/vite`), **Shiki** fed the existing `mimz.tmLanguage.json` for
  `.mimz` highlighting, **Pagefind** docs search, **`@astrojs/vercel`** adapter.
- **Decisions applied:** hand-authored stubs replaced with the official scaffold
  (founder ask); **Lit dropped** for **React only** (founder ask — one runtime);
  palette is **blue + lightning-yellow** ("மின்" = electricity, founder ask);
  dark-first + light toggle; English UI with Tamil in the wordmark/examples.
- **Shipped:** landing (`/`) with the reordered pitch, the real
  `examples/english/counter.mimz` snippet (Shiki), three highlights, and an
  animated blue-grid + yellow-clock **hero** React island (reduced-motion aware);
  docs routes `/guide/[slug]` + `/spec/[slug]` rendering the existing
  `docs/guide/*` + `spec/*` markdown **sourced via the Astro content-layer glob
  loader (never copied)**, sidebar + Pagefind search; a 404.
- **Doc links:** a `rehype-doc-links.mjs` plugin rewrites the docs' relative `.md`
  links at render time (→ `/guide`|`/spec` routes, or GitHub blob for unpublished
  files) — verified zero internal `.md` links remain in the build.
- **Astro 6 fixes:** moved the rehype plugin off the deprecated
  `markdown.rehypePlugins` onto `markdown.processor: unified({ rehypePlugins })`
  (`@astrojs/markdown-remark`; `shikiConfig` still applies via the shared config);
  added `syntaxHighlight.excludeLangs: ["ebnf"]` so spec/02 grammar blocks render
  as plain code instead of a Shiki warning. Both warnings gone.
- **Verify:** `npm run build` clean — **20 pages**; `npx astro check` 0/0/0;
  Pagefind indexed 18 docs pages; `.vercel/output` emitted. Repo docs gate kept
  green by excluding `site/**` from markdownlint + Prettier (the site has its own
  toolchain); `.gitignore` covers `dist/`, `.astro/`, `node_modules/`, `.vercel/`.
- **Next:** founder to supply the root domain for `mimz.<domain>` DNS; deploy a
  Vercel preview; then Phase 2 (the WASM playground). No commit (R12).

## spec/01 rewritten as a user-facing document (v0.3.3)

### Decision: make spec/01 a public spec, drop the internal framing

- **Context:** `spec/01-goals-and-philosophy.md` had accreted internal,
  working-doc framing (a founder-ordered "why", the learn-compilers /
  prove-silicon-tooling / personal-mastery motivations, inline `Decision YYYY`
  tags, and an explicit "20-year-old Tamil-Nadu polytechnic student" persona). It
  read like an internal conversation, not the project's public language spec.
- **Decision:** Rewrite it in place as a **user-facing** spec. Drop the
  founder-motivation goals and the explicit Tamil-Nadu-student persona; broaden the
  audience to **learners of digital design generally**, with native Tamil framed as
  **reach** (learners underserved by English-only tools) + **Tamil-rooted
  programming growth**. Keep all **normative** content verbatim in substance: the
  Constitution, G1–G5 (incl. the G2 safety table), non-goals, and the
  design-principle order. Bump to **v0.3.3**.
- **Why:** the file is the public face of the language (the website now sources
  `spec/*` directly); a spec should read as a spec. The founder's personal
  motivations belong in the dev log / README narrative, not the normative spec.
  This supersedes v0.3.2's choice to keep the TN-student archetype (founder asked
  to remove the explicit persona). Rejected: a separate public doc (the founder
  asked to fix this file itself).
- **Impact:** `spec/01-goals-and-philosophy.md` (header, section 1 "Goals",
  section 2 audience, section 8 success reworded; principles/non-goals de-tagged;
  changelog v0.3.3). G1–G5, Constitution, safety table, and principle order are
  unchanged in substance. README / RELEASE_NOTES still reference spec/01 correctly.
  No commit (R12).
- **Follow-up (same ask):** generalized the two other persona mentions —
  `spec/03` word-selection criterion 1 → "a student, or any Tamil-speaking HDL
  developer"; `docs/code/06-diagnostics.md` persona check → a neutral "student new
  to hardware design" while explicitly _including_ the native-Tamil target (a
  Tamil-speaking polytechnic student). `phase-4-ecosystem.md`'s "Tamil Nadu
  outreach" item is left as-is — it's a deliberate outreach target, not audience
  framing.

## Docs tone audit — non-conversational sweep

Audited every doc (three parallel passes: user-facing specs/readmes, the user
guide, and maintainer/planning docs) to confirm they read as documentation, not
as a founder↔AI working chat. **`docs/log/` and `docs/archive/` were excluded —
they are append-only records and stay as-is.** Verdict: overwhelmingly clean
(specs, all 12 guide chapters, every `docs/code/` page, architecture,
how-the-compiler-works, prior-art all clean). Fixed the few minor conversational
leaks found:

- `min-mozhi-roadmap.md`: Phase 1.8 header normalized to the `✅ COMPLETE (date)`
  convention (was a casual `🟢` + "solo dev runs 1.8 before 1.5" aside); the
  "Proposed solo-dev deadlines (… — correct me and these shift)" heading →
  "Proposed deadlines (assume ~8–10 h/week; subject to change)"; dropped the
  reviewer-ish "(tool, not syntax)" gloss.
- `docs/guide/12-cheatsheet.md`: dropped "founder-authorized for the tooling" from
  the provisional-spellings note (a reader only needs "provisional, pending
  native-speaker review").
- `docs/code/07-decisions-and-evolution.md`: "Things that bit us, recorded so they
  don't bite twice" → "Pitfalls hit during implementation, recorded so they don't
  recur".

`docs/Ideas/{language_plan,simulator_ideas}.md` use "Explanation:" / "Idea:"
framing, which the audit deemed appropriate for their declared exploratory-backlog
status — left as-is. Gate green; no commit (R12).

### "founder" / "founder-authorized" sweep (user-facing specs only)

Founder asked to ensure "founder-authorized" wasn't used elsewhere. It appeared 3×
more in `spec/03-keywords-trilingual.md` (the provisional-keyword changelog notes).
Decision (founder): **neutralize in the user-facing specs only**, leave internal
plan/Ideas/CHANGELOG/log docs.

- `spec/03`: dropped the 3 "founder-authorized" clauses (→ "used provisionally so
  the four-flavor tooling works…"); "Review & governance" reworded — "the founder"
  → "the maintainers / native-speaker reviewers".
- `spec/06-editions.md`: the edition codename is now "the maintainer's pick" (was
  "the founder's pick").
- `spec/01:183`: the lone remaining "founder" is in the v0.3.3 changelog line
  describing what was removed — kept (factual history).
- **Left as-is (out of the chosen scope):** `docs/plan/*`, `docs/Ideas/ci_plan.md`,
  and `CHANGELOG.md` ("founder-gated"); and `keywords.toml`'s data-file comments,
  which still say "Founder-authorized 2026-06-17" (it's a source data file, not a
  user-facing doc). Flag for later if a fully founder-free public surface is wanted.

Gate green; no commit (R12).

### "founder" sweep extended repo-wide + conversational-tone audit

Maintainer asked to ensure no "founder" mention survives in any doc, and that no
doc reads like an AI↔user chat. Two parallel sweeps:

- **"founder" sweep (whole repo):** 95 hits found; 81 are in `docs/log/` (these
  daily logs are append-only records — R4 — so they are left intact; rewriting them
  would falsify the historical record). The remaining **14 in current docs were
  neutralized → "maintainer"**: `spec/01:183` (changelog line), `CHANGELOG.md`
  ("maintainer-gated"), `docs/plan/phase-1-verilog-backend.md`,
  `docs/plan/phase-4-web-presence.md` (8×), `docs/Ideas/ci_plan.md`, and the three
  `keywords.toml` data-file comments ("Maintainer-authorized 2026-06-17"). This
  supersedes the earlier "user-facing specs only" scope above. Post-sweep grep:
  zero "founder" outside `docs/log/`.
- **Conversational-tone audit (specs, guides, code docs, README, roadmap, plans,
  Ideas; logs/archive excluded as records):** 5 first-person/chat-voice residues
  fixed — `docs/how-the-compiler-works.md` ("Now let's walk through" → "The sections
  below trace"; "Our counter" / "our counter" → "the counter"; "Five things to
  notice" → "Five details stand out") and `docs/guide/01-getting-started.md` ("You
  now have the loop" → "That completes the core loop"). Tutorial second-person
  ("you can run …") left as legitimate teaching voice. Rest of the corpus clean.

Gate green (markdownlint 0 errors, prettier clean); no commit (R12).

### Site: section README hubs become the `/guide` and `/spec` landing pages

The guide's `README.md` (hub index) was excluded from the site by the glob loader
and so had no home on the site; `spec/` had no README at all. Made each section's
README its landing page:

- **`site/src/content.config.ts`:** added `guideIndex` + `specIndex` collections
  (glob just `README.md`); excluded `README.md` from the `spec` collection (already
  excluded from `guide`) so the hubs do not also appear as sidebar chapters.
- **New pages** `site/src/pages/guide/index.astro` + `spec/index.astro` render the
  hubs via the `Docs` layout at `/guide` and `/spec`.
- **New `spec/README.md`** — a hub index mirroring the guide README's style
  (intro → mental-model callout → "Read in order" table of the six specs → start-here
  note); user-facing, non-conversational, no founder mention.
- **`Nav.astro`:** "Docs" → `/guide`, "Spec" → `/spec` (were deep chapter links).
- **`Sidebar.astro`:** each section gets a leading **Overview** link to its root,
  active on the landing page.
- **`rehype-doc-links.mjs`:** a section's `README.md` link now maps to the section
  root (`/guide`, `/spec`) instead of a dead `/guide/README`.

Verified: `astro build` → 22 pages (was 20), incl. `/guide` + `/spec`; Pagefind
indexed 20; `astro check` 0/0/0; markdownlint 0; prettier clean. No commit (R12).
