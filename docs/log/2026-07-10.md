# 2026-07-10

- Fixed a security vulnerability (`RUSTSEC-2026-0204`) in `crossbeam-epoch` by updating from version 0.9.18 to 0.9.20 via `cargo update -p crossbeam-epoch`.
- Updated `.github/workflows/deploy-site.yml` to gracefully skip the `vercel deploy` step if the `VERCEL_TOKEN` is empty. This prevents CI failures on PRs coming from forks where secrets are hidden by GitHub Actions.
