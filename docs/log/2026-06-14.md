# 2026-06-14 — Phase 1.8 grammar engine: the expression-first flips

Branch `feat/phase-1.8-grammar-flips` (off the keystone
`feat/phase-1.8-grammar-engine`). Landed three more `thamizh-order` clause
flips from `spec/04` §3 over the **same AST** as code-order: the seq
conditional (`<cond> endral { } illaiyel { }`), the if-expression
(`c endral { a } illaiyel { b }`), and match (`<expr> poruthu { }`). With the
clocked block from the keystone, four of the five §3 flips now ship; only the
**test** flip remains (deferred — see decision below).

## What changed

- **`src/parser/expr.rs`** — `expr` gains a `Profile::Thamizh` branch
  (`expr_thamizh`): parse the operand with `binary(0)`, then one-token trailing
  lookahead — `endral` → `if_expr_thamizh`, `poruthu` → `match_expr_thamizh`.
  The match arm loop is factored into `finish_match`, shared by the code-order
  and thamizh entries. `binary` is now `pub(super)` so `items.rs` can parse a
  clause head as an operand.
- **`src/parser/items.rs`** — `seq_stmt` gains a thamizh branch
  (`seq_stmt_thamizh`): parse the head, then `endral` → `seq_if_thamizh`,
  `<-` → assignment via the new `expr_to_lvalue` (reinterprets the parsed head
  as the `LValue` the code-order path gets from `lvalue()`), `=` → the existing
  E1106 teaching error. The assignment grammar is unchanged; only the parse
  route differs.
- Everything is gated on `profile == Profile::Thamizh`; the code-order path is
  byte-for-byte untouched. No new keywords, `Kw` variants, `keywords.toml`
  entries, or E-codes — `endral`/`illaiyel`/`poruthu` are the existing
  `Kw::If`/`Kw::Else`/`Kw::Match` tokens; errors reuse E1108/E1106/E1101.

## Tests (+8, 192 → 200)

`tests/grammar.rs` (5 → 13), same-Verilog oracle (a thamizh fixture and its
code-order twin must emit byte-identical Verilog):

- conditional → `blinker.thamizh.mimz` + Tamil-script `blinker_tamil.thamizh.mimz`
  vs `examples/tanglish|tamil/blinker.mimz` (+ English-golden agreement).
- if-expression → `comparator.thamizh.mimz` vs `examples/tanglish/comparator.mimz`.
- match → self-contained pair `match.thamizh.mimz` / `match.code.mimz` (the
  tanglish ALU example imports `adder`, which would not resolve from the
  fixtures dir).
- gating negatives → `<cond> endral`, `a > b endral { } illaiyel { }`, and
  `op poruthu { }` are each parse errors **without** `syntax thamizh`.

Gate clean in **debug and release**: `fmt`, `clippy -D warnings`, `cargo test`
(+`--release`), rustdoc. `graphify update` run (R14). No commit (R12).

## Decision: expression-first parsing, one-token lookahead after the operand

- **Context:** the keystone clocked-block flip dispatched on a _leading_ keyword
  (`Kw::Rise`). The conditional/if-expr/match flips are postpositional — the
  clause head trails the operand — so there is no leading keyword to switch on.
- **Decision:** in thamizh mode, parse the operand first with `binary(0)`, then
  decide on the single trailing token (`endral`/`poruthu`/`<-`). In a seq block
  the head may turn out to be either a condition or an assignment lvalue, so
  `expr_to_lvalue` reinterprets the parsed head as the target when `<-` follows.
- **Why:** spec/04 mandates one-token lookahead and **no backtracking**; parsing
  the operand once and classifying it afterwards honors both. Rejected: a
  speculative `lvalue()` parse with backtracking (extra parser state, forbidden
  by the spec); using full `expr()` for the head instead of `binary(0)` (would
  let a bare nested `if`/`match` act as a condition — `binary(0)` keeps the
  parenthesization rule consistent with the code-order match scrutinee).
- **Impact:** `src/parser/expr.rs`, `src/parser/items.rs`, `docs/code/03-parser.md`.

## Decision: defer the test-form flip to Phase 1.5

- **Context:** `spec/04` §3's fifth flip is the test header
  (`M() kaaga "…" sodhanai { }`) plus the flipped test-block conditional.
- **Decision:** do not implement it now; ship the other four flips.
- **Why:** the grammar engine's only oracle is byte-identical **Verilog** from a
  profile-blind backend, but `test` blocks emit no Verilog yet (the
  simulator/testbench backend is Phase 1.5, not started) and no example carries
  a `test` block — so there is no twin and no Verilog to compare. Doing it now
  would need a weaker, span-stripped AST-equality oracle. Wait until Phase 1.5
  gives test blocks a real backend.
- **Impact:** `spec/04-grammar-engine.md`, `docs/plan/phase-1.8-grammar-engine.md`.

## What's next (grammar flips entry)

- The `translate --order code|thamizh` pretty-printer + round-trip tests, then
  the morphology error helper (the remaining Phase 1.8 work items).
- The test-form flip lands with Phase 1.5.

## Security & overflow audit (same day)

Branch `fix/security-hardening` (off the committed Phase 1.8 work). A full
read-only audit of `src/` against malicious/malformed input — overflow, panics,
unbounded recursion, resource exhaustion, memory safety — then fixed everything
real. Findings are documented in the new `docs/audit/` folder (security / bugs /
hardening, with an index README). The compiler emits hardware-level logic, so the
bar was: never crash, never corrupt memory, never silently miscompute, never
exhaust resources on a crafted file.

## What was found and fixed

- **SEC-1 (CRITICAL) — parser stack overflow.** Recursive descent had no depth
  limit; `y = (((…50k…)))` (or `!!!!…x`, or a long `else if` chain) crashed the
  process. Added a `depth` guard (`MAX_DEPTH = 64`) with `enter()`/`leave()` on
  the five recursion necks (`expr`, `unary`, `if_expr`, `seq_if`, `test_if`) and
  a new diagnostic **E1113**. (256 was tried first and still overflowed before
  tripping — each source level is ~12 Rust frames and the CLI runs on the 1 MB
  Windows main-thread stack, so the cap must suit the smallest stack.)
- **SEC-2 (HIGH) — unhardened simulator evaluator.** `sim::comb`'s `const_eval`
  was a naive copy of the checker's evaluator (raw `i128` `+ - * << >> neg`,
  truncating `as u32` casts). The `mimz eval` path skips the checker
  (`main::eval_file`), so it was the only guard — and had none → debug panic /
  release silent-wrap on a crafted const. Fixed by **delegating to the checker's
  hardened `consteval::eval`** (single source of truth), plus `checked_index`
  for bit indices/slice bounds, `checked_width` for `extend`/`trunc`,
  `wrapping_sub`/`wrapping_neg` in `binary`/`unary`, and a `u64` concat sum.
- **BUG-1 (HIGH) — already fixed in `e519690`**, recorded in `docs/audit/bugs.md`:
  a stray top-level `}` spun `file()` into an OOM hang.
- **Hardening:** `#![forbid(unsafe_code)]` (lib + bin — locks the
  no-buffer-overflow guarantee), release `overflow-checks = true`, a 32 MB
  source-size cap in `read_source`, and per-process bench temp paths.
- **Corrected the audit's own over-rated findings** after verifying against the
  code: width arithmetic (capped at `MAX_WIDTH`), `repeat` (budget 4096), import
  cycles (detected), import path traversal (not expressible) are all already
  safe. Logged in `docs/audit/hardening.md` so they are not re-investigated.

## Tests (+5, 192 -> 197)

- Parser unit (+2): `deeply_nested_expression_errors_not_overflows`,
  `deeply_nested_unary_errors_not_overflows`.
- `eval` integration (+3): `oversized_shift_const_does_not_panic`,
  `overflowing_multiply_const_does_not_panic`,
  `out_of_range_index_is_rejected_cleanly`.
- Gate clean **in debug and release**: `fmt` · `clippy -D warnings` ·
  `cargo test` · `cargo test --release` (verifies nothing legitimate panics under
  the new `overflow-checks`) · rustdoc · prettier · markdownlint. `graphify
update` run (R14). No commit (R12).

### Decision: one evaluator, not two

- **Context:** SEC-2 existed because `sim::comb` reimplemented constant
  evaluation instead of reusing the checker's, and the copy drifted (unhardened).
- **Decision:** `sim::comb::const_eval` delegates to `checker::consteval::eval`
  (already `pub(crate)`, already shared by the emitter). The naive copy is
  deleted.
- **Why:** a single hardened evaluator cannot drift out of sync again; the
  checker's version already has `checked_*` arithmetic and the E0202 overflow
  diagnostic. Rejected: re-hardening the copy in place (leaves two
  implementations to keep aligned forever).
- **Impact:** `src/sim/comb.rs`, `docs/audit/security.md`.

### Decision: conservative MAX_DEPTH + release overflow-checks

- **Context:** the depth cap must hold on the smallest stack the parser ever runs
  on (1 MB Windows main thread), and release builds previously wrapped on
  overflow silently.
- **Decision:** `MAX_DEPTH = 64` (empirically safe; 256 overflowed), and
  `overflow-checks = true` in `[profile.release]` as a defense-in-depth backstop
  behind the checked arithmetic.
- **Why:** correctness of emitted hardware is safety-critical; a loud abort beats
  a silent miscompile. A future move of parsing onto a dedicated large-stack
  thread could raise the cap for machine-generated input.
- **Impact:** `src/parser/mod.rs`, `Cargo.toml`, `docs/audit/`.

## Continuous fuzzing (follow-up, same day)

Landed the `cargo-fuzz` target the audit recommended, closing the "ongoing
assurance" gap. New standalone `fuzz/` crate (`mimz-fuzz`, own manifest +
`[workspace]` so the root `build`/`clippy`/`test` gate never sees it) with one
target `lex_parse_eval`: NFC-normalize the raw bytes (mirroring
`project::read_source`) → `lexer::lex` → `parser::parse` →
`sim::comb::eval_outputs` (empty input/param maps — still drives the SEC-2
constant-evaluation path). libFuzzer treats any panic / abort / hang as a
finding, so it asserts the audit's core guarantee by construction.

- **CI:** new `fuzz` job in `.github/workflows/ci.yml` — ubuntu + nightly,
  `cargo install cargo-fuzz`, 60 s smoke per push/PR. Bounded so it gates without
  dominating CI; a crash writes a reproducer to `fuzz/artifacts/` and fails the
  build.
- **Platform constraint:** libFuzzer needs nightly and runs on Linux/macOS only
  — it does **not** build on this Windows dev box. So the harness is **authored
  here, verified in CI** (local devs use WSL2/Linux). Verified locally only that
  the existing gate is unaffected: `fuzz/` is excluded from root cargo commands.
- Docs synced: `docs/audit/hardening.md` (Ongoing assurance → done),
  `docs/audit/README.md` (audit-log row), `docs/code/10-test-map.md` (fuzz
  section, explicitly outside the 197-test count). `graphify update` run.

## Merge reconciliation + post-merge audit

Merged the security-hardening branch into the Phase 1.8 grammar-flips branch.
Both had edited the same parser files, and the merge silently combined them
**malformed**. Found and fixed, then re-audited the whole change set (two
independent reviewer passes over `git diff e519690`):

- **Conflict markers** left in `tests/grammar.rs` — removed, kept the 8 flip
  tests.
- **Dropped struct fields (compile-breaking):** the merged `Parser` struct lost
  `depth`/`too_deep`, but `enter`/`leave` (and every guarded neck) still
  referenced them. Restored both fields + their `parse()` initializers.
- **Unguarded thamizh recursion (SEC-1 regression):** the new `if_expr_thamizh`
  and `seq_if_thamizh` had no depth guard, so a deep thamizh `illaiyel … endral`
  chain could stack-overflow even though `MAX_DEPTH` was restored. Wrapped both
  with `enter()/leave()` (the `_inner` split). Locked by
  `deeply_nested_thamizh_else_if_errors_not_overflows` (200 levels → clean
  E1113, no abort).
- **Profile-boundary looseness (audit finding):** `primary()` dispatched
  `if_expr`/`match_expr` ignoring the profile, so a leading `endral`/`poruthu`
  (code order) inside a `syntax thamizh` file silently parsed as code order.
  Made `primary` profile-aware — it now emits E1101. Locked by
  `code_order_if_is_rejected_in_thamizh`.
- **Test-map duplication:** the merge produced three copies of the grammar
  section + a stale count. Consolidated to one (15 tests) and corrected the
  total to **207**.

The audit confirmed everything else combined correctly: all security hardening
(forbid-unsafe ×2, release overflow-checks, `MAX_DEPTH`/E1113, `comb.rs`
delegating to the hardened `consteval::eval` + `checked_index`/`checked_width`,
32 MB source cap, per-PID bench temp, the `fuzz/` target, the `docs/audit/`
folder) and all four thamizh-order flips are PRESENT + CORRECT. Code-order
behavior is unchanged (every flip gated on `profile == Thamizh`). Gate green in
debug **and** release: `fmt`, `clippy -D warnings`, `cargo test` (207),
`cargo test --release`, rustdoc, prettier, markdownlint. `graphify update` run.
Still mid-merge — not committed (R12).

One pre-existing, non-blocking note (not from this work): the runtime
`BinOp::Shl` arm in `sim::comb` casts the shift amount `r.bits as u32`, which
truncates a >2³² runtime shift; `checked_shl` still prevents any panic/overflow,
and it is on the not-yet-built simulator path, so it is a low-severity cleanup
for Phase 1.5, not a hardware-correctness or crash risk.

## `translate --order` — the AST pretty-printer (later same day)

Branch `feat/phase-1.8-translate-order` (off master, after the grammar flips +
security landed via PR #5). Added `mimz translate --order code|thamizh`: the
word-ORDER half of `spec/04`'s translate, complementing the existing
keyword-flavor `--to`. New module `src/pretty.rs` — a full AST → Min-Mozhi-source
pretty-printer parameterized by `(Flavor, Order)`; only `OnBlock`/`SeqStmt::If`/
`IfExpr`/`Match` are order-sensitive (test header + test `if` stay code-order,
the test-form flip being deferred). Keyword spellings reuse `TABLE.canonical`,
so flavor and order compose: `--order thamizh --to tamil` yields natural-word-
order Tamil with an `இலக்கணம் தமிழ்` header.

- **CLI** (`src/main.rs`): `--to` is now optional (default english); `--order`
  added. No `--order` → the existing trivia-preserving token reskin; with
  `--order` → parse + pretty-print.
- **Tests (+5, 207 → 212):** `tests/translate.rs` (+4): every example × flavor ×
  order pretty-prints to byte-identical Verilog (semantic identity) and the
  printer is idempotent; plus the directive + CLI checks. `tests/grammar.rs`
  (+1): a committed Tamil thamizh-order traffic-light fixture (generated by the
  tool, exercises all four flips) compiles to the same Verilog as its twin.
- Two real bugs found+fixed while testing: match arms must be **newline**- not
  comma-separated; and an instance's parameter list needs explicit `()` even
  when empty (`Module() { … }`).

### Decision (R3): `--order` output is canonical, not trivia-preserving

- **Context:** the AST carries no comments and no original layout (only spans),
  so an AST pretty-printer cannot reproduce them.
- **Decision:** `--order` re-emits canonically formatted source and **drops
  comments** — it is meaning-preserving (same Verilog, same AST), not
  byte-preserving. Trivia-preservation stays with the keyword-only `--to` token
  path. The oracle is therefore semantic (Verilog identity) + idempotency, not a
  byte round-trip.
- **Why:** reordering clause heads needs structure the token path lacks, and the
  user chose the full pretty-printer (it also seeds a future `mimz fmt`).
  Rejected: token-span surgery (smaller, would keep trivia, but brittle for
  arbitrary reorders); carrying comments in the AST (a separate, larger change).
- **Impact:** `src/pretty.rs`, `src/main.rs`, `spec/04` §2, `docs/code/13-tooling.md`.

## Phase 1.8 error-language plumbing (`morph` + `--lang`)

Built the **engineering half** of the spec/04 §5 morphology helper — selection
and the inflection mechanism — leaving the human-authored content for the panel.

- **`src/morph.rs`** (new tooling module): `majority_flavor(tokens)` (counts a
  file's keyword flavors), `effective_lang(cli, tokens)` (`--lang` override else
  majority), `inflect(name, case, flavor)` (the four வேற்றுமை suffixes, data in
  `case_suffixes.toml`), and `localized_msg(diag, src, flavor)` — the render hook.
- **CLI:** `--lang en|tanglish|tamil` on `check`/`compile`/`eval`; the effective
  flavor threads into the human render path (`diag::render_lang` /
  `project::render_diags_lang`). Unknown `--lang` is a clean error. JSON stays
  English (machine contract unchanged).
- **Additive, English-fallback:** the ~36 inline English `self.err()` messages
  are untouched; a localized template is used only where the (stub) catalog
  covers the E-code, else the English `msg` renders verbatim. Proven by
  `uncovered_code_is_identical_across_languages`.
- **Tests (+15, 212 → 227):** 8 `morph` lib unit + 7 `tests/morph.rs` integration
  (selection, inflection, the localized E0501 path, the fallback invariant,
  bad-`--lang` error). Manually verified: `mimz check --lang ta` on a
  double-driven output prints `error[E0501]: y-க்கு …`; `--lang en` and uncovered
  codes are unchanged.

### Decision (R3): build the morphology mechanism now, gate the content on C3

- **Context:** spec/04 §5 wants Tamil error text whose interpolated identifiers
  inflect (`'sum'-ஐ`). Machine-guessed Tamil is the "broken Tamil" the section
  warns against; the real catalog + sandhi rules need the native-speaker panel
  (decision C3), which is unstaffed.
- **Decision:** ship the _mechanism_ (selection + table-driven inflection +
  render hook) now as an **additive, English-fallback** layer; ship the _content_
  as a one-shape STUB (E0501) that exercises the whole path. The committed sandhi
  join is minimal and marked PROVISIONAL. Do **not** refactor the 36 inline
  English messages while English is the fallback.
- **Why:** the user chose the plumbing-now scope; this makes selection +
  inflection real, tested code without baking in unreviewed Tamil, and the panel
  later only adds data (catalog + suffix/sandhi table), no further wiring.
- **Impact:** `src/morph.rs`, `case_suffixes.toml`, `src/diag.rs`,
  `src/project.rs`, `src/main.rs`, `src/lib.rs`, `tests/morph.rs`, `spec/04` §5,
  `spec/03`, `docs/code/13-tooling.md`, `docs/code/10-test-map.md`,
  `docs/plan/phase-1.8-grammar-engine.md`.

## `mimz fmt` — keyword-flavor normalizer

First of the panel-independent batch. `fmt` is the conventional in-place
formatter, built as `translate` pointed at a file: it normalizes every keyword to
one flavor **losslessly** (the token reskin preserves comments/layout — unlike
the AST `--order` printer, which drops them, so `fmt` deliberately does not use
it). Default target is the file's predominant flavor (`morph::majority_flavor`);
`--to` overrides; `-o` redirects; `--strict` reports a mixed-flavor file
(`morph::flavors_used`, new) and exits non-zero while still writing the fix.

- Code: `Cmd::Fmt` + `fmt_file` (`src/main.rs`), `morph::flavors_used`.
- Tests (+5, 227 → 232): `tests/fmt.rs` — normalize-to-majority + idempotent,
  `--to`, `--strict` (warn + non-zero + still fixes), single-flavor clean, `-o`.
- Docs: spec/03 (fmt line marked implemented), `docs/code/13-tooling.md`,
  test-map.

## What's next

- Remaining Phase 1.8: the human-authored Tamil/Tanglish error catalog + final
  sandhi rules (panel-gated, decision C3 — the `morph` mechanism is ready for
  them), and the deferred test-form flip (Phase 1.5).
- Possible fuzz extensions (noted in `hardening.md`): seed corpus from
  `examples/`, derive AST input-port values for runtime eval, a
  `lex -> parse -> compile` backend target, a longer scheduled run.
