# Dev Log — 2026-06-10

> Project day 1 — Phase 0 design sprint.

## What was done

- Wrote the four language spec documents:
  - `spec/01-goals-and-philosophy.md` — goals (beginner-first, safe-by-construction, trilingual-by-design, full-stack ownership), non-goals, tie-breaker principles
  - `spec/02-syntax-and-grammar.md` — full syntax tour, operator/width rules, types, formal EBNF, the seven static safety rules
  - `spec/03-keywords-trilingual.md` — Layer 1 keyword-skin mechanism + DRAFT EN/Tanglish/Tamil word table
  - `spec/04-grammar-engine.md` — Layer 2 design: `thamizh-order` parser profile for natural Tamil word order + morphology-correct error messages
- Wrote reference examples: `adder`, `counter` (English + Tanglish), `alu`, `traffic_light`
- Updated root roadmap: Phase 0 items checked, **Phase 1.8 Grammar Engine** added (parallel with 1.5), Why-Rust decision record added, timeline/deliverables tables updated
- Created `docs/` structure: per-phase plans (`docs/plan/`), this dev log (`docs/log/`), repo rules (`docs/RULES.md`), architecture doc (`docs/architecture.md`), docs index

## Decisions

### Decision: Compiler implemented in Rust

- **Context:** roadmap originally said "Python or Rust"
- **Decision:** Rust, from day one — handwritten lexer + recursive-descent parser
- **Why:** avoids a guaranteed rewrite when the simulator/synthesis phases need performance; single static binary = zero-friction install for students (goal G1); enums + exhaustive match fit compiler work; `miette`/`ariadne` give teaching-quality diagnostics. Rejected: Python (perf wall, env friction), Go (no sum types/pattern matching)
- **Impact:** roadmap Phase 0 decision record; architecture doc section 3 crate layout

### Decision: Trilingual support = two layers over one AST

- **Context:** English/Tanglish/Tamil support is the project's identity; original roadmap never specified the mechanism
- **Decision:** Layer 1 (Phase 1): keyword skins — one grammar, three keyword columns in one lexer table, freely mixable per file, `minmo translate` lossless. Layer 2 (Phase 1.8): Grammar Engine — `thamizh-order` parser profile with SOV/postpositional clause forms, selected by a `syntax thamizh` directive, producing the identical AST
- **Why:** mixing keywords is the low-friction migration path (G1+G3); keyword-swap alone is not real Tamil (SOV), but full natural-language parsing is out of scope — flipping a small closed set of clause heads gets natural reading at recursive-descent cost
- **Impact:** spec/03 + spec/04 written; roadmap gained Phase 1.8; spec/01 non-goal reworded ("not in v0.1 core" → planned as Layer 2)

### Decision: Safety model ("safe like Rust", translated to hardware)

- **Context:** goal — Go/TS feel, Rust-grade safety, for an HDL
- **Decision:** seven compile-time rules (spec/02 section 6): exact widths with lossless `+`/`-`/`*` growth and explicit wrapping `+%` family; single driver; exhaustive `match` / mandatory `else` on wire-driving `if`; mandatory reg reset values; `=` wires-only vs `<-` regs-only; clock/reset as dedicated types; combinational DAG check
- **Why:** each rule kills a classic Verilog footgun (truncation, latch inference, multi-drivers, X-state, blocking/non-blocking confusion, CDC, comb loops) at compile time with a teaching error
- **Impact:** spec/01 section G2 table, spec/02 section 6, all examples written to demonstrate the rules

### Decision: Repo process rules adopted

- **Context:** need the repo to always tell the true story as plans evolve
- **Decision:** `docs/RULES.md` — sources of truth (R1), plan changes update plan+roadmap+log same day (R2), major decisions get spec bump + Decision log block (R3), append-only dated logs (R4), spec versioning (R5), conventions (R6), session-close checklist (R7)
- **Impact:** this log file is the first artifact of the process

## Design review (same day, later session)

Full consistency pass over spec/01–04, plans, architecture, examples →
`docs/open-questions.md` created (the living question register).

- **6 inconsistencies found** (I1–I6): missing `const` grammar production;
  `and/or/not` contradiction between spec/02 "universal operators" and spec/03
  keyword table; test statements `tick`/`expect` absent from EBNF; orphan
  `signedval` builtin; `alu.minmo` cross-file instantiation with no import
  semantics; comparison-over-bitwise precedence reproduces C's `x & 1 == 0`
  footgun.
- **31 open questions** registered across identity (A), semantics (B),
  trilingual design (C), plan/process (D), branding (E). Biggest gaps:
  no generate/repeat construct (B7), signed-arithmetic rules undefined (B2),
  multi-clock policy undefined (B3), review panel unstaffed (C3), solo-dev
  parallelism conflict between Phases 1.5/1.8 (D3).
- Answers will land as Decision blocks per RULES R3/R4 and drive the spec
  v0.1 → v0.2 bump.

## Decisions applied — design review answered (same day, third session)

The founder answered all 44 register questions + 6 inconsistencies (register
archived: `docs/archive/open-questions-2026-06-10.md`). Specs bumped to
**v0.2**; all docs/plans/examples cascaded. The batch:

### Decision: Philosophy locked (spec/01 v0.2 rewritten)

- **Decision:** ranked why (1 learn compilers · 2 help TN students · 3 prove
  Indian tooling · 4 mastery); persona = 20-y/o TN polytechnic student not
  comfortable in English; **educational project** — success = one person
  learns/appreciates, failure = founder learns nothing; not-for professionals;
  acceptable losses = global/industry/parity/academic recognition; 10-yr scene
  = self-sustaining community repo; tie-breakers = honesty > safety >
  readability > speed > brevity > Tamil idiom (Tamil _presence_ constitutional)
- **Constitution:** open source forever (MIT + Apache-2.0 dual) · Verilog
  interop forever (always emits; future: wrap external Verilog modules) ·
  Tamil first-class forever
- **Measurable G1:** basics in 1–2 h; counter compiles in 5 min from install

### Decision: Naming — `.mimz` files, `mimz` CLI

- **Why:** founder's call (A7/E1); short, collision-safer than `minmo`
- **Impact:** all specs, plans, README, roadmap, architecture; examples renamed

### Decision: Language semantics batch (spec/02 v0.2)

- Rust-style precedence (bitwise > comparison; comparisons non-associative) — kills `x & 1 == 0`
- Logical ops: **both** `&&`/`||`/`!` (universal) and `and/or/not` (translated aliases) — documented G1-x exception; `mimz fmt --strict` normalizes
- `signed[N]` ships in v0.1 code: no mixing, `signed()`/`unsigned()` casts (replace orphan `signedval`), type-directed `extend`, signed-only negative literals & unary minus (lossless N+1)
- Added: `const` declarations (was a missing production), `repeat i: lo..hi` compile-time generation, `import` semantics (file-relative, project-unique names, no cycles), full test grammar (drives/`tick`/`expect`)
- Cut: `on fall` (reserved); division/modulo declared deliberately absent
- B-block accepted as recommended: no div/mod, multi-clock reg-ownership rule, reg-requires-reset, inout→Phase 2, memories→Phase 2, structs→post-P2, match ranges/wildcards→v0.3+, ASCII digits only, naming = linter warnings

### Decision: Trilingual policy (spec/03 v0.2)

- One canonical phonetic Tanglish spelling per keyword (no diacritics, no aliases) + did-you-mean; TN SCERT textbook alignment as selection criterion; panel = tech/coder friends, panel majority wins over founder once formed; new keywords repeat/tick/expect added, fall removed

### Decision: Process & plan

- Solo dev; execution order **1 → 1.8 → 1.5** (grammar engine before simulator)
- Proposed deadlines (~8–10 h/wk assumption): Phase 1 → 31 Dec 2026 · 1.8 → 28 Feb 2027 · 1.5 → 31 May 2027 · Phase 2 → mid-2028
- v0.1.0 tag when executable + testable; repo **private until after Phase 1.8**, then public; no FPGA board yet → Phase 2 verifies bitstream in CI/emulation, hardware demo when board acquired; VS Code TextMate grammar + CI-from-first-commit pulled into Phase 1; docs stay English-only until post-Phase 1, then Tamil translation begins

## Repo scaffolding (same day, fourth session) — Phase 1 work item 1 begun

- `git init` done by founder (branch `master`, no commits yet)
- Added: `LICENSE-MIT` + `LICENSE-APACHE` (dual, per decision A5),
  `.gitignore` (Rust/target, outputs, IDE/OS, agent scratch), `.gitattributes`
  (LF normalization incl. `.mimz`), `.editorconfig`,
  `.github/workflows/ci.yml` (fmt + clippy + test + build — decision D5,
  activates on push to GitHub)
- Installed Rust via winget/rustup (stable 1.96.0); first attempt left the
  toolchain half-installed (missing rustc manifest), recovered to a clean
  stable default
- `cargo init --name mimz` at repo root; Cargo.toml filled in (version
  `0.1.0-dev`, dual license, repo `github.com/Naveen2070/min-mozhi`,
  edition 2024, planned deps noted as comments); placeholder `main.rs`
- Verified: `cargo build` ✅ · `cargo run` prints banner ✅ ·
  `cargo fmt --check` ✅ · `cargo clippy -D warnings` ✅

## The compiler is born (same day, fifth session) — Phase 1 items 2, 3, most of 5

Implemented in Rust (~2,400 lines incl. tests), all green: 25/25 tests,
clippy `-D warnings`, fmt.

- **`keywords.toml`** — trilingual table as data; loader asserts column
  disjointness; reserved-word list (caught a TOML gotcha: root keys must
  precede tables — the `reserved` list silently landed inside `[keywords.not]`
  until tests flagged it)
- **Lexer** (`src/lexer/`) — NFC + Unicode XID idents (Tamil script works),
  union-of-flavors keyword recognition with flavor recorded per token,
  Go-style newline continuation, spans everywhere; teaching rejections for
  `/`, `%`, reserved words, Tamil digits
- **Parser** (`src/parser.rs`) — full v0.2 grammar: modules, ports, wire/reg,
  const, enum, let-instances, on-blocks, repeat, imports, tests, expressions
  with Rust precedence (the `x & 1 == 0` defusal has a locked-in test),
  non-associative comparisons, error recovery (3+ errors per run verified)
- **Verilog emitter** (`src/emit_verilog.rs`) — parameterized modules
  (symbolic widths → Verilog parameters, no const-eval needed), sync
  active-high reset generated from reg reset values, enums → localparams,
  match → ternary chains, instances with auto-wired outputs and implicit
  clk/rst; clean not-yet-supported errors for `repeat`/non-ASCII idents
- **CLI** — `mimz check` (multi-error, caret diagnostics with spec
  references), `mimz compile` (resolves imports file-relative, emits one .v)
- **Proof of the trilingual thesis:** `counter.mimz` and
  `counter.tanglish.mimz` compile to byte-identical Verilog (asserted in CI)

## Doc audit (same day, sixth session)

First commit landed (`a2fefd7` — scaffold + docs + examples). Audit fixes:
stale `minmo`/`.minmo` references in phase-2/3/4 plans → `mimz`/`.mimz`;
README status updated (was "nothing compiles yet" — now Phase 1 in progress
with Build/Test/Run instructions added); architecture header updated (front
end built); docs index Phase 1 → 🟡; code fences `minmo` → `mimz`.
Log/archive history left untouched (append-only, R4).

## Refactor: module-scoped source layout (same day, seventh session)

Split the two monolith files into scoped modules (no behavior change —
25/25 tests, clippy, fmt all green before and after):

- `parser.rs` (1176 lines) → `parser/{mod,items,expr,tests}.rs`
  (148/595/370/85) — mod: state+plumbing; items: declarations & blocks;
  expr: precedence climbing & patterns. Added a `help()` helper replacing
  the repeated `diags.last_mut().unwrap().help = ...` pattern.
- `emit_verilog.rs` (578) → `emit_verilog/{mod,module,expr}.rs` (134/322/145)
- lexer unit tests → `lexer/tests.rs` (mod.rs 420 → 360)
- `main.rs` (218 → 136) — source loading + import resolution extracted to
  `project.rs` (91); main is CLI-only
- Cross-module visibility via `pub(super)` on the few entry methods;
  everything else stays private to its file
- `docs/architecture.md` section 3 layout updated to the real tree
- Follow-up: `ast.rs` (278) → `ast/{mod,expr}.rs` (167/120) — items vs
  expressions, with `pub use expr::*` so all `crate::ast::*` consumers are
  untouched. Every source file is now ≤ 595 lines, most under 200

## Architecture review (same day, eighth session)

Founder asked whether the architecture holds up long-term. Assessment
recorded in `docs/architecture.md`:

- New **section 5 Evolution Triggers** — the staged-on-purpose pieces and exactly
  when each must change: emitter loses semantics to the checker; IR demotes
  the string emitter; **stable error codes before any message translation
  (P1.8)** — flagged as the one early must-do; `lib.rs`/workspace split only
  when a second front-end consumer exists; trivia-preserving lexing only
  when `fmt` starts; interning only if perf ever matters.
- New **section 3 Repository layout** — spec/ (the language, normative) vs docs/
  (this implementation + process) separation rule; future dirs
  (tools/vscode, stdlib/, crates/, targets/, site/) listed with the trigger
  that creates each.

## Markdown tooling (same day, ninth session)

- Added **Prettier** (`.prettierrc`, `.prettierignore`) and **markdownlint**
  (`.markdownlint-cli2.jsonc`) for docs; one-time `prettier --write` pass
  normalized 8 files; all 17 md files now lint clean
- Config notes: MD060 (table pipe alignment) disabled — Tamil combining
  marks make visual width ≠ char count, so Prettier and that rule can never
  agree on Tamil tables; MD013/MD040/MD036/MD033 off (wide tables, ASCII
  diagrams); `docs/archive/` exempt from both tools (append-only history)
- One genuine fix: roadmap's two adjacent blockquotes merged (MD028)
- CI gained a `docs` job: markdownlint-cli2-action + `prettier --check`
- Commands documented in README (Build/Test) and RULES R6

## Open items / next

- **Second commit: the compiler itself** (src/, keywords.toml, tests/ are untracked)
- Early must-do from the review: introduce stable error codes (`E0001`…) before the error catalog grows
- Native-speaker review of the Tanglish/Tamil keyword table (weakest picks: `let`→`vai`, `not`→`illa`, new `tick`→`thattu`)
- Next: checker passes (work item 4 — widths, single-driver, exhaustiveness, const-eval which unblocks `repeat` emission), install Icarus Verilog for simulation tests

## Code documentation pass (same day, tenth session)

Two housekeeping requests: remove the `§` symbol everywhere (replaced with
the word "section" — in all markdown except `docs/archive/`, and in all
Rust source including user-facing help messages), then a full rustdoc pass
over `src/`.

- Every type, enum variant group, and non-trivial function now has a `///`
  doc comment; `main.rs` gained a crate map table and the
  `cargo doc --document-private-items --open` entry point
- Parser routines document their EBNF production from `spec/02` section 5
  (e.g. `inst = "let" ident ...`) — grammar and code can now be diffed
- Doc comments explain the WHY where it is not obvious: why `else` is
  optional in seq-`if` but mandatory in if-expressions, why `raw` literal
  spellings are kept, why the keyword table panics at startup
- `cargo doc` builds with zero warnings; fmt/clippy/tests all green (25/25)
- Convention recorded in RULES R6; `cargo doc` added to README Build/Test

## Maintainer code docs + CONTRIBUTING (same day, eleventh session)

New `docs/code/` folder — markdown explaining HOW the code works, for
maintenance and future contributors (rustdoc covers items; these cover
the picture between files):

- `README.md` (index + 60-second pipeline), `01-pipeline.md` (end-to-end
  walk of `mimz compile`), `02-lexer.md`, `03-parser.md`, `04-ast.md`,
  `05-emit-verilog.md`, `06-diagnostics.md` (incl. how to write a good
  error), `07-decisions-and-evolution.md` (code-shaping decisions, honest
  "how it turned out" notes, what comes next), `08-contributing.md`
  (recipes: change a keyword word, add a keyword, add a syntax form,
  extend the emitter, testing conventions)
- Root `CONTRIBUTING.md` added — quick orientation that defers to
  `docs/code/08-contributing.md` for details
- Wired into `docs/README.md`, root README table, and RULES R1 (rule:
  update the matching `docs/code/` page the same session behavior changes)
- Accuracy findings while writing: emitter does NOT reorder clock/reset
  ports first (doc corrected to "declared order"); project-level diags
  render against the entry file's source even for imported-file spans —
  recorded as a known bug in `06-diagnostics.md`, fix planned with the
  checker

## Doc-quality gaps closed + multi-file diag fix (same day, twelfth session)

Acted on the documentation self-review: all four gaps fixed, plus the
recorded Diag bug.

- **Bug fix — diagnostics know their file now.** `Diag` gained
  `file: Option<usize>`; `Project::from_files` and the `Emitter` stamp it
  (new `cur_file`), and the CLI renders project-level errors via new
  `project::render_diags`, which picks each diagnostic's own source.
  Previously every project-level error rendered against the entry file —
  wrong excerpt for errors in imported files. Regression test:
  `diags_carry_the_file_index`.
- **`docs/code/09-walkthrough-counter.md`** — counter.mimz traced through
  every stage with REAL artifacts (actual token dump with offsets, the
  AST shape, the byte-for-byte emitted Verilog) and what to notice at
  each step.
- **`docs/code/10-test-map.md`** — every test's intent ("what a failure
  means"), the deliberately-uncovered list with closure conditions, and
  house rules for creating new tests.
- **`tests/docs_sync.rs`** — mechanical staleness guard (4 tests): both
  crate-map copies must list every module, module pages must list every
  source file, new modules must be documented, the "Last synced" stamp
  must survive. Doc drift now fails CI.
- Test count 25 → 30 (file-index test + 4 docs-sync). README status,
  test map, and reading order (walkthrough slotted after pipeline)
  updated. Full gate green: fmt, clippy -D warnings, 30/30, md checks.
