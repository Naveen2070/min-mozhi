# 2026-08-10

## v0.2 release-readiness review, round 4 — do-not-ship, gate 3 of 5

Filed [`docs/audit/review-2026-08-10.md`](../audit/review-2026-08-10.md) on
`master` at `359e5db` (4 commits since round 3's base `96f95e3`). Read-only
review: no source change was committed. Every revert experiment below was
restored and the working tree verified clean (`git diff` empty) before filing.

Toolchain: Icarus Verilog 14.0 (devel), rustc/cargo 1.97.0, Windows 11.

### Round-3 plan Tasks 1–9: all verified done

Held to the standard round 3 set, plus two things no prior round did — the
claimed build enforcement was **triggered**, and the fuzzer's acceptance test
was **re-run from scratch**.

- **Task 1 (BUG-48)** — all three repros hoist correctly and match real
  `iverilog`+`vvp` at the exact filed values (350, 2575, 350). Revert-proven:
  with both arms reverted, exactly the five hand-written BUG-48/49 tests fail
  and nothing else.
- **Task 2 (BUG-49)** — both trunc shapes elaborate at `iverilog -g2005 -t null`
  (exit 0) and compute 6. The decision to drop a dedicated `-t null` helper is
  correct: `differential`/`differential_clocked`'s own `iverilog` BUILD step
  already asserts success before any value compare.
- **Task 3 (GAP-13)** — build enforcement is real, and I triggered it on **both**
  copies. Removing `ExprKind::EnumConstruct` from `kinds::infer_kind` →
  `error[E0004]`, exit 101. Removing `ExprKind::Match` from
  `expr_kind_self_determined_coverage` → same. The stretch goal genuinely
  landed; `infer_kind` has no `_ => None`.
  **Scope note for the record:** the property enforced is "a _new_ `ExprKind`
  variant cannot be silently unclassified," not "an existing arm cannot be
  silently too narrow." BUG-48 was the second kind, and with Task 1 reverted the
  exhaustive axis still compiles and still passes.
- **Task 4 (fuzz vocabulary)** — acceptance criterion reproduced exactly as
  claimed. Task 1 reverted, `MIMZ_DIFF_FUZZ_N=400 MIMZ_DIFF_FUZZ_CLOCKED_N=400`:
  clocked caught it at seed `202427629` (**fresh index 0**, not in the corpus),
  comb at `12648435` (index 5). Both in 0.95 s.
- **Task 5** — `ci.yml:18` is `0 4 * * *`; `fuzz-nightly` gated on exactly
  `0 4 * * *`, `nightly-bench` on exactly `0 3 * * *`. The two daily crons do
  not cross-trigger. Verified, not assumed.
- **Task 6** — guide §Shifts states the shipped rule (`bits[W] << k` is
  `bits[W+k]`, runtime amount grows by `2^N - 1`, `>>` does not grow, cost and
  rationale both given). E0401's help now teaches shift growth at the point of
  failure. CHANGELOG announces the breaking change.
- **Task 7** — all five records reconciled and consistent with their bodies
  (BUG-41 FIXED, GAP-11 CLOSED with the "stays open" sentence gone — 0 matches
  repo-wide, GAP-12 CLOSED, HARD-7 DONE, README rows present).
- **Task 8** — the "cover misses time zero" disposition confirmed against real
  Icarus with my own case (a condition true from t=0 that never toggles):
  `HITS 1`. Round 2's claim does not hold; reverting the naive `initial #0` fix
  was correct. **BUG-51 reproduced from scratch** by rewriting the emitted
  testbench's stimulus drives back to blocking `=`: the register never resets,
  `y` stays `xxxx` for the whole run. Fix is real and load-bearing.
- **Task 9** — the GAP-11(a) deferral is documented honestly at the source
  (`differential_fuzz.rs:154-166` says the assert cannot fire today and why),
  with the non-tautological half separately implemented at `:1446-1460`.

### BUG-50 reproduced from scratch

Reverted `Replicate`'s `per_iter * n` to `per_iter`: the hoisted wire drops from
`wire [5:0]` to `wire [2:0]`, and `trunc({2{p0}}, 5)` reads back `y = xx101`
under `vvp` — the filed failure mode exactly (top copy undriven). Fix confirmed
load-bearing.

### Four new defects, two of them CRITICAL

- **[BUG-52](../audit/bugs.md) (CRITICAL)** —
  `verilog_self_determined_kind` still ends `_ => None` over `ExprKind`. That is
  the one cell of the gate×classifier / `Builtin`×`ExprKind` grid that four
  rounds never audited: Task 3 made the _gate_ exhaustive and left the
  _classifier_ open. So an `if`/`match`/unary sitting in a self-determined
  position with a width-mismatched child skips the hoist. Four repros; repro A
  emits **byte-identical wrong Verilog to BUG-28's Repro A**
  (`{b, ((s) ? ((a)) : ((a)))}`, mimz 2575 vs Icarus 175).
  Fix written and verified in-review — three arms mirroring `Min`/`Max`, all
  repros corrected, 1229/0 suite green, 2000 seeds clean — then **reverted**;
  HEAD is unmodified.
- **[BUG-55](../audit/bugs.md) (CRITICAL)** — BUG-47's signed-`>>` context hoist
  does not recurse into `if`/`match` branches, so a _previously fixed_ bug is
  live one AST node deeper. `mimz eval` 0 vs Icarus 3. Confirmed independent of
  BUG-52: with BUG-52 fixed the clocked fuzz goes green at 1000 seeds while comb
  seed `12649355` still fails.
- **[BUG-53](../audit/bugs.md) (HIGH)** —
  `insert_repeat_instance_output_kinds` keys array-instance ports by the
  `repeat` **loop counter** while `inst_name` renders them from the folded
  `inst.index`, and it does not recurse into nested `repeat`/`const if` the way
  `emit_instances` does. Three repros (`let s[i + 1]`, nested `repeat`,
  `const if` inside `repeat`), all Icarus 174 against a declared `bits[9]`
  requiring 350. Control case (`repeat i: 1..2 { let s[i] }`) hoists correctly,
  which pins the diagnosis. Secondary finding: `mimz check` accepts all three,
  `mimz compile` miscompiles them, and `mimz sim` rejects them — three
  components disagreeing about whether the program exists.
- **[BUG-54](../audit/bugs.md) (HIGH)** — `--emit-testbench`'s `expect` renders
  `if (!(cond))`, so an `x`-valued comparison prints **PASS**. Verified: with
  BUG-51 reintroduced, `y = xxxx`, `(y == 5)` is `x`, and the testbench reports
  PASS. Consequence:
  `emitted_testbench_reset_deassert_does_not_race_the_dut` cannot fail on the
  regression it exists to catch. Its sibling
  `emitted_testbench_prints_the_cover_summary` **is** load-bearing
  (`clocked first edge: 1` vs `0`). BUG-51's own bugs.md entry claims the
  `expect` "correctly fails here" for an `x` compare; it does not.

### [GAP-14](../audit/gaps.md) — the gate was scored at the wrong depth

The deep fuzz at HEAD, nothing reverted,
`MIMZ_DIFF_FUZZ_N=2000 MIMZ_DIFF_FUZZ_CLOCKED_N=2000`: **exit 101**, 2 failed.
Clocked seed `202428078` (fresh index **449**) → BUG-52. Comb seed `12649355`
(index **925**) → BUG-55. Both are past the per-PR depth of 400 and inside the
nightly depth of 5000 that Task 5 had just made daily.

So the instruments see this class now — Task 4 is why, and it out-performed a
domain-expert hand sweep on BUG-55. What failed is the gate _procedure_: gate 5
was scored from a 400-seed run. Round 3 recorded the identical miss ("BUG-47 was
found at i=642, past the per-PR depth of 400") and used it to argue for the
daily cron, but nothing changed how the gate is scored.

Two seeds to append to `tests/fixtures/fuzz-seeds/` once fixed, so neither is
reachable only from a nightly:

```text
comb.txt     12649355   # i=925  BUG-55
clocked.txt  202428078  # i=449  BUG-52
```

### Verification runs

| command                                                              | result                                                                               |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| `cargo build --release`                                              | exit 0                                                                               |
| `REQUIRE_IVERILOG=1 cargo test --workspace --release --no-fail-fast` | **exit 0**, 35 suites, **1229 passed / 0 failed**, 85.0 s test execution             |
| `cargo fmt --check`                                                  | exit 0                                                                               |
| `cargo clippy --workspace --all-targets -D warnings`                 | exit 0 (the single "warning" is the build script's own git-hooks notice, not a lint) |
| deep fuzz 2000/2000 at HEAD                                          | **exit 101**, 2 failed — BUG-52, BUG-55                                              |
| fuzz 400/400 with Task 1 reverted                                    | 2 failed, clocked fresh index 0, comb index 5                                        |
| candidate BUG-52 fix: full suite                                     | exit 0, 1229 passed / 0 failed                                                       |
| candidate BUG-52 fix: fuzz 1000/1000                                 | clocked green, comb still fails at 12649355 (= BUG-55)                               |

### Ratings delta vs round 3

Compiler quality 6 → **5.5**, HDL correctness 5 → **4**, Overall architecture
6 → **5.5**, Maintainability 7.5 → **8** (Task 7 done properly),
Testing 7 → **8** (the harness found two CRITICALs a hand sweep missed).
New dimension, **Release process 4/10**, scored separately because that is where
this round actually failed.

Family total in the "two implementations of one width rule disagreed" bucket:
**21** bugs. GAP-1's priority raised to 🔴 — four rounds have now shown that
hardening the emitter's shape analysis one cell at a time does not converge.

## Round-4 plan Task 1 (GAP-14) — done

Filed [`docs/plan/v0.2-class-closure-round4.local.md`](../plan/v0.2-class-closure-round4.local.md)
(gitignored, same convention as round 3's plan). Task 1 closed same day: no
code, a procedure change. Added a standing "Release-gate scoring convention"
section to [`docs/audit/README.md`](../audit/README.md) — gate 5 must be
scored at the nightly depth (5000/5000), never the per-PR depth (400/400),
and every review records the depth its gate-5 run used next to the result.
Retrofitted `review-2026-08-10.md`'s own gate table with a depth column.
Next: Task 2 (BUG-52 classifier fix, already written and verified during the
review, just needs landing + its own regression tests).

## Round-4 plan Task 2 (BUG-52) — done, uncommitted

Landed the 3-arm classifier fix in `self_determined.rs`
(`IfExpr`/`Match`/`Unary`) plus explicit reasoned `None` for every remaining
`ExprKind` variant, `_ => None` deleted. 4 new differentials in
`tests/self_determined_regression.rs`, TDD: ran against pre-fix HEAD first
(stashed just the source fix) — all 4 failed at the exact filed values
(2575/175, 2575/175, 2800/160, 3855/255) — then restored the fix and got all
4 green. Caught and fixed my own test bug along the way: `match` arms need
newlines between them, not commas (`E1101`) — bugs.md's inline notation isn't
the real grammar. Re-confirmed `E0004` build-enforcement on this exact patch
(deleting the `BundleLit`/`ArrayLit`/`EnumConstruct` line). Full workspace
**1233/1233** (1229 + 4), 35 suites, fmt/clippy clean. Deliberately did not
hand-minimize the fuzz-found clocked instance (seed 202428078) into its own
test — deferred to Task 7's corpus append, after Task 3 (BUG-55) also lands.
Capped `CARGO_BUILD_JOBS`/`RUST_TEST_THREADS` at 4 throughout (16GB box).
**Not committed** — holding per instruction until told to commit.
Next: Task 3 (BUG-55, signed `>>` recursion into `if`/`match` branches).
