# 2026-07-27

## Full docs audit (docs/code, docs/guide, docs/source-guide, how-the-compiler-works.md, BUILD.md)

Prompted by the `oversized-test-file-split` branch (checker/tests.rs →
`checker/tests/` 11 files, parser/tests.rs → `parser/tests/` 13 files).
Ran 5 parallel audits, each scoped to a distinct doc area:

- `docs/code/10-test-map.md`: fully rebuilt. Master count corrected
  765→1034 (`cargo test-summary --workspace`). Checker/parser sections
  restructured from single-file tables into per-topic-file tables
  matching the new split; 79 previously-undocumented tests (64 checker,
  15+ parser — actual parser gap was 37) given one-line descriptions.
  Keyword-table/lexer/transliteration counts corrected in passing
  (12→13, 13→15, 6→7).
- `docs/code/` (rest): `03-parser.md`/`08-contributing.md`/
  `11-checker.md` test-file-path references updated for the split.
  `06-diagnostics.md`/`13-tooling.md`: stale E-code table (missing
  E13xx) and localization count ("33 of 44" → "33 of 73") fixed.
  `13-tooling.md`: added missing `host.rs` row.
- `docs/guide/09-modules-and-reuse.md`: added an `extern module` section
  — the only active keyword with zero guide coverage.
- `docs/source-guide/`: `08-simulator.md`'s `Val` description corrected
  post-BUG-13 (was still the flat `u128` model). Added coverage for
  `bits.rs`/`wide.rs`/`width_rules.rs`, `extern_module.rs`,
  `sync_prim_lower.rs`, `builtin_bundles.rs`, and the self-determined-
  width emitter files across `01-overview.md`/`02-foundations.md`/
  `04-parser.md`/`05-ast.md`/`06-checker.md`/`07-verilog-emitter.md`.
  `10-ecosystem.md`'s test/fixture/golden counts refreshed (badly
  stale: fixtures 84→117, goldens 50→70, Icarus TBs 38→45).
- `docs/how-the-compiler-works.md`: rewritten for the workspace split
  (paths still pointed at pre-split `src/lexer/` etc.); checker pass
  count corrected 7→9; test count 737→1034; one broken anchor link
  fixed.
- `docs/BUILD.md`: quality-gate command block was missing `--workspace`
  on clippy/test (verified against `.github/workflows/ci.yml`); fixed,
  same for the `cargo test-summary` extras section.
- `docs/code/README.md`: "last synced" stamp refreshed.

Verified `cargo test --test docs_sync` (the mechanically-checked
structural facts) still green throughout. All touched files pass
`npx prettier --check` and `npx markdownlint-cli2`.

Flagged, not fixed (out of scope for a docs-only audit — source code):

- `src/commands/compile.rs:59`: stale comment ("check is the six
  checker passes") — real count is now 9.
- `tests/packages.rs`: doc comment references the old
  `src/checker/tests.rs` path.
- `crates/mimz-sim/src/sim/value.rs`: module doc comment still says
  "≤128 bits" (pre-BUG-13).

No spec/architecture conflicts found — everything fixed was stale prose
or counts, not a behavior disagreement.
