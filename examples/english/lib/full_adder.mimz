// Min-Mozhi example library: one-bit full adder. Imported by
// chained.mimz via the dotted path `lib.full_adder`.

module FullAdder {
  in  a:    bit
  in  b:    bit
  in  cin:  bit

  out sum:  bit
  out cout: bit

  sum  = a ^ b ^ cin
  cout = (a & b) | (cin & (a ^ b))
}
