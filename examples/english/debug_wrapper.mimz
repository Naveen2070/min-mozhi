// Demonstrates: const if for compile-time conditional signal routing
module DebugWrapper {
  const DEBUG: int = 0

  in  data_in: bits[8]
  out data_out: bits[8]
  out dbg_out:  bits[8]

  const if (DEBUG) {
    dbg_out = data_in
  } else {
    dbg_out = 0
  }

  data_out = data_in
}
