module SyncPulse {
    clock clk_src
    clock clk_dst
    reset rst

    in src_pulse: bit
    reg src_reg: bit = 0
    out dst_pulse: bit

    on rise(clk_src) {
        src_reg <- src_pulse
    }

    wire dst_pulse_w: bit = sync.pulse(src_reg, clk_src, clk_dst)

    dst_pulse = dst_pulse_w
}
