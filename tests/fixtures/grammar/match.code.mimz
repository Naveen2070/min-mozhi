// Code-order twin of match.thamizh.mimz (spec/04, Phase 1.8). Self-contained
// (no `import`) so the same-Verilog oracle compares two fixtures directly. The
// match head leads: `y = poruthu op { … }`.
thoguthi Alu(WIDTH: int = 8) {
  ulle  a:  bits[WIDTH]
  ulle  b:  bits[WIDTH]
  ulle  op: bits[2]
  veli y:  bits[WIDTH]

  y = poruthu op {
    0b00 => a +% b
    0b01 => a -% b
    0b10 => a & b
    0b11 => a | b
  }
}
