இலக்கணம் தமிழ்

தொகுதி Counter(WIDTH: int = 8) {
  துடிப்பு clk
  மீட்டமை rst
  வெளியீடு count: bits[WIDTH]
  பதிவேடு value: bits[WIDTH] = 0
  ஏற்றம்(clk) போது {
    value <- value +% 1
  }
  count = value
}
