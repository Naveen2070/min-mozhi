ilakkanam thamizh

thoguthi Counter(WIDTH: int = 8) {
  thudippu clk
  meettamai rst
  veliyeedu count: bits[WIDTH]
  pathivedu value: bits[WIDTH] = 0
  yetram(clk) pothu {
    value <- value +% 1
  }
  count = value
}
