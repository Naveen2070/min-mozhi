# Append-only history — never reformatted (RULES R4)
docs/archive/

# Build output & tool scratch
target/
.remember/
.claude/

# The website has its own toolchain (formats with its own Prettier, not the repo docs gate)
site/

# Local-only plan/workflow docs (git-excluded via .git/info/exclude)
*.local.md

# Not Prettier's formats
*.mimz
*.v
LICENSE-*
