//! Stub — Task 4 fills this in.
use super::super::elaborate::Width;
use super::Peripheral;
use crate::ast::BindArg;

pub(super) fn construct(_width: Width, _args: &[BindArg]) -> Result<Box<dyn Peripheral>, String> {
    unimplemented!("Task 4")
}
